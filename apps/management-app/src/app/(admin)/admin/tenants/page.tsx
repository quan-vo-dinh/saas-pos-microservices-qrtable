import { FeaturePlaceholder } from '@einvoice/frontend-ui';

export default function AdminTenantsPage() {
  return (
    <FeaturePlaceholder
      section="Admin / Tenants"
      title="Tenant Directory Skeleton"
      description="Placeholder for tenant lifecycle controls: activate, suspend, and inspect."
    />
  );
}
