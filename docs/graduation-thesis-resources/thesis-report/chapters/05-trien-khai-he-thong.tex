\chapter{Các luồng vận hành chính của QRTable}

Chương này trình bày kết quả triển khai các luồng vận hành chính từ thiết kế ở Chương 4. Nội dung được tổ chức theo luồng nghiệp vụ đại diện, qua đó làm rõ cách ranh giới dịch vụ, quyền sở hữu dữ liệu, cô lập đơn vị thuê bao, tính lũy đẳng, Saga, Redis, Kafka và WebSocket được áp dụng trong hệ thống.

Các luồng gồm phiên đặt món qua mã QR và giỏ dùng chung; xác nhận đơn và bảo toàn tồn kho; KDS nhận phiếu bếp từ sự kiện; ghi nhận thanh toán tiền mặt, VietQR và SePay; khởi tạo đơn vị thuê bao; bảng điều khiển và báo cáo theo gói dịch vụ. Mỗi luồng được phân tích theo mục tiêu nghiệp vụ, bất biến, dịch vụ sở hữu dữ liệu, thành phần triển khai và phạm vi hiện tại.

\section{Mục tiêu và phạm vi triển khai}

Các luồng triển khai tuân theo ba nguyên tắc từ Chương 4: cơ sở dữ liệu được tách theo ranh giới dịch vụ, BFF là điểm vào của ứng dụng và WebSocket chỉ phát tín hiệu làm mới. Trọng tâm của chương là cách từng luồng duy trì quyền sở hữu dữ liệu và các bất biến nghiệp vụ trong quá trình phối hợp liên dịch vụ.

Phạm vi trình bày gồm cấu trúc dịch vụ, đăng ký mô-đun, thực thể và lược đồ dữ liệu, hợp đồng TCP/Kafka, kiểm thử đơn vị, kiểm thử hợp đồng, kiểm thử tích hợp có điều kiện, sơ đồ luồng và ảnh giao diện. Ảnh chụp thể hiện bề mặt vận hành; mã nguồn, kiểm thử và sơ đồ làm rõ ranh giới giao dịch, tính lũy đẳng, lọc theo đơn vị thuê bao và cơ chế bù trừ.

Chương 5 tập trung vào cấu trúc và kết quả triển khai. Chương 6 tiếp tục kiểm chứng các kết quả này bằng kiểm thử tự động, kiểm thử có điều kiện với SePay và hiện vật vận hành trên môi trường công khai.

\section{Nền tảng triển khai và ranh giới trách nhiệm}

QRTable được triển khai trong Nx monorepo gồm các ứng dụng phía người dùng, BFF và dịch vụ nghiệp vụ. PWA khách phục vụ quy trình đặt món tại bàn qua mã QR; ứng dụng quản trị phục vụ nhân viên, bếp, quầy bar, chủ quán, quản lý và quản trị viên nền tảng. Cả hai ứng dụng đi qua BFF để sử dụng thống nhất chuỗi guard, ngữ cảnh đơn vị thuê bao, phân quyền và cổng WebSocket.

Ở lớp dịch vụ, Authorizer xác minh JWT/OIDC và tích hợp Keycloak; User-Access lưu hồ sơ ứng dụng và vai trò nghiệp vụ; Catalog sở hữu thực đơn, bàn, token QR và tồn kho; Order sở hữu phiên, giỏ, đơn, dòng đơn, hóa đơn và yêu cầu phục vụ; Kitchen sở hữu bản chiếu KDS khi vận hành; Payment sở hữu giao dịch, nhật ký kiểm toán và cấu hình thanh toán; SaaS sở hữu đơn vị thuê bao, gói giá, thuê bao, hóa đơn thuê bao và quyền theo gói. Các bảng/bộ sưu tập tương ứng đã được trình bày ở Bảng~\ref{tab:chapter4-data-ownership-schema}; Chương 5 chỉ nhắc lại dữ liệu khi nó giúp giải thích một luồng cụ thể.

Các thư viện dùng chung trong monorepo tạo lớp hợp đồng giữa các phần của hệ thống. Dịch vụ có thể dùng chung DTO, enum, token tích hợp, bộ dựng khóa Redis, bộ dựng phòng WebSocket, guard và kiểu dữ liệu phía ứng dụng. Dữ liệu thuộc ngữ cảnh nghiệp vụ khác được truy cập qua TCP/gRPC hoặc Kafka. Nhờ đó, Nx hỗ trợ tính nhất quán của hợp đồng mà không làm mất ranh giới dữ liệu.

\section{Phiên đặt món qua mã QR và giỏ đặt món dùng chung}

Hình~\ref{fig:chapter5-qr-ordering-session} tóm tắt luồng quản lý phiên đặt món qua mã QR, thao tác giỏ đặt món dùng chung và gửi đơn đầu tiên.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth,height=0.82\textheight,keepaspectratio]{chapter5-qr-ordering-session.pdf}
  \caption{Luồng quản lý phiên đặt món qua mã QR và giỏ đặt món dùng chung.}
  \label{fig:chapter5-qr-ordering-session}
\end{figure}

Mục tiêu nghiệp vụ của luồng là cho phép nhiều khách cùng bàn xem thực đơn, sử dụng chung giỏ đặt món và gửi đơn mà không cần tài khoản Keycloak. Khách chỉ được thao tác trong phạm vi đơn vị thuê bao, bàn và phiên hợp lệ; bàn có phiên đã phát sinh đơn không được mở lại sai trạng thái; xung đột cập nhật từ nhiều thiết bị phải được phát hiện.

Order là dịch vụ sở hữu phiên và giỏ. Catalog sở hữu token QR, bàn, thực đơn và trạng thái món có thể đặt. Khi khách mở QR, BFF xác thực ngữ cảnh ở lớp biên rồi gọi Order; Order phối hợp với Catalog để xác nhận bàn và cập nhật trạng thái bàn khi cần. PostgreSQL của Order giữ phiên bền vững, còn Redis giúp đọc/ghi nhanh phiên đang hoạt động và giỏ đặt món dùng chung. Nếu Redis mất hoặc hết hạn, Order vẫn có thể dựa vào trạng thái bền vững để xác định phiên còn hợp lệ hay không.

Giỏ đặt món dùng chung được kiểm soát bằng số phiên bản. Mỗi thao tác thay đổi gửi kèm phiên bản mà thiết bị đang giữ; phiên bản không khớp làm phát sinh xung đột và ứng dụng phải tải lại trạng thái mới nhất. Khi khách gửi đơn, Order tạo đơn chờ xác nhận, tạo hoặc liên kết hóa đơn mở và xóa giỏ sau khi xác nhận phiên bản. Tồn kho chỉ được xử lý ở bước nhân viên xác nhận đơn. Phạm vi hiện tại hỗ trợ phiên trực tuyến, kiểm soát phiên bản và kết nối lại; hàng đợi thao tác ngoại tuyến đầy đủ cần thêm đồng bộ nền và cơ chế giải quyết xung đột.

Hình~\ref{fig:chapter5-screenshot-customer-pwa-ordering} minh họa ba bước đầu trên PWA khách: tham gia phiên từ mã QR, duyệt thực đơn và sử dụng giỏ chung trước khi gửi đơn. Ảnh giao diện thể hiện bề mặt vận hành; kiểm thử Order đối chiếu phiên bản giỏ và trạng thái phiên (Bảng~\ref{tab:chapter5-implemented-evidence}).

\clearpage
\screenshotpwafigurethree{PWA khách — tham gia phiên QR, duyệt thực đơn và gửi đơn từ giỏ chung.}{fig:chapter5-screenshot-customer-pwa-ordering}{chapter5-01-customer-qr-session.png}{chapter5-02-customer-menu-browsing.png}{chapter5-03-customer-cart-submit.png}
\screenshotgridanchor{fig:chapter5-screenshot-customer-qr-session}
\screenshotgridanchor{fig:chapter5-screenshot-customer-menu}
\screenshotgridanchor{fig:chapter5-screenshot-customer-cart-submit}

\section{Xác nhận đơn hàng và bảo toàn tồn kho theo Order Confirm Saga}

Hình~\ref{fig:chapter5-order-confirm-stock} trình bày Order Confirm Saga: luồng nhân viên xác nhận đơn và cách QRTable giữ bất biến tồn kho theo ranh giới Catalog--Order.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth,height=0.82\textheight,keepaspectratio]{chapter5-order-confirm-stock.pdf}
  \caption{Order Confirm Saga: xác nhận đơn hàng và bảo toàn tồn kho qua ranh giới Order--Catalog.}
  \label{fig:chapter5-order-confirm-stock}
\end{figure}

Mục tiêu nghiệp vụ của luồng này là chỉ chuyển đơn sang trạng thái chế biến khi nhân viên đã xác nhận và tồn kho còn đủ. Bất biến cần giữ là Catalog là chủ sở hữu duy nhất của tồn kho, Order là chủ sở hữu vòng đời đơn/hóa đơn, và việc xác nhận lặp lại không được trừ tồn kho hai lần. Đây là Order Confirm Saga, Saga đại diện cho luồng POS chính của QRTable.

Khi nhân viên xác nhận đơn từ POS, BFF kiểm tra JWT, ngữ cảnh đơn vị thuê bao và quyền thao tác trước khi gọi Order. Order khóa đơn trong phạm vi đơn vị thuê bao, kiểm tra trạng thái đơn/hóa đơn, rồi gọi Catalog qua hợp đồng để trừ tồn kho. Catalog thực hiện phần cập nhật tồn kho trong cơ sở dữ liệu của chính Catalog. Nếu thiếu tồn kho, Order không được chuyển trạng thái đơn sang chế biến. Nếu Catalog trừ thành công nhưng bước cập nhật Order hoặc ghi outbox thất bại, Order gọi lại Catalog để bù trừ bằng đường hoàn tồn kho tương ứng.

Sau khi Order ghi nhận thành công, outbox \texttt{order.confirmed} đánh dấu điểm hoàn tất nghiệp vụ và được thành phần xuất bản chuyển sang Kafka để Kitchen dựng phiếu bếp. Phản hồi cho nhân viên không phụ thuộc vào thời điểm Kitchen xử lý xong phiếu vì KDS là tác dụng phụ bất đồng bộ sau bước xác nhận. Order Confirm Saga phối hợp giao dịch cục bộ, sự kiện outbox và cơ chế bù trừ. Trạng thái Saga bền vững, CDC và phân phối chính xác một lần nằm ngoài phạm vi triển khai hiện tại.

Hình~\ref{fig:chapter5-screenshot-staff-table-map} và Hình~\ref{fig:chapter5-screenshot-staff-order-confirm} minh họa luồng thành công của Order Confirm Saga trên POS: nhân viên xem sơ đồ bàn, kiểm tra đơn đang mở và xác nhận đơn để kích hoạt trừ tồn kho qua Catalog. Nhánh bù trừ được kiểm tra tự động; kết quả nằm ở Chương~6 và Phụ lục~\ref{app:test-evidence}.

\screenshotfiguremgmt{chapter5-05-staff-pos-table-map.png}{POS — sơ đồ bàn và trạng thái đơn trước bước xác nhận trong Order Confirm Saga.}{fig:chapter5-screenshot-staff-table-map}
\screenshotfiguremgmt{chapter5-06-staff-order-confirm.png}{POS — xác nhận đơn sau khi Order phối hợp Catalog kiểm tra và trừ tồn kho.}{fig:chapter5-screenshot-staff-order-confirm}

\section{Điều phối KDS cho bếp và cập nhật thời gian thực}

Hình~\ref{fig:chapter5-kds-ticket-lifecycle} mô tả cách Kitchen nhận sự kiện \texttt{order.confirmed}, tạo phiếu KDS, phát gợi ý thời gian thực và cho phép ứng dụng tải lại trạng thái mới nhất.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth,height=0.82\textheight,keepaspectratio]{chapter5-kds-ticket-lifecycle.pdf}
  \caption{Luồng điều phối KDS từ sự kiện Order đến gợi ý WebSocket và tải lại dữ liệu.}
  \label{fig:chapter5-kds-ticket-lifecycle}
\end{figure}

Mục tiêu nghiệp vụ của luồng này là đưa món đã xác nhận đến đúng trạm bếp/bar, hỗ trợ cập nhật trạng thái phiếu bếp và phản ánh thay đổi cho màn hình KDS gần thời gian thực. Bất biến cần giữ là Kitchen sở hữu hàng đợi KDS khi vận hành, nhưng Order vẫn sở hữu vòng đời đơn và dòng đơn. Kitchen không có cơ sở dữ liệu nghiệp vụ bền vững riêng trong phạm vi hiện tại; Redis là bản chiếu khi vận hành để phục vụ hàng đợi, phiên bản, SLA và khôi phục.

Kitchen tiêu thụ topic Kafka \texttt{order.confirmed}, kiểm tra nội dung sự kiện theo hợp đồng rồi tách phiếu theo trạm. Phiếu được lưu trong Redis theo cấu trúc hàng đợi, độ ưu tiên và SLA. Sau mỗi thay đổi, Kitchen phát \texttt{kds.queue\_changed} cho BFF qua kênh nội bộ; BFF gửi gợi ý WebSocket để ứng dụng KDS tải trạng thái mới nhất từ API. Trạng thái có thẩm quyền vì vậy vẫn nằm ở phía máy chủ.

Khi đầu bếp bắt đầu hoặc hoàn tất phiếu bếp, thao tác đi qua BFF đến Kitchen. Nếu phiếu đã sẵn sàng làm thay đổi trạng thái dòng món, Kitchen phối hợp với Order qua hợp đồng để Order cập nhật vòng đời dòng món/đơn tương ứng. KDS trong Chương 5 được trình bày như bản chiếu vận hành gần thời gian thực cho bếp/bar. Lịch sử bếp dài hạn hoặc phân tích chuyên sâu phù hợp hơn với bản chiếu bền vững hoặc kho phân tích trong hướng phát triển.

Hình~\ref{fig:chapter5-screenshot-kds-queue} và Hình~\ref{fig:chapter5-screenshot-kds-ticket-status} minh họa ứng dụng KDS tiếp nhận phiếu sau sự kiện \texttt{order.confirmed} và cập nhật trạng thái trên hàng đợi vận hành.

\screenshotfiguremgmt{chapter5-07-kds-queue.png}{Ứng dụng KDS — hàng đợi phiếu theo trạm và độ ưu tiên trên bản chiếu Redis.}{fig:chapter5-screenshot-kds-queue}
\screenshotfiguremgmt{chapter5-08-kds-ticket-status.png}{Ứng dụng KDS — cập nhật trạng thái phiếu và tải lại hàng đợi sau gợi ý WebSocket.}{fig:chapter5-screenshot-kds-ticket-status}

\section{Ghi nhận thanh toán tiền mặt, VietQR và SePay}

Hình~\ref{fig:chapter5-payment-settlement} tổng hợp hai nhánh thanh toán chính: tiền mặt tại POS và VietQR/SePay qua webhook.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth,height=0.82\textheight,keepaspectratio]{chapter5-payment-settlement.pdf}
  \caption{Luồng ghi nhận thanh toán tiền mặt, VietQR và SePay.}
  \label{fig:chapter5-payment-settlement}
\end{figure}

Mục tiêu nghiệp vụ của luồng này là ghi nhận thanh toán đúng hóa đơn, đúng đơn vị thuê bao, đúng số tiền đã làm tròn VND và không hoàn tất hóa đơn nhiều lần khi có yêu cầu hoặc webhook lặp. Bất biến dữ liệu là Payment sở hữu bản ghi thanh toán, nhật ký thanh toán, cấu hình thanh toán và outbox thanh toán; Order sở hữu việc hoàn tất hóa đơn/phiên; Catalog sở hữu trạng thái bàn sau khi phiên kết thúc.

Với tiền mặt, Payment lấy bản chụp hóa đơn từ Order qua hợp đồng, kiểm tra số tiền và ghi thanh toán, nhật ký kiểm toán và outbox trong cơ sở dữ liệu Payment. Sau đó Payment phối hợp với Order để đánh dấu hóa đơn đã thanh toán, đóng phiên, xóa trạng thái phiên/giỏ khi vận hành và chuyển bàn sang trạng thái phù hợp. Nếu cùng một giao dịch thanh toán được phát lại, phần hoàn tất hóa đơn phải đi theo hướng lũy đẳng để không tạo kết quả trùng.

Với VietQR/SePay, Payment tạo tham chiếu hóa đơn nhà hàng theo dòng tiền \texttt{QRTBL}. Webhook từ SePay đi vào BFF, được định tuyến và chuyển cho Payment. Payment kiểm tra bí mật của đơn vị thuê bao, khớp tham chiếu từ nội dung chuyển khoản, khóa bản ghi thanh toán, ghi nhật ký cho giao dịch trùng hoặc thanh toán thiếu, và chỉ gọi Order hoàn tất hóa đơn khi đủ điều kiện. Cấu hình tài khoản và trạng thái OAuth ngắn hạn thuộc Payment; hóa đơn gói nền tảng \texttt{QRSUB} thuộc SaaS, không bị trộn với bản ghi thanh toán của hóa đơn nhà hàng.

Phần triển khai thanh toán bao gồm xử lý tiền mặt, webhook VietQR/SePay, thông báo trùng và giao dịch thiếu tiền. Các hành vi này được đối chiếu bằng mã nguồn và kiểm thử nội bộ. Đường gọi lại công khai, thông tin xác thực hợp lệ và dữ liệu giao dịch thực tế được thu thập riêng khi kiểm chứng với nhà cung cấp trên môi trường công khai.

Hình~\ref{fig:chapter5-screenshot-customer-payment-flow} minh họa nhánh khách của luồng thanh toán (Hình~\ref{fig:chapter5-payment-settlement}): theo dõi tiến độ đơn và yêu cầu thanh toán VietQR tại bàn. Chương~6 đối chiếu thêm webhook SePay và tính lũy đẳng ghi nhận thanh toán với kiểm thử tương ứng.

\screenshotpwafiguretwo{PWA khách — theo dõi đơn và yêu cầu thanh toán bằng VietQR.}{fig:chapter5-screenshot-customer-payment-flow}{chapter5-04-customer-order-tracking.png}{chapter5-04-customer-request-payment.png}
\screenshotgridanchor{fig:chapter5-screenshot-customer-order-tracking}
\screenshotgridanchor{fig:chapter5-screenshot-customer-request-payment}

\section{Khởi tạo đơn vị thuê bao và vòng đời gói dịch vụ}

Hình~\ref{fig:chapter5-saas-onboarding-saga} trình bày luồng quản trị viên nền tảng khởi tạo đơn vị thuê bao và cơ chế bù trừ khi một bước phụ thuộc thất bại.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth,height=0.82\textheight,keepaspectratio]{chapter5-saas-onboarding-saga.pdf}
  \caption{Luồng khởi tạo đơn vị thuê bao theo SaaS Onboarding Mini-Saga.}
  \label{fig:chapter5-saas-onboarding-saga}
\end{figure}

Mục tiêu nghiệp vụ của luồng này là tạo một đơn vị thuê bao có thể vận hành: có thông tin đơn vị thuê bao, chủ sở hữu, hồ sơ/vai trò, gói thuê bao ban đầu và cấu hình thanh toán tương ứng. Bất biến cần giữ là SaaS sở hữu đơn vị thuê bao, gói, thuê bao và hóa đơn thuê bao; Authorizer/Keycloak sở hữu định danh; User-Access sở hữu hồ sơ ứng dụng và vai trò; Payment sở hữu cấu hình thanh toán. SaaS không tự ghi dữ liệu của User-Access hoặc Payment.

Luồng bắt đầu từ quản trị viên nền tảng trong ứng dụng quản trị. BFF kiểm tra quyền nền tảng trước khi gọi SaaS. Dịch vụ SaaS chuẩn hóa slug, kiểm tra trùng lặp và tạo đơn vị thuê bao cùng gói thuê bao trong cơ sở dữ liệu của mình. Tiếp đó, SaaS gọi Authorizer để tạo định danh chủ sở hữu, User-Access để tạo hồ sơ và vai trò, Payment để tạo cấu hình thanh toán rỗng, rồi ghi outbox \texttt{tenant.created}. Nếu lỗi xảy ra sau một tác dụng phụ, SaaS bù trừ bằng cách vô hiệu hóa chủ sở hữu, hủy gói thuê bao khởi tạo hoặc xóa đơn vị thuê bao chưa hoàn tất.

Vòng đời đơn vị thuê bao gồm hoạt động, tạm ngưng và đóng. SaaS giữ trạng thái bền vững, còn Redis hỗ trợ đọc nhanh cho guard và quyền theo gói. Trạng thái tạm ngưng vẫn cho phép một số thao tác đọc hoặc xử lý thanh toán đang chờ nhưng chặn việc tạo đơn mới và các thay đổi không phù hợp. Đây là Saga đại diện thứ hai của đề tài, thể hiện cơ chế điều phối và bù trừ có kiểm soát. Tiến trình thử lại độc lập và trạng thái Saga bền vững thuộc hướng củng cố tiếp theo.

Hình~\ref{fig:chapter5-screenshot-admin-tenant-onboarding}, Hình~\ref{fig:chapter5-screenshot-owner-payment-settings} và Hình~\ref{fig:chapter5-screenshot-owner-subscription} minh họa các bước sau SaaS Onboarding Mini-Saga: quản trị viên nền tảng tạo đơn vị thuê bao, chủ quán cấu hình thanh toán và xem gói đang hoạt động. Phần điều phối cùng bù trừ được đối chiếu với kiểm thử ở Chương~6 và Phụ lục~\ref{app:test-evidence}.

\screenshotfiguremgmt{chapter5-12-admin-tenant-onboarding.png}{Ứng dụng quản trị — khởi tạo đơn vị thuê bao, chủ sở hữu và gói ban đầu.}{fig:chapter5-screenshot-admin-tenant-onboarding}
\screenshotfiguremgmt{chapter5-11-owner-payment-settings.png}{Ứng dụng quản trị — cấu hình thanh toán SePay/VietQR của đơn vị thuê bao.}{fig:chapter5-screenshot-owner-payment-settings}
\screenshotfiguremgmt{chapter5-11-owner-subscription.png}{Ứng dụng quản trị — gói thuê bao và trạng thái đăng ký của nhà hàng.}{fig:chapter5-screenshot-owner-subscription}

\section{Bảng điều khiển và báo cáo theo gói dịch vụ}

Phần bảng điều khiển và báo cáo bổ sung lớp đọc tổng hợp mà không tạo thêm dịch vụ phân tích độc lập. Chủ quán và quản lý theo dõi doanh thu, đơn hàng, phương thức thanh toán, món bán chạy và trạng thái bàn trong phạm vi đơn vị thuê bao. Quản trị viên nền tảng theo dõi số liệu tổng hợp về đơn vị thuê bao, gói dịch vụ, hóa đơn và doanh thu nền tảng.

Về ranh giới dữ liệu, BFF là lớp HTTP được bảo vệ bằng guard. BFF kiểm tra quyền \texttt{report.read\_own} hoặc \texttt{report.read\_any}, chuẩn hóa truy vấn và chuyển yêu cầu đến dịch vụ sở hữu dữ liệu. Payment cung cấp số liệu doanh thu và phương thức thanh toán; Order cung cấp số liệu đơn/hóa đơn/món bán chạy; Catalog cung cấp trạng thái bàn/thực đơn; SaaS cung cấp số liệu nền tảng. Cách làm này giữ nguyên quyền sở hữu dữ liệu đã trình bày ở Chương 4; các đường xử lý phân tích Kafka hoặc kho OLAP thuộc nhóm mở rộng phân tích.

Sự khác biệt giữa báo cáo theo đơn vị thuê bao và phân tích nền tảng nằm ở phạm vi quyền và quyền theo gói. Chủ quán cùng quản lý dùng \texttt{report.read\_own}; API còn kiểm tra gói thuê bao đang hoạt động và tính năng \texttt{analytics\_basic}. Quản trị viên nền tảng dùng \texttt{report.read\_any} để xem số liệu tổng hợp và chi tiết theo đơn vị thuê bao, không phụ thuộc gói của đơn vị đang được xem. Phạm vi hiện tại tập trung vào mô hình đọc vận hành; dự báo, phân tích hiệu suất nhân viên, dòng doanh thu thời gian thực và kho dữ liệu phân tích thuộc hướng phát triển.

Hình~\ref{fig:chapter5-screenshot-owner-menu-management} và Hình~\ref{fig:chapter5-screenshot-owner-table-qr} minh họa chủ quán thiết lập thực đơn, bàn và mã QR. Hình~\ref{fig:chapter5-screenshot-owner-dashboard-reporting} cùng Hình~\ref{fig:chapter5-screenshot-admin-platform-analytics} minh họa mô hình đọc theo quyền \texttt{report.read\_own} và \texttt{report.read\_any}.

\screenshotfiguremgmt{chapter5-09-owner-menu-management.png}{Ứng dụng quản trị — quản lý danh mục, món và tồn kho trong Catalog.}{fig:chapter5-screenshot-owner-menu-management}
\screenshotfiguremgmt{chapter5-10-owner-table-qr-management.png}{Ứng dụng quản trị — quản lý khu vực, bàn và mã QR tại bàn.}{fig:chapter5-screenshot-owner-table-qr}
\screenshotfiguremgmt{chapter5-13-owner-dashboard-reporting.png}{Ứng dụng quản trị — bảng điều khiển doanh thu và đơn hàng của nhà hàng.}{fig:chapter5-screenshot-owner-dashboard-reporting}
\screenshotfiguremgmt{chapter5-14-admin-platform-analytics.png}{Ứng dụng quản trị — số liệu đơn vị thuê bao, gói dịch vụ và doanh thu SaaS.}{fig:chapter5-screenshot-admin-platform-analytics}

\section{Đóng gói và vận hành trên môi trường công khai}

QRTable được đóng gói bằng Docker/Docker Compose cùng các thành phần PostgreSQL, MongoDB, Redis, Kafka, Keycloak, BFF và ứng dụng phía người dùng. Môi trường công khai bổ sung URL hoặc tên miền, HTTPS/TLS, cấu hình kết nối dịch vụ, đường gọi lại webhook SePay, kiểm tra sức khỏe và kiểm thử khói cho các luồng phiên QR, xác nhận đơn, KDS, thanh toán và khởi tạo đơn vị thuê bao.

Hiện vật vận hành gồm kết quả kiểm tra sức khỏe, nhật ký rút gọn, kiểm thử khói, cấu hình đường gọi lại, phiên bản phát hành và thông tin mã nguồn tương ứng. Nhóm hiện vật này phục vụ đánh giá khả năng đóng gói và vận hành các dịch vụ chính. Tính sẵn sàng cao, tự động hoàn tác và khả năng chịu tải cần quy trình kiểm chứng riêng.

\section{Tổng hợp kết quả triển khai}

Bảng~\ref{tab:chapter5-implemented-evidence} tổng hợp các luồng chính theo mục tiêu nghiệp vụ, bất biến, thành phần triển khai và phạm vi hiện tại.

{\scriptsize\sloppy
\setlength{\tabcolsep}{2pt}
\begin{longtable}{>{\raggedright\arraybackslash}p{0.16\textwidth}>{\raggedright\arraybackslash}p{0.24\textwidth}>{\raggedright\arraybackslash}p{0.25\textwidth}>{\raggedright\arraybackslash}p{0.17\textwidth}>{\raggedright\arraybackslash}p{0.1\textwidth}}
  \caption{Tổng hợp kết quả triển khai các luồng chính của QRTable.}
  \label{tab:chapter5-implemented-evidence}\\
  \toprule
  Luồng & Mục tiêu nghiệp vụ & Bất biến và ranh giới chính & Thành phần triển khai & Phạm vi hiện tại \\
  \midrule
  \endfirsthead
  \toprule
  Luồng & Mục tiêu nghiệp vụ & Bất biến và ranh giới chính & Thành phần triển khai & Phạm vi hiện tại \\
  \midrule
  \endhead
  \bottomrule
  \endfoot
  Phiên QR và giỏ chung & Khách vào phiên từ QR, dùng chung giỏ và gửi đơn. & Order sở hữu phiên, giỏ và đơn; Catalog sở hữu bàn và thực đơn; Redis hỗ trợ đường xử lý tần suất cao. & Hình~\ref{fig:chapter5-qr-ordering-session}; kiểm thử phiên, giỏ và gửi đơn của Order. & Phiên trực tuyến, kết nối lại và tải trạng thái mới nhất; hàng đợi ngoại tuyến đầy đủ là hướng mở rộng. \\
  Xác nhận đơn và tồn kho & Nhân viên xác nhận đơn khi đủ tồn kho. & Catalog sở hữu tồn kho; Order sở hữu đơn; xác nhận lặp không trừ tồn kho lần hai. & Hình~\ref{fig:chapter5-order-confirm-stock}; kiểm thử đơn vị, hợp đồng và tích hợp có điều kiện cho Order Confirm Saga. & Saga đại diện đã được triển khai; trạng thái Saga bền vững, CDC và phân phối chính xác một lần là hướng củng cố. \\
  KDS & Phiếu bếp hoặc bar xuất hiện sau khi đơn được xác nhận và ứng dụng cập nhật gần thời gian thực. & Kitchen sở hữu bản chiếu Redis; Order sở hữu vòng đời đơn và dòng đơn; WebSocket gợi ý làm mới. & Hình~\ref{fig:chapter5-kds-ticket-lifecycle}; kiểm thử bộ tiêu thụ Kitchen/KDS. & Bản chiếu phục vụ vận hành; lịch sử KDS dài hạn cần kho bền vững riêng. \\
  Thanh toán & Ghi nhận tiền mặt hoặc VietQR/SePay và hoàn tất hóa đơn, phiên. & Payment sở hữu bản ghi thanh toán; Order sở hữu hóa đơn và phiên; webhook trùng hoặc thiếu được xử lý lũy đẳng. & Hình~\ref{fig:chapter5-payment-settlement}; kiểm thử ghi nhận thanh toán và webhook. & Kiểm thử nội bộ xác nhận tính lũy đẳng; kiểm chứng nhà cung cấp cần đường gọi lại và dữ liệu giao dịch thực tế. \\
  Khởi tạo SaaS & Tạo đơn vị thuê bao, chủ sở hữu, thuê bao và cấu hình thanh toán. & SaaS sở hữu đơn vị thuê bao/thuê bao; Authorizer/User-Access/Payment chỉ qua hợp đồng; có bù trừ. & Hình~\ref{fig:chapter5-saas-onboarding-saga}; kiểm thử khởi tạo với cơ sở dữ liệu/Payment có điều kiện. & Chứng minh mini-saga đại diện; kiểm chứng đầu-cuối qua mọi phụ thuộc thuộc nhóm hiện vật toàn ngăn xếp. \\
  Bảng điều khiển và báo cáo & Chủ quán, quản lý và quản trị viên nền tảng xem báo cáo đúng phạm vi quyền và gói. & BFF không truy vấn kết hợp trực tiếp cơ sở dữ liệu; Payment, Order, Catalog và SaaS cung cấp mô hình đọc của dữ liệu mình sở hữu. & Bộ điều khiển báo cáo BFF, dịch vụ báo cáo và kiểm thử ứng dụng quản trị. & Mô hình đọc vận hành và quyền theo gói; phân tích nâng cao và kho dữ liệu thuộc hướng mở rộng. \\
  Đóng gói và triển khai công khai & Vận hành các dịch vụ chính trên môi trường công khai. & Đánh giá dựa trên URL, kiểm tra sức khỏe, kiểm thử khói và đường gọi lại webhook. & Docker, Docker Compose, cấu hình môi trường và hiện vật vận hành. & Tính sẵn sàng cao và tự động hoàn tác cần bằng chứng riêng. \\
\end{longtable}
}

Bảng~\ref{tab:chapter5-representative-sagas} so sánh Saga xác nhận đơn của POS với mini-saga khởi tạo nền tảng. Cả hai sử dụng điều phối tập trung, hợp đồng liên dịch vụ và cơ chế bù trừ, nhưng khác nhau về điểm ghi nhận và phạm vi kiểm chứng.

{\scriptsize\sloppy
\setlength{\tabcolsep}{2pt}
\begin{longtable}{>{\raggedright\arraybackslash}p{0.16\textwidth}>{\raggedright\arraybackslash}p{0.19\textwidth}>{\raggedright\arraybackslash}p{0.22\textwidth}>{\raggedright\arraybackslash}p{0.22\textwidth}>{\raggedright\arraybackslash}p{0.13\textwidth}}
  \caption{Hai Saga đại diện trong QRTable.}
  \label{tab:chapter5-representative-sagas}\\
  \toprule
  Saga & Mục tiêu & Thành phần tham gia & Điểm ghi nhận và cơ chế bù trừ & Loại bằng chứng \\
  \midrule
  \endfirsthead
  \toprule
  Saga & Mục tiêu & Thành phần tham gia & Điểm ghi nhận và cơ chế bù trừ & Loại bằng chứng \\
  \midrule
  \endhead
  \bottomrule
  \endfoot
  Order Confirm Saga & Xác nhận đơn tại POS và bảo toàn tồn kho. & BFF, Order, Catalog, outbox Order, Kafka và Kitchen. & Ghi nhận khi Order lưu trạng thái \texttt{PROCESSING} cùng outbox \texttt{order.confirmed}; nếu bước này thất bại sau khi Catalog đã trừ tồn kho, Order yêu cầu Catalog hoàn tồn kho. & Kiểm thử đơn vị và hợp đồng đã bao phủ luồng chính; tích hợp tồn kho có điều kiện; tiêm lỗi toàn ngăn xếp là hướng củng cố. \\
  SaaS Onboarding Mini-Saga & Khởi tạo đơn vị thuê bao có chủ sở hữu, gói ban đầu và cấu hình thanh toán. & BFF, SaaS, Authorizer, Keycloak, User-Access, Payment, outbox SaaS và bộ tiêu thụ Catalog. & Ghi nhận khi SaaS tạo đơn vị thuê bao, gói thuê bao, cấu hình thanh toán và outbox \texttt{tenant.created}; lỗi sau tác dụng phụ kích hoạt bù trừ và vô hiệu hóa chủ sở hữu khi cần. & Tích hợp cơ sở dữ liệu và Payment TCP đã bao phủ một phần; Authorizer, Keycloak và User-Access cần kiểm chứng toàn ngăn xếp. \\
\end{longtable}
}

Các luồng trên cho thấy thiết kế kiến trúc đã được triển khai tại những ngữ cảnh nghiệp vụ chính. Giao tiếp liên dịch vụ đi qua hợp đồng thay vì truy cập chéo cơ sở dữ liệu; các bất biến về cô lập đơn vị thuê bao, quyền sở hữu tồn kho, tính lũy đẳng thanh toán, bù trừ Saga và WebSocket gợi ý làm mới được thể hiện trong mã nguồn, kiểm thử và sơ đồ. Chương 6 đánh giá mức độ kiểm chứng của từng nhóm kết quả.
