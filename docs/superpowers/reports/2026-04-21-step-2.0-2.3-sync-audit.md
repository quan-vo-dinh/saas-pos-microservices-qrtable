# Step 2.0 & Step 2.3 — Sync Audit Report

> **Audience:** Project owner (Quân) **và** các agent khác sẽ đọc-follow file này.
> **Status:** 🟡 Active — Task 2 (F2 + F15) đã execute xong; còn 11 findings pending.
> **Date:** 2026-04-21
> **Scope:** Phase 2A (pre-Step 2.4) — audit đồng bộ giữa (a) tài liệu × tài liệu, (b) tài liệu × source code, (c) source code × runtime state (MongoDB + Keycloak).
> **Companion (executable):** `[docs/superpowers/plans/2026-04-21-step-2.0-2.3-sync-fix.md](../plans/2026-04-21-step-2.0-2.3-sync-fix.md)`
> **Method:** Read-only discovery (Superpowers `brainstorming` skill → `plan-writing`). MCP-style queries: `docker exec mongosh` + `kcadm.sh`.

---

## 0. Context & Orientation — READ FIRST

> Mọi reader (user hoặc agent follow file này) phải đọc §0 trước khi nhảy xuống findings. §0 thay đổi meaning của nhiều finding bên dưới.

### 0.1 Ground-truth status của 6 step trong Phase 2A

| Step    | Title                                      | Status thực tế 2026-04-21                                                                                                                                                        | Artifacts                                                                                                                                                                                        |
| ------- | ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **2.0** | Mở rộng PERMISSION enum & mapping role     | ✅ **DONE** (commit `e81e439`, 2026-04-19)                                                                                                                                       | `libs/constants/src/lib/enum/role.enum.ts` 51 perms + `apps/user-access/src/seeder/role.json` 6 roles + canonical `docs/architecture/permission-matrix.md` + `tools/verify-permission-matrix.sh` |
| **2.1** | Nền tảng Kafka (học tập)                   | 🟢 Course material — không require source artifact                                                                                                                               | —                                                                                                                                                                                                |
| **2.2** | Mock UI: Cart, POS, KDS                    | 🔴 **NOT STARTED** — từng thử triển khai, sau đó **revert toàn bộ**; cleanup đã hoàn tất 2026-04-21 (spec file dropped khỏi git index, `phase-2a:88` badge đổi `⬜ Not Started`) | Không có spec/plan canonical. Không có mock UI component code trong `apps/customer-pwa/`** hay `apps/management-app/**`. Sẽ tạo spec/plan mới khi bắt đầu triển khai lại.                        |
| **2.3** | Shared types & hợp đồng realtime           | ✅ **DONE** (commit `5089d41`, 2026-04-19)                                                                                                                                       | `libs/shared/types/src/lib/`** 6 entities + 7 enums + 3 matrices + 5 events; `libs/shared/mock-data/src/lib/**`refactor; 2 test files`**tests**/{enum-completeness,transitions}.spec.ts`         |
| **2.4** | Order Service backend + Kafka + BFF Direct | ⬜ **NOT STARTED** — target tiếp theo                                                                                                                                            | —                                                                                                                                                                                                |
| **2.5** | Tích hợp FE ↔ BE                          | ⬜ **NOT STARTED**                                                                                                                                                               | —                                                                                                                                                                                                |

### 0.2 Thứ tự triển khai KHÔNG theo số thứ tự — intentional

Thứ tự thực tế đã thực hiện: **2.0 → 2.3 → (thử 2.2 → revert)**. Lý do hợp lý:

- Step **2.3** là thuần refactor `libs/shared/types/` + mock-data — **không phụ thuộc UI**, có thể làm ngay sau 2.0 để **khóa contract FE↔BE trước**. Mock UI và Order Service backend đều consume types → cần types stable trước.
- Step **2.2** (Mock UI) phụ thuộc FE components + state mock + WebSocket fake + animation — rủi ro cao hơn. Quyết định làm sau 2.3 khi types đã ổn.
- Attempt 2.2 ban đầu đã thử nhưng **revert toàn bộ** (không commit vào main). Nguyên nhân revert **không** nằm trong phạm vi audit này; điều quan trọng: **filesystem + doc phải phản ánh đúng trạng thái "Step 2.2 chưa triển khai"** — hiện tại một số doc vẫn còn dấu vết của attempt (xem F2, F15).

### 0.3 Phạm vi audit này (RÕ ràng)

Audit chỉ soi **Step 2.0 và Step 2.3** — hai step đã commit vào `main`. Audit KHÔNG bao gồm:

- Step 2.1 (course material, không audit được)
- Step 2.2 implementation chi tiết (chưa có code) — chỉ kiểm tra **doc có phản ánh đúng trạng thái NOT-DONE** không
- Step 2.4, 2.5 (chưa bắt đầu)

### 0.4 Nếu bạn là **agent** đọc file này để follow fix plan

Đọc theo thứ tự:

1. **§0** — context (bạn đang đọc)
2. **§1** — executive summary (bức tranh tổng)
3. **§6** — consolidated findings table (15 items với severity + file:line)
4. **§8** — Definition of Done (điều kiện đóng 2.0 + 2.3)
5. Rồi mở [companion plan](../plans/2026-04-21-step-2.0-2.3-sync-fix.md) §Task 1–10 để execute tuần tự

Các section §2–§5, §7 là **evidence/context** — đọc khi cần verify một finding cụ thể.

### 0.5 Hard constraints (cấm-chỉ cho agent follow)

- ❌ **KHÔNG tự ý "restore" spec Step 2.2** — file bị delete là có chủ ý (revert). Coi Step 2.2 là **chưa triển khai**. Xem Task 2 trong plan để biết cách xử lý đúng.
- ❌ **KHÔNG chạm production code** (`apps/**/src/`**, `libs/**/src/\*\*`) trong phạm vi fix plan này. Audit chỉ sửa doc + Keycloak housekeeping. Mọi thay đổi code = Step mới.
- ❌ **KHÔNG đổi ma trận permission** trong `role.enum.ts`, `role.json`, `permission-matrix.md` — 3 file này đã đồng bộ 100% với MongoDB (§3.1). Thay đổi permission = Step mới.
- ❌ **KHÔNG tự ý git push** lên remote. Owner xử lý commits.
- ✅ **ĐƯỢC PHÉP** truy cập MCP-style read-only: `docker exec` vào mongodb/keycloak containers để verify.
- ✅ **ĐƯỢC PHÉP** sửa doc files trong `docs/phases/`**, `docs/references/**`, `docs/architecture/**`, `docs/business-logic.md`, `docs/superpowers/specs/**`, `docs/superpowers/plans/\*\*` theo đúng Task mapping trong companion plan.

### 0.6 Quick glossary

| Term                     | Definition trong dự án này                                                                                                                                                                               |
| ------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **RBAC**                 | Role-Based Access Control — 6 roles × 51 permissions. Canonical: `docs/architecture/permission-matrix.md`                                                                                                |
| **Guard chain**          | `UserGuard` → `TenantGuard` → `PermissionGuard` cho staff; `SessionGuard → TenantGuard` cho customer                                                                                                     |
| **BFF Direct (AP1)**     | Anti-Pattern 1 — BFF emit WebSocket + cache invalidate SAU TCP response, KHÔNG qua Kafka. Dùng cho UI-latency events. Chi tiết: `technical-architecture.md §7.3`                                         |
| **4P+2AP**               | Kafka registry: 4 domain-event Producers + 2 Anti-Pattern rules (AP1 = BFF Direct cho UI; AP2 = TCP cho cross-service sync). Chi tiết: `technical-architecture.md §7.4`                                  |
| **Canonical matrix**     | `docs/architecture/permission-matrix.md §6` — bảng 6×51 là SSOT cho RBAC; mọi doc khác phải trỏ tới đây                                                                                                  |
| **const-object pattern** | Cú pháp `export const X = {...} as const; export type X = typeof X[keyof typeof X]` thay cho TS `enum` — do `erasableSyntaxOnly` của customer-pwa. Xem `libs/shared/types/src/lib/order.types.ts` header |
| **"AD" git status**      | File được stage là mới (Added) sau đó deleted khỏi working tree. Giải quyết đúng: hoặc commit deletion (`git add <path>`), hoặc restore (`git restore <path>`). Xem §5.1                                 |

---

## 1. Executive Summary

| Axis                            | Verdict                    | Key numbers                                                                                                                                                                                                                                                                                     |
| ------------------------------- | -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Step 2.0 — code ↔ data**     | ✅ **SYNCED**              | `role.enum.ts` 51 PERMISSION + 6 ROLE · `role.json` counts 51/32/31/14/5/5 · MongoDB actual counts **identical** 51/32/31/14/5/5 · 7 users (bootstrap unchanged) · 0 legacy `ACCOUNTANT/ADMINISTRATOR` ref trong `apps/`** hoặc `libs/`**                                                       |
| **Step 2.3 — code ↔ code**     | ✅ **SYNCED**              | `libs/shared/types/src/lib/` có 6 entities + 7 enums + 3 transition matrices + 5 event/data types đúng spec; `libs/shared/constants/src/lib/status.ts` derive từ enum bằng `Object.values` (always-in-sync); mock-data đã refactor                                                              |
| **Step 2.0 — code ↔ Keycloak** | 🔴 **DRIFT**               | Realm `qrtable` tồn tại song song **uppercase** (`OWNER/MANAGER/WAITER/CHEF/BARISTA/SUPER_ADMIN` — đúng) **và lowercase legacy** (`owner/manager/waiter/chef/barista`) — cần dọn                                                                                                                |
| **Step 2.0 — doc internal**     | 🟠 **DRIFT**               | `auth-system-reference.md` ToC + heading `§3` ghi "5 Actors" nhưng body đã cập nhật 6 roles · `business-logic.md §9.A.3` vẫn nhắc "Inventory Staff (Thủ kho)" không có trong enum · `business-logic.md §9.B` vẫn dùng bảng 4-actor cũ, không link tới canonical matrix                          |
| **Step 2.3 — doc internal**     | 🟠 **DRIFT**               | `phase-2a-order-kafka.md` Step 2.3 mô tả bằng cú pháp `enum` nhưng code dùng `const object` (lý do: `erasableSyntaxOnly`) · Step 2.3 thiếu badge `✅ Done` · Spec/plan Step 2.3 trình bày `export enum OrderStatus` trong code-blocks nhưng triển khai thực tế là const-object — cần ADR inline |
| **Step 2.2 status drift**       | ✅ **CLOSED** (2026-04-21) | `phase-2a-order-kafka.md:88` đã đổi badge sang `⬜ Not Started` + xóa 2 link broken. Xem F15.                                                                                                                                                                                                   |
| **Filesystem integrity**        | ✅ **CLOSED** (2026-04-21) | Spec file Step 2.2 đã drop khỏi git index (`git rm --cached`); disk + index đồng bộ với HEAD. Plan file chưa từng tồn tại — reference cũng đã xóa. 0 broken link `2026-04-19-step-2.2-mock-ui` trong canonical docs. Xem F2.                                                                    |
| **Cross-phase propagation**     | 🟡 **PARTIAL**             | `technical-architecture.md` đã update bills-in-Order + 4P+2AP + BFF Direct ✅ · `phase-3-payment.md`, `phase-4a-saga-hardening.md`, `erd_explanation.md` đang ở trạng thái modified (chưa commit) — need verify content alignment                                                               |

**Bottom line:** Step 2.0 & Step 2.3 ở layer **code + database đều đạt chuẩn production-like** cho Phase 2A. Các drift còn lại **100% là doc/Keycloak housekeeping**, không chặn Step 2.4 về mặt kỹ thuật nhưng **cần dọn trước khi merge Phase 2A** để tránh nợ tài liệu dồn sang Phase 3.

---

## 2. Phương Pháp & Phạm Vi Audit

### 2.1 Dimensions đã kiểm

| #   | Dimension                                                                                                                        | Approach                   | Tool                             |
| --- | -------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | -------------------------------- |
| D1  | **Code source** (enum + entities + guards)                                                                                       | Static read + grep         | `Read`, `Grep`                   |
| D2  | **Seeder data** (role.json)                                                                                                      | Read + manual count        | `Read`                           |
| D3  | **MongoDB actual runtime state**                                                                                                 | Read-only query            | `docker exec mongosh`            |
| D4  | **Keycloak realm roles**                                                                                                         | Read-only kcadm            | `docker exec kcadm.sh get roles` |
| D5  | **Canonical docs** (permission-matrix, phase-2a, business-logic, technical-architecture, auth-system-reference, erd_explanation) | Full read + cross-ref grep | `Read`, `Grep`                   |
| D6  | **Superpowers spec/plan docs** cho Step 2.0 & 2.3                                                                                | Full read                  | `Read`                           |
| D7  | **Git state** (modified/staged/deleted)                                                                                          | `git status`, `git log`    | Shell                            |

### 2.2 Không kiểm trong audit này (noted cho Step 2.4)

- Runtime behaviour của `PermissionGuard`/`SessionGuard`/`TenantGuard` trên request thật (thuộc Layer 3 của Step 2.0 — đã có script `tools/verify-permission-matrix.sh` nhưng chưa chạy trong phiên này)
- Postgres schema cho Order domain (chưa tồn tại — Step 2.4)
- Redis key pattern cho session/cart (chưa implement — Step 2.4)
- Kafka broker connectivity (container `kafka_server` đang chạy nhưng chưa có producer/consumer)

---

## 3. Evidence Matrix — Step 2.0 (Permission Extension)

### 3.1 Permission count — 4 sources

| Source                                                                           | Count per role                                                     | Status       |
| -------------------------------------------------------------------------------- | ------------------------------------------------------------------ | ------------ |
| `libs/constants/src/lib/enum/role.enum.ts` — PERMISSION enum total               | **51**                                                             | ✅           |
| `apps/user-access/src/seeder/role.json` — permissions array lengths              | SUPER_ADMIN=51, OWNER=32, MANAGER=31, WAITER=14, CHEF=5, BARISTA=5 | ✅           |
| `docs/architecture/permission-matrix.md` §6 Total row                            | 51 / 32 / 31 / 14 / 5 / 5                                          | ✅           |
| **MongoDB** `db.role.find().permissions` (verified via `docker exec` 2026-04-21) | **51 / 32 / 31 / 14 / 5 / 5**                                      | ✅ **match** |

**Verdict:** 4-way consistency achieved. Step 2.0 Acceptance Criterion "role seed MongoDB khớp mapping OWNER/MANAGER/WAITER/CHEF/BARISTA" → **PASSED**.

### 3.2 Legacy enum removal

| Check                                                      | Expected                                               | Actual                                             | Status |
| ---------------------------------------------------------- | ------------------------------------------------------ | -------------------------------------------------- | ------ |
| `ROLE.ACCOUNTANT` in `apps/`** + `libs/`** + `tools/**`    | 0 refs                                                 | 0 refs (only in design docs as historical context) | ✅     |
| `ROLE.ADMINISTRATOR` in `apps/**` + `libs/**` + `tools/**` | 0 refs                                                 | 0 refs                                             | ✅     |
| `role.enum.ts` ROLE values                                 | 6 (SUPER_ADMIN, OWNER, MANAGER, WAITER, CHEF, BARISTA) | 6                                                  | ✅     |
| `user.repository.ts:84` micro-fix                          | `[ROLE.WAITER]` only                                   | confirmed via grep (no ACCOUNTANT)                 | ✅     |

### 3.3 Keycloak realm role sync

Query: `docker exec qrtable-provider-keycloak-1 /opt/keycloak/bin/kcadm.sh get roles -r qrtable`

| Expected roles                                                 | Present? | Unexpected legacy roles         | Severity          |
| -------------------------------------------------------------- | -------- | ------------------------------- | ----------------- |
| `SUPER_ADMIN`                                                  | ✅       | —                               | —                 |
| `OWNER`                                                        | ✅       | `owner` (lowercase duplicate)   | 🔴 drift          |
| `MANAGER`                                                      | ✅       | `manager` (lowercase duplicate) | 🔴 drift          |
| `WAITER`                                                       | ✅       | `waiter` (lowercase duplicate)  | 🔴 drift          |
| `CHEF`                                                         | ✅       | `chef` (lowercase duplicate)    | 🔴 drift          |
| `BARISTA`                                                      | ✅       | `barista` (lowercase duplicate) | 🔴 drift          |
| `uma_authorization`, `offline_access`, `default-roles-qrtable` | ✅       | —                               | — (built-ins, OK) |

**Impact:** `Authorizer.validateRoleMapping` (xem `auth-system-reference.md §5.2`) normalize cả 2 sets về uppercase trước khi check intersection → functional behaviour KHÔNG broken. Nhưng:

- Realm có **11 custom roles** thay vì 6 → gây nhầm lẫn khi vận hành
- Nếu admin accidentally assign `owner` (lowercase) cho user, `validateRoleMapping` vẫn pass (do normalize) nhưng `user.roles` trong Mongo sẽ không có entry lowercase → collectPermissions = [] → user login thành công nhưng permission DENIED → red-herring support ticket
- Sau Step 2.0 (cleanup ADMINISTRATOR/ACCOUNTANT), các lowercase này là hậu duệ của seed cũ — nên được dọn trong cùng đợt housekeeping/

### 3.4 Documentation drift cho Step 2.0

| Location                                                           | Issue                                                                                                                                                                               | File:Line                             | Severity |
| ------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | -------- |
| `docs/references/auth-system-reference.md` ToC                     | "5 Actors/Roles Chính Của Hệ Thống" nhưng body (§3.1) đã có 6 roles                                                                                                                 | `auth-system-reference.md:13`, `:139` | 🟠       |
| `docs/business-logic.md` §9.A.3 Staff sub-roles                    | Liệt kê "Inventory Staff (Thủ kho)" — role KHÔNG tồn tại trong ROLE enum                                                                                                            | `business-logic.md:683`               | 🟠       |
| `docs/business-logic.md` §9.A.2                                    | Mô tả MANAGER là "sub-role" của "Restaurant Owner/Manager" — nhưng ROLE enum có MANAGER là top-level role độc lập                                                                   | `business-logic.md:657-660`           | 🟡       |
| `docs/business-logic.md` §9.B "Permission Matrix (4 Actors Chính)" | Matrix 4-actor (Super Admin, Restaurant Owner, Staff-Waiter, Staff-Chef/Bar, Customer) không còn là canonical sau Step 2.0; nên trỏ tới `docs/architecture/permission-matrix.md §6` | `business-logic.md:719-747`           | 🟡       |
| `docs/architecture/permission-matrix.md` §5 "Removed in Step 2.0"  | ✅ đã nêu việc xóa ADMINISTRATOR/ACCOUNTANT — OK                                                                                                                                    | `permission-matrix.md:117-118`        | ✅       |

---

## 4. Evidence Matrix — Step 2.3 (Shared Types & Realtime Contract)

### 4.1 Type catalogue verification

| Spec requirement                                | Source file                                                                   | Present? | Notes                                                                                         |
| ----------------------------------------------- | ----------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------- |
| `Order`, `OrderItem` entities                   | `libs/shared/types/src/lib/order.types.ts`                                    | ✅       | 10+ audit fields (confirmedAt, cancelledByUserId, etc.)                                       |
| `Bill` entity                                   | `libs/shared/types/src/lib/bill.types.ts`                                     | ✅       | orderIds[], subtotal/total/roundingAmount, paymentMethod optional                             |
| `Session`, `CartItem`                           | `libs/shared/types/src/lib/session.types.ts`                                  | ✅       | CartItem có `version: number` (optimistic lock)                                               |
| `ServiceRequest`                                | `libs/shared/types/src/lib/service-request.types.ts`                          | ✅       | audit fields: acknowledgedAt, resolvedAt                                                      |
| `OrderStatus` enum                              | `order.types.ts`                                                              | ✅       | DRAFT, PENDING, PROCESSING, READY, SERVED, COMPLETED, CANCELED                                |
| `OrderItemStatus`                               | `order.types.ts`                                                              | ✅       | PROCESSING, READY, SERVED, CANCELED                                                           |
| `BillStatus`                                    | `bill.types.ts`                                                               | ✅       | OPEN, PENDING_PAYMENT, PAID                                                                   |
| `SessionStatus`                                 | `session.types.ts`                                                            | ✅       | ACTIVE, CLOSED                                                                                |
| `ServiceRequestType`                            | `service-request.types.ts`                                                    | ✅       | CALL_STAFF, REQUEST_BILL, GENERAL_HELP                                                        |
| `ServiceRequestStatus`                          | `service-request.types.ts`                                                    | ✅       | PENDING, ACKNOWLEDGED, RESOLVED                                                               |
| `PaymentMethod` (CASH only Phase 2A)            | `bill.types.ts`                                                               | ✅       | CASH only, commented placeholders cho Phase 3                                                 |
| `ALLOWED_ORDER_TRANSITIONS`                     | `order.types.ts`                                                              | ✅       | khớp `business-logic.md §8` + có `DRAFT → CANCELED` (customer abandon)                        |
| `ALLOWED_BILL_TRANSITIONS`                      | `bill.types.ts`                                                               | ✅       | Có phép `PENDING_PAYMENT → OPEN` (revert case)                                                |
| `ALLOWED_SERVICE_REQUEST_TRANSITIONS`           | `service-request.types.ts`                                                    | ✅       | PENDING → ACKNOWLEDGED → RESOLVED                                                             |
| `OrderCreatedEvent` (BFF Direct)                | `realtime-events.types.ts`                                                    | ✅       |                                                                                               |
| `OrderStatusChangedEvent` (BFF Direct)          | `realtime-events.types.ts`                                                    | ✅       | `fromStatus`, `toStatus`, optional `changedByUserId`                                          |
| `ServiceRequestedEvent` (BFF Direct)            | `realtime-events.types.ts`                                                    | ✅       |                                                                                               |
| `OrderConfirmedEvent` (Kafka `order.confirmed`) | `realtime-events.types.ts`                                                    | ✅       | Partition key = tenantId (doc comment)                                                        |
| `KDSTicket` data shape                          | `realtime-events.types.ts`                                                    | ✅       | priority, slaSeconds                                                                          |
| Barrel export                                   | `libs/shared/types/src/index.ts`                                              | ✅       | exports tất cả, runtime values (enums + matrices) + types                                     |
| Constants derived                               | `libs/shared/constants/src/lib/status.ts`                                     | ✅       | `Object.values(*)` — always-in-sync                                                           |
| Mock-data refactor                              | `libs/shared/mock-data/src/lib/{bills,carts,service-requests}.ts`             | ✅       | new files present                                                                             |
| Tests Layer 1                                   | `libs/shared/types/src/lib/__tests__/{enum-completeness,transitions}.spec.ts` | ✅       | present (runtime PASS chưa verify trong phiên này — cần `npx nx test shared-types` run riêng) |

**Verdict:** Step 2.3 code-layer đạt 100% theo spec. Không có I-prefix còn sót. Không có TS `enum` leftover cho domain types.

### 4.2 Const-object vs TS `enum` — ADR trace

**Observation:** Source code (`order.types.ts:17`, `bill.types.ts:14`, v.v.) dùng **const object + type alias pattern**, KHÔNG phải TS `enum`.

**Rationale in code (header comments):** `erasableSyntaxOnly` trong `customer-pwa` tsconfig — TS `enum` sinh runtime code phải được strip, const object tương thích 100%.

**Drift trong tài liệu:**

| Doc                                                                 | Cú pháp dùng                                                               | Drift với code?                                                  |
| ------------------------------------------------------------------- | -------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| `phase-2a-order-kafka.md` Step 2.3 §"Chi tiết field"                | `OrderStatus { DRAFT, PENDING, ... }` — mơ hồ (có thể hiểu là enum)        | 🟡 mơ hồ                                                         |
| `docs/superpowers/specs/2026-04-19-step-2.3-shared-types-design.md` | `export enum OrderStatus {...}` trong code blocks                          | 🟠 drift                                                         |
| `docs/superpowers/plans/2026-04-19-step-2.3-shared-types.md` Task 2 | `export enum OrderStatus {...}`                                            | 🟠 drift (đã thực hiện khác với plan, chưa update plan post-hoc) |
| Source code                                                         | `export const OrderStatus = {...} as const; export type OrderStatus = ...` | ✅ ground truth                                                  |

**Assessment:** Đây là drift **ngược** — implementation tốt hơn spec (đúng lý do kỹ thuật) nhưng spec/plan chưa được cập nhật ngược. Chấp nhận được nhưng nên thêm ADR mini trong `phase-2a-order-kafka.md` Step 2.3 để người đọc không bị confuse khi copy snippet từ phase doc.

### 4.3 State machine alignment: code vs `business-logic.md §8`

| Transition (code)       | `business-logic.md §8.B` mentions?                              | Notes                                                             |
| ----------------------- | --------------------------------------------------------------- | ----------------------------------------------------------------- |
| `DRAFT → PENDING`       | ✅ (Draft → Pending, "Submit Order")                            | khớp                                                              |
| `DRAFT → CANCELED`      | ❌ không nêu                                                    | code ALLOW customer abandon cart (hợp lý, nên note)               |
| `PENDING → PROCESSING`  | ✅                                                              | khớp                                                              |
| `PENDING → CANCELED`    | ✅                                                              | khớp                                                              |
| `PROCESSING → READY`    | ✅                                                              | khớp                                                              |
| `PROCESSING → CANCELED` | ✅ (Manager-only)                                               | khớp — business rule note                                         |
| `READY → SERVED`        | ✅                                                              | khớp                                                              |
| `SERVED → COMPLETED`    | ✅                                                              | khớp                                                              |
| **Casing**              | `Draft/Pending/Processing/Canceled` (mixed-case display labels) | Code dùng UPPERCASE — nên có note "display labels vs enum values" |

### 4.4 Step 2.3 doc-drift inventory

| Issue                                                                                              | File:Line                        | Severity |
| -------------------------------------------------------------------------------------------------- | -------------------------------- | -------- |
| `phase-2a-order-kafka.md` Step 2.3 thiếu badge `✅ Done` (Step 2.2 có)                             | `phase-2a-order-kafka.md:~135`   | 🟠       |
| `phase-2a-order-kafka.md` Step 2.3 dùng cú pháp `OrderStatus { ... }` mơ hồ (enum vs const-object) | `phase-2a-order-kafka.md:114`    | 🟡       |
| `business-logic.md §8` dùng labels mixed-case, chưa note rằng enum code là UPPERCASE               | `business-logic.md:517-548`      | 🟡       |
| `business-logic.md §8.B` thiếu `Draft → Canceled` transition (code có)                             | `business-logic.md:553-625`      | 🟡       |
| Spec 2.3 + plan 2.3 giữ `export enum` trong code snippets                                          | spec `:291-343`, plan `:157-478` | 🟠       |

---

## 5. Filesystem & Git Integrity

### 5.1 Step 2.2 revert residue — ✅ Resolved (2026-04-21)

> **Status update:** Section này mô tả trạng thái **trước khi fix** (historical). Cleanup đã hoàn tất — xem §5.1.Resolution ở cuối.

**Context từ owner (xác nhận 2026-04-21):** Step 2.2 ban đầu đã được **thử triển khai** sau khi Step 2.3 đóng (thứ tự: 2.0 → 2.3 → 2.2). Attempt sau đó **bị revert toàn bộ** (code changes rollback, không commit vào `main`). Tuy nhiên việc dọn dẹp sau revert **chưa hoàn tất** ở 3 layer:

1. **Filesystem layer:** Spec file đã được `git add` trước khi revert → vẫn còn trong index dù working tree đã bị `rm`.
2. **Doc layer:** `phase-2a-order-kafka.md:88` vẫn mang badge `✅ Done (2026-04-19)` cho Step 2.2 — badge được viết trong attempt, không revert.
3. **Link layer:** Phase doc và Step 2.3 plan trỏ tới 2 file không tồn tại.

```
git status cho Step 2.2 artifacts:
  Changes to be committed:   new file:   docs/superpowers/specs/2026-04-19-step-2.2-mock-ui-design.md  ← đang trong index
  Changes not staged:        deleted:    docs/superpowers/specs/2026-04-19-step-2.2-mock-ui-design.md  ← working tree đã rm
```

**Diagnosis:** File được stage thêm (chuẩn bị commit cho attempt) rồi khi revert chỉ `rm` khỏi working tree, quên `git add -u` để record deletion vào index. Git index hiện giữ **bản cũ (staged-new)**, filesystem **trống**.

**Broken references hiện tại:**

| Nguồn reference                                                                                      | Target                                                         | Trạng thái                                                                                             |
| ---------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| `docs/phases/phase-2a-order-kafka.md:88` link 1                                                      | `docs/superpowers/specs/2026-04-19-step-2.2-mock-ui-design.md` | 🔴 broken (working tree không có file)                                                                 |
| `docs/phases/phase-2a-order-kafka.md:88` link 2                                                      | `docs/superpowers/plans/2026-04-19-step-2.2-mock-ui.md`        | 🔴 broken (file CHƯA TỪNG tồn tại trên git history — chắc plan chỉ nằm trong attempt local chưa stage) |
| `docs/superpowers/plans/2026-04-19-step-2.3-shared-types.md` (pre-flight reading hoặc task doc-sync) | Step 2.2 spec                                                  | 🟡 nhỏ — nếu có reference thì cần xóa                                                                  |

**Correct fix direction** (được chọn):

- **KHÔNG restore spec** — vì Step 2.2 chưa triển khai, spec chưa có giá trị canonical
- **Drop khỏi git index** — dùng `git rm --cached` để sync index với HEAD state (cleaner hơn `git add` vì không có HEAD baseline)
- **Fix `phase-2a-order-kafka.md:88`** — đổi badge `✅ Done` → `⬜ Not Started` + xóa 2 link broken
- **Grep repo** để đảm bảo không còn reference nào khác tới 2 file đã xóa

#### 5.1.Resolution — Executed 2026-04-21

| Action                              | Command                                                                              | Outcome                                                                      |
| ----------------------------------- | ------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------- |
| Drop spec khỏi git index            | `git rm --cached docs/superpowers/specs/2026-04-19-step-2.2-mock-ui-design.md`       | ✅ File không còn trong index; `git status` cho path này trống               |
| Verify disk state                   | `ls docs/superpowers/specs/2026-04-19-step-2.2-mock-ui-design.md`                    | ✅ `No such file or directory` (đã đồng bộ)                                  |
| Update `phase-2a-order-kafka.md:88` | Manual edit qua StrReplace                                                           | ✅ Badge `⬜ Not Started` + prose mô tả thứ tự thực tế; 2 link broken đã xóa |
| Verify canonical docs               | `rg "2026-04-19-step-2\.2-mock-ui" docs/` (loại trừ self-context trong audit + plan) | ✅ 0 matches trong canonical docs                                            |

**Plan file** `docs/superpowers/plans/2026-04-19-step-2.2-mock-ui.md` chưa từng tồn tại trên disk/git → không cần action; chỉ cần đảm bảo không còn reference (đã verify).

**Remaining references** nằm trong 2 file do audit process sinh ra (self-context, không phải broken link):

- `docs/superpowers/reports/2026-04-21-step-2.0-2.3-sync-audit.md` (file này — mô tả issue)
- `docs/superpowers/plans/2026-04-21-step-2.0-2.3-sync-fix.md` (companion plan — Task 2 instructions)

Hai file này sẽ được archive khi tất cả findings đóng (xem §8 DoD).

### 5.2 Ahead of origin

```
main...origin/main [ahead 1]
```

Có 1 commit local chưa push. `git log --oneline -2`:

- `5089d41 feat(realtime): introduce shared types and websocket contract for order domain` ← Step 2.3
- `e81e439 feat(auth): introduce canonical permission matrix and extend domain permissions` ← Step 2.0

Step 2.3 commit đã tồn tại, Step 2.0 commit đã push trước. Nghĩa là Step 2.0 và 2.3 đã đóng ở layer git — chỉ còn **doc-sync + Keycloak cleanup + broken link** là pending.

---

## 6. Consolidated Findings Table

| ID  | Step    | Layer  | Summary                                                                                                                                                                                                              | Severity  | Source of Truth                               | Location cần sửa                                                                              |
| --- | ------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | --------------------------------------------- | --------------------------------------------------------------------------------------------- |
| F1  | 2.3     | Doc    | `phase-2a-order-kafka.md` Step 2.3 mơ hồ giữa TS enum vs const-object                                                                                                                                                | 🟡 Minor  | `libs/shared/types/src/lib/order.types.ts:17` | `phase-2a-order-kafka.md` ~line 114                                                           |
| F2  | 2.2     | FS     | ✅ **CLOSED (2026-04-21)** — Spec Step 2.2 đã drop khỏi git index bằng `git rm --cached`; plan Step 2.2 không tồn tại → không action. Link trong `phase-2a:88` đã xóa (xem F15). 0 broken link trong canonical docs. | ✅ Closed | Executed 2026-04-21                           | — (hoàn tất)                                                                                  |
| F3  | 2.0     | DB     | Verified ✅ CLOSED — MongoDB role counts match role.json                                                                                                                                                             | ✅        | MongoDB                                       | —                                                                                             |
| F4  | 2.0     | DB     | Verified ✅ CLOSED — permission strings match enum                                                                                                                                                                   | ✅        | MongoDB                                       | —                                                                                             |
| F5  | 2.0     | Doc    | `business-logic.md §9.A` dùng 4-actor model cũ + mention Inventory Staff không có trong enum                                                                                                                         | 🟠 Medium | `role.enum.ts` + `permission-matrix.md`       | `business-logic.md:657-747`                                                                   |
| F6  | 2.3     | Doc    | `phase-2a-order-kafka.md` Step 2.3 thiếu badge `✅ Done`                                                                                                                                                             | 🟠 Medium | git commit `5089d41`                          | `phase-2a-order-kafka.md` ~line 135                                                           |
| F7  | 2.0     | IAM    | Keycloak realm `qrtable` có **duplicate lowercase legacy roles** (owner/manager/waiter/chef/barista)                                                                                                                 | 🔴 Major  | `role.enum.ts` ROLE values                    | Keycloak realm (thông qua `kcadm.sh delete`)                                                  |
| F8  | 2.0     | Doc    | `auth-system-reference.md` ToC + H2 `§3` ghi "5 Actors" trong khi body đã là 6 roles                                                                                                                                 | 🟠 Medium | `role.enum.ts`                                | `auth-system-reference.md:13`, `:139`                                                         |
| F9  | 2.3     | Doc    | Spec 2.3 + plan 2.3 giữ `export enum` trong code blocks (implementation đã đổi sang const-object)                                                                                                                    | 🟠 Medium | `order.types.ts:10-14` header ADR             | spec `2026-04-19-step-2.3-shared-types-design.md`, plan `2026-04-19-step-2.3-shared-types.md` |
| F10 | 2.3     | Doc    | `business-logic.md §8` dùng mixed-case labels, không note UPPERCASE là enum canonical                                                                                                                                | 🟡 Minor  | `order.types.ts` enum values                  | `business-logic.md:517-548`                                                                   |
| F11 | 2.3     | Doc    | `business-logic.md §8.B` không có `Draft → Canceled` rule (code cho phép)                                                                                                                                            | 🟡 Minor  | `ALLOWED_ORDER_TRANSITIONS` code              | `business-logic.md:553-625`                                                                   |
| F12 | 2.0/2.3 | Verify | Chưa verify Layer 3 `tools/verify-permission-matrix.sh` + `npx nx test shared-types` trong phiên audit này                                                                                                           | 🟡 Minor  | —                                             | Cần chạy khi fix-plan executed                                                                |
| F13 | X-phase | Doc    | `phase-3-payment.md`, `phase-4a-saga-hardening.md`, `erd_explanation.md` đang **modified chưa commit** — chưa confirm có tham chiếu đúng Step 2.3 types không                                                        | 🟡 Minor  | git status                                    | Cần review diff các file modified                                                             |
| F14 | 2.0     | Doc    | `business-logic.md §9.A.2` mô tả MANAGER như sub-role của Owner (lịch sử), không khớp với ROLE enum (MANAGER top-level)                                                                                              | 🟡 Minor  | `role.enum.ts`                                | `business-logic.md:657-660`                                                                   |
| F15 | 2.2     | Doc    | ✅ **CLOSED (2026-04-21)** — Badge `phase-2a-order-kafka.md:88` đã đổi `✅ Done` → `⬜ Not Started` + prose phản ánh đúng thứ tự `2.0 → 2.3 → (2.2 pending) → 2.4 → 2.5`; 2 link broken đã xóa.                      | ✅ Closed | Executed 2026-04-21                           | — (hoàn tất)                                                                                  |

**Severity distribution:** 0 × 🔴 Major · 4 × 🟠 Medium · 6 × 🟡 Minor · **4 ✅ Closed** (F2, F3, F4, F15). Tổng: **15 findings** (F1–F15); 11 still open.

**Remaining blocker to "close" 2.0 & 2.3:** F7 (Keycloak duplicate lowercase roles — 🔴 Major). F1, F5, F6, F8–F14 là doc housekeeping (🟠 Medium + 🟡 Minor).

---

## 7. Acceptance-Check Against Phase 2A §Acceptance Criteria

Ref: `docs/phases/phase-2a-order-kafka.md` lines 185-196.

| Acceptance criterion (trích phase doc)                            | Status hiện tại                 | Evidence                                                                                         |
| ----------------------------------------------------------------- | ------------------------------- | ------------------------------------------------------------------------------------------------ |
| Enum permission Step 2.0 đầy đủ                                   | ✅                              | 51 values trong `role.enum.ts`                                                                   |
| Role seed MongoDB khớp OWNER/MANAGER/WAITER/CHEF/BARISTA          | ✅                              | verified via `docker exec mongosh`                                                               |
| Endpoint staff đi qua `UserGuard → TenantGuard → PermissionGuard` | ⏳                              | Guard chain có sẵn (guards/src/lib/\*), nhưng chưa có endpoint Order Service để apply — Step 2.4 |
| CUSTOMER bọc SessionGuard đúng scope                              | ⏳                              | SessionGuard có sẵn, endpoints Step 2.4                                                          |
| `OrderStatus` và chuyển trạng thái khớp §8                        | ✅ (với note mixed-case labels) | `ALLOWED_ORDER_TRANSITIONS`                                                                      |
| Redis session + cart TTL rules                                    | ⏳                              | Spec document hóa, implement Step 2.4                                                            |
| Bill thuộc Order Service (PostgreSQL)                             | ✅ (design level)               | `technical-architecture.md §6.2.5` + `erd.dbml`                                                  |
| Kafka `order.confirmed`                                           | ⏳                              | Broker chạy, producer Step 2.4                                                                   |
| BFF Direct `order.created` / `service.requested`                  | ⏳                              | Event types defined, emit Step 2.4                                                               |
| Pessimistic lock cho confirm                                      | ⏳                              | Step 2.4                                                                                         |
| Atomic table transfer                                             | ⏳                              | Step 2.4                                                                                         |
| Customer PWA + POS dùng API thật                                  | ⏳                              | Step 2.5                                                                                         |

**Phase 2A sub-total (2.0 + 2.3 only):** 2/2 code-layer acceptance criterion passed. Remaining criteria thuộc Step 2.4 + 2.5.

---

## 8. Definition of Done — Proposed Gate to "close" 2.0 + 2.3

Để tuyên bố Step 2.0 + Step 2.3 là **CLOSED** trước khi mở Step 2.4, cần 5 điều kiện:

1. ✅ **F2 resolved (2026-04-21)** — Spec Step 2.2 đã drop khỏi git index; không còn file `AD` trong `git status`.
2. ✅ **F15 resolved (2026-04-21)** — `phase-2a-order-kafka.md:88` phản ánh đúng Step 2.2 = `⬜ Not Started`, 0 link broken.
3. ⬜ **F7 resolved** — Keycloak realm `qrtable` chỉ còn 6 uppercase custom roles + 3 built-ins (không còn lowercase legacy `owner/manager/waiter/chef/barista`).
4. ⬜ **F5, F6, F8, F9 resolved** — 4 drift 🟠 medium ở doc layer.
5. ⬜ **F12 green** — `tools/verify-permission-matrix.sh` PASS + `npx nx test shared-types` PASS trong môi trường local của owner.

F1, F10, F11, F13, F14 là 🟡 minor, batch fix chung nhưng KHÔNG chặn acceptance.

**Exit state kỳ vọng sau fix plan:**

- ✅ `git status` sạch đối với spec Step 2.2 (đã drop khỏi index 2026-04-21)
- ✅ Mọi `[Step 2.2 ...](...)` link broken trong canonical docs đã xóa (2026-04-21)
- ⬜ `phase-2a-order-kafka.md` có bảng "Step progress" chính xác: 2.0 ✅, 2.1 ✅, 2.2 ⬜, 2.3 ✅, 2.4 ⬜, 2.5 ⬜ (chờ verify/add nếu chưa có)
- ⬜ `docs/architecture/permission-matrix.md` được link từ tất cả doc nhắc roles (Task 5)
- ⬜ Keycloak `kcadm.sh get roles -r qrtable` → chỉ 6 uppercase custom + 3 built-ins (Task 3)

---

## 9. Related Artifacts

| File                                                                        | Purpose                                                      |
| --------------------------------------------------------------------------- | ------------------------------------------------------------ |
| `docs/superpowers/plans/2026-04-21-step-2.0-2.3-sync-fix.md`                | **Executable fix plan** tương ứng với report này (companion) |
| `docs/architecture/permission-matrix.md`                                    | Canonical RBAC matrix — Step 2.0 deliverable                 |
| `docs/superpowers/specs/2026-04-19-step-2.0-permission-extension-design.md` | Step 2.0 design spec                                         |
| `docs/superpowers/specs/2026-04-19-step-2.3-shared-types-design.md`         | Step 2.3 design spec                                         |
| `tools/verify-permission-matrix.sh`                                         | Layer 3 Step 2.0 integration test                            |

---

## 10. Changelog

| Date       | Author                  | Change                                                                                                                                                                                         |
| ---------- | ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-04-21 | Superpowers audit agent | Initial audit — 12 open findings, 2 closed via MongoDB verification                                                                                                                            |
| 2026-04-21 | Superpowers audit agent | Rev 2 — Owner xác nhận Step 2.2 revert; thêm §0 Context & Orientation + F15 (badge sai trong phase-2a:88); F2 tái định hướng "record deletion" thay vì "restore"; DoD nâng lên 5 điều kiện     |
| 2026-04-21 | Superpowers audit agent | Rev 3 — Execute Task 2: `git rm --cached` spec Step 2.2 + fix `phase-2a:88` badge → F2 & F15 CLOSED; §1/§5.1/§6/§8 update; severity distribution 3 🔴 → 0 🔴; 4/15 findings closed, 11 pending |
