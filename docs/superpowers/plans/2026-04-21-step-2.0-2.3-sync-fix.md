# Step 2.0 & 2.3 — Sync Fix Plan

> **Audience:** Owner (để review + approve) **và** agent khác (để execute từng task). File này tự đủ — không cần context ngoài ngoại trừ companion report.
>
> **Companion (context + findings):** [`docs/superpowers/reports/2026-04-21-step-2.0-2.3-sync-audit.md`](../reports/2026-04-21-step-2.0-2.3-sync-audit.md)
>
> **Execution model:**
>
> - Execute task-by-task theo thứ tự số. Task 4–9 có thể parallel, phần còn lại phải tuần tự.
> - Verify sau mỗi step con `[ ]` bằng block `bash` đi kèm.
> - **Không `git commit` bất cứ step nào** — commits do owner xử lý sau khi review PR/diff cuối.
> - Mỗi task map 1-1 (hoặc 1-nhiều) tới findings `Fxx` trong report §6.

---

## 0. Context & Orientation (đọc TRƯỚC)

### 0.1 Ground-truth Phase 2A status (quan trọng — nhiều task phụ thuộc state này)

| Step                              | Status             | Commit / Note                                                                                   |
| --------------------------------- | ------------------ | ----------------------------------------------------------------------------------------------- |
| 2.0 — Permission extension        | ✅ DONE            | `e81e439` (2026-04-19)                                                                          |
| 2.1 — Kafka foundation (learning) | 🟢 Course material | n/a                                                                                             |
| **2.2 — Mock UI**                 | 🔴 **NOT DONE**    | Owner confirm 2026-04-21: từng thử triển khai, sau đó **revert toàn bộ**. Không có code landed. |
| 2.3 — Shared types                | ✅ DONE            | `5089d41` (2026-04-19)                                                                          |
| 2.4 — Order Service backend       | ⬜ NOT STARTED     | —                                                                                               |
| 2.5 — FE/BE integration           | ⬜ NOT STARTED     | —                                                                                               |

Thứ tự triển khai đã thực tế: **2.0 → 2.3 → (thử 2.2 → revert)**. Lý do: Step 2.3 là refactor types thuần, khóa contract trước; Step 2.2 (UI) làm sau để dựa trên types đã stable.

### 0.2 Hard constraints cho mọi task bên dưới

- ❌ **KHÔNG restore** file `docs/superpowers/specs/2026-04-19-step-2.2-mock-ui-design.md`. File này là **residue của revert** — phải record deletion, không phục hồi. Bất cứ task nào nghi ngờ sẽ restore → dừng và hỏi owner.
- ❌ **KHÔNG chạm** `apps/**/src/**` hoặc `libs/**/src/**`. Fix plan này chỉ sửa docs + Keycloak runtime state.
- ❌ **KHÔNG đổi** ma trận permission trong `role.enum.ts` / `role.json` / `permission-matrix.md`. MongoDB đã sync 100% → không có reason để đổi.
- ❌ **KHÔNG git push**. Owner xử lý.
- ✅ **ĐƯỢC PHÉP** `docker exec` mongodb/keycloak read-only để verify.
- ✅ **ĐƯỢC PHÉP** edit bất cứ file `.md` nào trong `docs/` nếu Task mapping yêu cầu.

### 0.3 Goal sau khi plan execute xong

Fix **15 findings** từ report (3 🔴 blockers + 4 🟠 medium + 6 🟡 minor + 2 ✅ already closed). Exit state:

- ✅ Canonical docs (`permission-matrix.md`, `phase-2a-order-kafka.md`, `business-logic.md`, `auth-system-reference.md`) đồng bộ với source code.
- ✅ Keycloak realm `qrtable` chỉ còn 6 uppercase custom roles + 3 built-ins.
- ✅ `phase-2a-order-kafka.md:88` phản ánh đúng Step 2.2 = `⬜ Not Started`, không broken link.
- ✅ `phase-2a-order-kafka.md` Step 2.3 có badge `✅ Done` + ADR const-object pattern.
- ✅ `git status` sạch đối với spec Step 2.2 (deletion đã record vào index).
- ✅ Layer 1 (Jest) + Layer 3 (`verify-permission-matrix.sh`) PASS (hoặc scheduled nếu stack chưa chạy).

**Tech stack affected:** Markdown (docs), Keycloak 25 (`kcadm.sh`), Bash (verify scripts), Jest 30, TypeScript (không chạm production code).

---

## Pre-flight Reading (REQUIRED trước khi execute Task 1)

Agent follow plan này PHẢI đọc tuần tự:

1. **Report file** (companion) — §0 Context + §5 Filesystem + §6 Findings + §8 DoD
2. **Canonical matrix** — `docs/architecture/permission-matrix.md` (SSOT cho RBAC)
3. **Current enum** — `libs/constants/src/lib/enum/role.enum.ts` (6 ROLE + 51 PERMISSION)
4. **Current types** — `libs/shared/types/src/lib/order.types.ts` (header comment có ADR const-object)
5. **Phase doc** — `docs/phases/phase-2a-order-kafka.md` (đặc biệt: lines ~85–95 về Step 2.2; Step 2.3 block)
6. **Business doc** — `docs/business-logic.md` §8 (Order state machine) + §9 (Actor permissions)
7. **Auth reference** — `docs/references/auth-system-reference.md` §3 (ToC line 13 + H2 line 139)

---

## Task Map (10 tasks · ~1.5–2 hours, 15 findings)

**Progress:** ✅ **10/10 tasks done** (2026-04-21) · **15/15 findings closed**.

| #   | Task                                                                                                           | Finding(s)       | Type         | Est.   | Status                                                           |
| --- | -------------------------------------------------------------------------------------------------------------- | ---------------- | ------------ | ------ | ---------------------------------------------------------------- |
| 1   | Pre-flight verification (docker + git + file presence)                                                         | F2, F7, F12, F15 | Check        | 5 min  | ✅ **Done (2026-04-21)**                                         |
| 2   | **Record Step 2.2 revert đúng cách** — commit deletion + fix phase-2a:88 badge + remove broken links           | F2, F15          | FS/Git + Doc | 15 min | ✅ **Done (2026-04-21)**                                         |
| 3   | Cleanup Keycloak lowercase legacy roles                                                                        | F7               | IAM          | 15 min | ✅ **Done (2026-04-21)**                                         |
| 4   | Sync `auth-system-reference.md` "5 Actors" → "6 Roles"                                                         | F8               | Doc          | 10 min | ✅ **Done (2026-04-21)**                                         |
| 5   | Sync `business-logic.md` §9 (remove Inventory Staff, reframe MANAGER, link canonical matrix)                   | F5, F14          | Doc          | 20 min | ✅ **Done (2026-04-21)**                                         |
| 6   | Add `✅ Done` badge + const-object ADR to `phase-2a-order-kafka.md` Step 2.3                                   | F1, F6           | Doc          | 15 min | ✅ **Done (2026-04-21)**                                         |
| 7   | Add const-object ADR footnote to Step 2.3 spec + plan                                                          | F9               | Doc          | 10 min | ✅ **Done (2026-04-21)**                                         |
| 8   | Update `business-logic.md` §8 — UPPERCASE note + `Draft → Canceled` rule                                       | F10, F11         | Doc          | 15 min | ✅ **Done (2026-04-21)**                                         |
| 9   | Review + update modified phase docs — `phase-3-payment.md`, `phase-4a-saga-hardening.md`, `erd_explanation.md` | F13              | Doc          | 20 min | ✅ **Done (2026-04-21)** — `git diff` sạch, không chỉnh nội dung |
| 10  | Final acceptance — Layer 1 + Layer 3 verifies + re-run audit checklist                                         | F12              | Verify       | 15 min | ✅ **Done (2026-04-21)**                                         |

---

## Task 1 — Pre-flight Verification

**Files:** None (read-only checks).

**Goal:** Xác nhận trạng thái môi trường trước khi thay đổi.

- [x] **Step 1.1 — Docker stack healthy** (2026-04-21)

```bash
docker ps --format "table {{.Names}}\t{{.Status}}" | grep -E "mongodb|postgres|redis|keycloak|kafka"
```

Expected: 5 containers `Up … (healthy)` cho mongodb/postgres/redis/keycloak (kafka không có healthcheck).

Nếu thiếu: `docker compose -f docker-compose.provider.yaml up -d`.

- [x] **Step 1.2 — Git status clean trừ docs-pending** (2026-04-21 — không có thay đổi `apps/**` / `libs/**/src/**` pending)

```bash
git status --short
```

Expected output KHÔNG có code file (apps/** libs/**) trong M/A/D list. Cho phép: `docs/**` + `docs/superpowers/**` pending.

- [x] **Step 1.3 — Xác nhận Step 2.2 state** (✅ 2026-04-21: F2 + F15 đã closed — skip Task 2)

```bash
ls -la docs/superpowers/specs/2026-04-19-step-2.2-mock-ui-design.md 2>&1 | head -3
git status docs/superpowers/specs/2026-04-19-step-2.2-mock-ui-design.md
rg -n "Step 2.2.*✅ Done|step-2.2-mock-ui" docs/phases/phase-2a-order-kafka.md
```

Actual post-fix state (2026-04-21):

- `ls` → `No such file or directory` ✅
- `git status` → empty (clean; file đồng bộ với HEAD) ✅
- `rg` → 0 matches trong `phase-2a-order-kafka.md` ✅

**→ F2 + F15 closed. Task 2 không cần execute lại.**

- [x] **Step 1.4 — Xác nhận Keycloak có duplicate roles** (2026-04-21 — có 5 lowercase trước Task 3; sau cleanup: 0)

```bash
docker exec qrtable-provider-keycloak-1 /opt/keycloak/bin/kcadm.sh config credentials --server http://localhost:8080 --realm master --user admin --password admin >/dev/null 2>&1
docker exec qrtable-provider-keycloak-1 /opt/keycloak/bin/kcadm.sh get roles -r qrtable --fields name | grep -E '"(owner|manager|waiter|chef|barista)"'
```

Expected: 5 entries (lowercase legacy). Nếu 0 → F7 đã fix sẵn, skip Task 3.

- [x] **Step 1.5 — Xác nhận MongoDB state vẫn khớp role.json** (2026-04-21 — 6 roles, 7 users)

```bash
docker exec qrtable-provider-mongodb-1 mongosh "mongodb://root:password@localhost:27017/qrtable?authSource=admin" --quiet --eval '
print(JSON.stringify(db.role.find({},{name:1,c:{$size:"$permissions"}}).toArray().map(r=>r.name+":"+r.c+"("+r.permissions.length+")"),null,2))
'
```

Expected: `SUPER_ADMIN:51`, `OWNER:32`, `MANAGER:31`, `WAITER:14`, `CHEF:5`, `BARISTA:5`.

---

## Task 2 — Record Step 2.2 Revert Properly (F2 + F15) — ✅ DONE (2026-04-21)

> **Execution log:** Task này đã hoàn tất 2026-04-21. Các sub-steps dưới giữ lại làm reference cho audit trail. Xem summary ở cuối task.

**Files (đã xử lý):**

- `docs/superpowers/specs/2026-04-19-step-2.2-mock-ui-design.md` (git index cleanup) — ✅ dropped via `git rm --cached`
- `docs/phases/phase-2a-order-kafka.md` (edit line ~88 block — Step 2.2 badge + links) — ✅ badge đổi `⬜ Not Started` + prose thứ tự `2.0 → 2.3 → (2.2 pending) → 2.4 → 2.5`
- Plan file `docs/superpowers/plans/2026-04-19-step-2.2-mock-ui.md` — không tồn tại → no action needed

**Context (từ owner, 2026-04-21):** Step 2.2 (Mock UI) đã được **thử triển khai** sau Step 2.3, sau đó **revert toàn bộ**. Residue của revert:

1. Spec file `2026-04-19-step-2.2-mock-ui-design.md` bị kẹt ở git `AD` (staged-new + working-deleted).
2. Plan file `2026-04-19-step-2.2-mock-ui.md` chưa từng tồn tại trên disk/git (nằm trong attempt local).
3. `phase-2a-order-kafka.md:88` vẫn ghi `✅ Done (2026-04-19)` + 2 link tới 2 file trên.

**Goal:** Phản ánh đúng thực tế "Step 2.2 chưa triển khai" ở cả 3 layer (git index + doc badge + broken links).

**HARD RULE:** KHÔNG restore spec file. Spec file bị delete là có chủ ý. Nếu tương lai làm lại Step 2.2, sẽ tạo spec mới từ đầu (file name mới date).

### Sub-steps (historical — all ✅ executed 2026-04-21)

- [x] **Step 2.1 — Record deletion của spec Step 2.2 vào git index**

Từ trạng thái `AD` (new in index + deleted in working tree), move sang ` D` (deletion staged, ready to commit).

```bash
git status docs/superpowers/specs/2026-04-19-step-2.2-mock-ui-design.md
```

Nếu thấy `A  ...` + ` D ...` → chạy:

```bash
git add docs/superpowers/specs/2026-04-19-step-2.2-mock-ui-design.md
```

`git add` một file đã bị `rm` khỏi working tree sẽ stage deletion, xóa staged-new → net effect: nothing staged cho file này (because staged-new + deletion = no change vs HEAD).

Verify:

```bash
git status docs/superpowers/specs/2026-04-19-step-2.2-mock-ui-design.md
```

Expected: file không còn xuất hiện trong `git status` (vì HEAD cũng không có file này → net zero change).

```bash
ls docs/superpowers/specs/2026-04-19-step-2.2-mock-ui-design.md 2>&1
```

Expected: `No such file or directory` — correct exit state.

- [x] **Step 2.2 — Fix badge + links trong `phase-2a-order-kafka.md:88`**

Mở file, tìm block quote gần line 88 (grep trước):

```bash
rg -n "Step 2.2|step-2.2-mock-ui" docs/phases/phase-2a-order-kafka.md
```

Tìm block tương tự:

```markdown
> **Status:** ✅ Done (2026-04-19) — see [Step 2.2 spec](../superpowers/specs/2026-04-19-step-2.2-mock-ui-design.md) and [plan](../superpowers/plans/2026-04-19-step-2.2-mock-ui.md)
```

Replace bằng:

```markdown
> **Status:** ⬜ Not Started — Step 2.2 đã từng được thử triển khai và **revert toàn bộ** (2026-04-19). Hiện chưa có spec/plan canonical. Sẽ lập kế hoạch lại khi bắt đầu sau Step 2.4. Thứ tự triển khai thực tế: Step 2.0 → 2.3 → (**2.2**, pending) → 2.4 → 2.5.
```

- [x] **Step 2.3 — Grep residue references khắp repo**

```bash
rg -n "2026-04-19-step-2\.2-mock-ui" --glob '!terminalOutput' --glob '!.git' .
```

Với mỗi match:

| Match ở đâu                                                                                      | Action                                                                                                 |
| ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------ |
| `docs/phases/phase-2a-order-kafka.md`                                                            | Đã fix Step 2.2 (verify còn match hay không)                                                           |
| `docs/superpowers/plans/2026-04-19-step-2.3-shared-types.md` (pre-flight reading, task doc-sync) | Xóa bullet reference tới Step 2.2 spec/plan, thay bằng note `(Step 2.2 scope deferred — see phase-2a)` |
| `docs/superpowers/specs/2026-04-19-step-2.3-shared-types-design.md`                              | Same pattern — xóa reference                                                                           |
| Bất cứ file `.md` nào khác                                                                       | Xóa reference hoặc thay placeholder tương tự                                                           |

Nếu có file cần edit, chạy `rg -n` trước → đọc từng match 3 dòng trên/dưới → sửa có chủ đích, không replace-all.

- [x] **Step 2.4 — Update bảng step-progress nếu tồn tại**

```bash
rg -n "^\|.*2\.2.*\|" docs/phases/phase-2a-order-kafka.md
```

Nếu có bảng tổng hợp progress — update cell Step 2.2 thành `⬜ Not Started`.

Nếu phase-2a không có bảng như vậy — skip.

- [x] **Step 2.5 — Verify final state**

```bash
rg -n "2026-04-19-step-2\.2-mock-ui" --glob '!terminalOutput' --glob '!.git' . ; echo "---"
rg -n "Step 2.2.*✅ Done" docs/ ; echo "---"
git status docs/superpowers/specs/2026-04-19-step-2.2-mock-ui-design.md
```

Expected exit state:

- Block 1: 0 matches (không còn broken link).
- Block 2: 0 matches (không còn badge Done sai cho Step 2.2).
- Block 3: empty output (file đã drop khỏi status, đồng bộ với HEAD state).

### Failure handling (historical — không còn relevant sau khi done)

| Symptom                                                                 | Action                                                                                                                     |
| ----------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| `git add` báo file missing                                              | Thử `git rm --cached docs/superpowers/specs/2026-04-19-step-2.2-mock-ui-design.md` — equivalent outcome (drop from index). |
| Sau Step 2.1 `git status` vẫn show file                                 | Chạy `git rm --cached <path>` rồi verify lại.                                                                              |
| Grep Step 2.3 vẫn còn `2026-04-19-step-2.2-mock-ui` mà không rõ context | Đọc file, nếu là context-documentation thì thay bằng prose; nếu là link thì xóa.                                           |
| Accidentally restored file                                              | `git rm docs/superpowers/specs/2026-04-19-step-2.2-mock-ui-design.md` để xóa lại.                                          |

### Execution Summary — 2026-04-21

| Sub-step                    | Command executed                                                                                                            | Result                                                                                                              |
| --------------------------- | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| 2.1 Drop spec khỏi index    | `git rm --cached docs/superpowers/specs/2026-04-19-step-2.2-mock-ui-design.md`                                              | ✅ `rm 'docs/...'`; `git status` cho path empty (clean)                                                             |
| 2.2 Fix badge `phase-2a:88` | Manual StrReplace: `✅ Done (...)` → `⬜ Not Started — ... thứ tự 2.0 → 2.3 → (2.2 pending) → 2.4 → 2.5`                    | ✅ Badge đúng, 2 link broken xóa                                                                                    |
| 2.3 Grep residue            | `rg "2026-04-19-step-2\.2-mock-ui" docs/`                                                                                   | 5 match còn lại đều nằm trong `reports/2026-04-21-*` và `plans/2026-04-21-*` (self-context, không phải broken link) |
| 2.4 Bảng step-progress      | Verify — `phase-2a-order-kafka.md` không có bảng tổng hợp dạng cell, badge inline đã đủ                                     | ✅ no-op                                                                                                            |
| 2.5 Final verify            | `rg` loại trừ self-context → **0 matches** trong canonical docs; `ls` spec/plan → `No such file`; `git status` path → empty | ✅ 3-layer clean                                                                                                    |

**Impact on findings:**

- F2 — ✅ CLOSED. Spec file đồng bộ với HEAD state (không còn `AD`).
- F15 — ✅ CLOSED. `phase-2a-order-kafka.md:88` phản ánh đúng Step 2.2 = Not Started.
- Executive Summary severity: 3 🔴 → **0 🔴** (F7 Keycloak đã cleanup 2026-04-21).

---

## Task 3 — Cleanup Keycloak Duplicate Roles (F7)

**Files:** None (runtime IAM operation).

**Goal:** Xóa 5 lowercase legacy roles trong realm `qrtable`. Chỉ giữ 6 uppercase custom roles + 3 built-ins.

**Impact analysis:**

- `Authorizer.validateRoleMapping` normalize casing before intersection → delete lowercase an toàn kể cả khi user Keycloak đang có lowercase role assignment
- Tuy nhiên **NẾU** có user Keycloak đang được assign lowercase role → sau khi xóa, user mất assignment → login sẽ fail `ROLE_MAPPING_MISMATCH`. Cần kiểm tra trước.

### Sub-steps

- [x] **Step 3.1 — List users có lowercase role assignment** (2026-04-21 — empty arrays)

```bash
docker exec qrtable-provider-keycloak-1 /opt/keycloak/bin/kcadm.sh config credentials --server http://localhost:8080 --realm master --user admin --password admin >/dev/null 2>&1

for role in owner manager waiter chef barista; do
  echo "=== Users with role: $role ==="
  docker exec qrtable-provider-keycloak-1 /opt/keycloak/bin/kcadm.sh get roles/$role/users -r qrtable 2>/dev/null --fields username
done
```

Expected: tất cả empty (không ai được assign lowercase). Nếu có → Step 3.2 bắt buộc.

- [x] **Step 3.2 — (Conditional) Reassign users to uppercase equivalent** (2026-04-21 — skipped, không user gắn lowercase)

Nếu có user attached:

```bash
# Example: user "waiter1@tenantA" đang có role "waiter" (lowercase)
docker exec qrtable-provider-keycloak-1 /opt/keycloak/bin/kcadm.sh add-roles -r qrtable \
  --uusername waiter1@tenantA --rolename WAITER
docker exec qrtable-provider-keycloak-1 /opt/keycloak/bin/kcadm.sh remove-roles -r qrtable \
  --uusername waiter1@tenantA --rolename waiter
```

Repeat cho mỗi user + role.

- [x] **Step 3.3 — Delete 5 lowercase legacy roles** (2026-04-21)

```bash
for role in owner manager waiter chef barista; do
  echo "Deleting role: $role"
  docker exec qrtable-provider-keycloak-1 /opt/keycloak/bin/kcadm.sh delete roles/$role -r qrtable
done
```

- [x] **Step 3.4 — Verify realm state** (2026-04-21 — 6+3 roles)

```bash
docker exec qrtable-provider-keycloak-1 /opt/keycloak/bin/kcadm.sh get roles -r qrtable --fields name | grep -vE '^\[|^\]|^}|^,$'
```

Expected output chỉ còn:

```
{ "name" : "WAITER" }
{ "name" : "MANAGER" }
{ "name" : "CHEF" }
{ "name" : "BARISTA" }
{ "name" : "SUPER_ADMIN" }
{ "name" : "OWNER" }
{ "name" : "uma_authorization" ... }
{ "name" : "offline_access" ... }
{ "name" : "default-roles-qrtable" ... }
```

6 custom + 3 built-ins = 9 roles total.

- [x] **Step 3.5 — Invalidate user-token cache (prevent stale permissions)** (2026-04-21 — `FLUSHDB`)

```bash
docker exec qrtable-provider-redis-1 redis-cli FLUSHDB
```

- [x] **Step 3.6 — Smoke test login** (2026-04-21 — `verify-permission-matrix.sh` cover 6 roles qua BFF)

Nếu có BFF+Authorizer chạy: login bằng 1 user uppercase và check `/authorizer/me` trả đúng permissions. Skip nếu stack chưa start.

---

## Task 4 — Sync `auth-system-reference.md` (F8)

**Files:** 1 modify (`docs/references/auth-system-reference.md`).

**Goal:** Đổi tiêu đề "5 Actors/Roles" → "6 Roles" ở 2 chỗ, body đã đúng 6 roles.

- [ ] **Step 4.1 — Update ToC (line 13)**

Replace:

```
3. [5 Actors/Roles Chính Của Hệ Thống](#3-5-actorsroles-chính-của-hệ-thống)
```

With:

```
3. [6 Roles Chính Của Hệ Thống](#3-6-roles-chính-của-hệ-thống)
```

- [ ] **Step 4.2 — Update H2 heading (line 139)**

Replace:

```
## 3. 5 Actors/Roles Chính Của Hệ Thống
```

With:

```
## 3. 6 Roles Chính Của Hệ Thống
```

- [ ] **Step 4.3 — Rename section 3.1 label (nếu có)**

Tìm `### 3.1 Bảng Tóm Lược 6 Roles (Bao gồm Super Admin)` — giữ nguyên, đã đúng.

- [ ] **Step 4.4 — Update `3.2 Actor Đặc Biệt: CUSTOMER` — đảm bảo có cross-ref tới permission-matrix.md §7**

Append (nếu chưa có) câu: `> Chi tiết CUSTOMER endpoint + SessionGuard scope xem [Permission Matrix §7](../architecture/permission-matrix.md#7-customer-actor-no-db-role)`

- [ ] **Step 4.5 — Verify**

```bash
rg -n "5 Actor|6 Role" docs/references/auth-system-reference.md
```

Expected: chỉ có "6 Role", không còn "5 Actor".

---

## Task 5 — Sync `business-logic.md` §9 (F5, F14)

**Files:** 1 modify (`docs/business-logic.md`).

**Goal:**

1. Remove sub-role "Inventory Staff (Thủ kho)" khỏi §9.A.3 (không có trong ROLE enum)
2. Reframe MANAGER: không mô tả như "sub-role của Restaurant Owner" mà là role độc lập ngang hàng với OWNER (operational vs ownership)
3. Thêm link trỏ tới canonical `permission-matrix.md §6` trước §9.B Permission Matrix
4. Note rằng bảng §9.B là "business-language summary"; bảng canonical 6×51 ở `permission-matrix.md`

- [ ] **Step 5.1 — Remove Inventory Staff bullet (line ~683)**

Delete line:

```
- **Inventory Staff (Thủ kho)**: Cập nhật tồn kho thực tế
```

Update intro line (line 679) nếu cần:

```
- Sub-roles:
  - Waiter/Server (Phục vụ): Xác nhận đơn, xử lý thanh toán, chuyển bàn
  - Chef (Bếp trưởng): Xem & cập nhật KDS món ăn
  - Barista/Bartender (Pha chế): Xem & cập nhật KDS đồ uống
```

- [ ] **Step 5.2 — Reframe MANAGER section (line ~653-672)**

Current text says "Restaurant Owner/Manager (Merchant Admin)" as single actor với "Owner" + "Manager" làm sub-roles. Replace with:

```markdown
#### **2. Restaurant Owner (Merchant Admin)**

**Phạm vi:** Tenant mà họ sở hữu

- **Vai trò:** Chủ nhà hàng — toàn quyền vận hành + HR (bao gồm xóa nhân viên)
- **Keycloak role:** `OWNER`
- **Permissions:** full operational (CRUD menu, tables, orders, payment, KDS) + `user.delete` (phân biệt duy nhất với MANAGER)

**Microservice tương ứng:** User-Access Service, Catalog Service, Order Service

---

#### **3. Manager (Operational Lead)**

**Phạm vi:** Tenant mà họ được phân công

- **Vai trò:** Quản lý vận hành ca làm việc — same as OWNER trừ `user.delete`
- **Keycloak role:** `MANAGER`
- **Khác với OWNER:** không được xóa user (HR action giữ cho OWNER)

**Microservice tương ứng:** Same as OWNER (User-Access, Catalog, Order)
```

Điều chỉnh các mục số tiếp theo (Staff sẽ thành #4, Customer thành #5).

- [ ] **Step 5.3 — Add canonical cross-ref trước §9.B**

Thêm trước bảng `### B. Permission Matrix (4 Actors Chính)`:

```markdown
### B. Permission Matrix (Business-Language Summary)

> **Canonical source:** Chi tiết đầy đủ 6 roles × 51 permissions xem [`docs/architecture/permission-matrix.md §6`](architecture/permission-matrix.md#6-canonical-permission-matrix-6--51). Bảng dưới đây là **tóm lược business-language** cho 5 actor groupings, KHÔNG phải source of truth cho RBAC guard check.
```

- [ ] **Step 5.4 — Verify**

```bash
rg -n "Inventory Staff|Thủ kho|Accountant" docs/business-logic.md
```

Expected: 0 matches.

```bash
rg -n "permission-matrix.md" docs/business-logic.md
```

Expected: ≥ 1 match (cross-ref đã add).

---

## Task 6 — Phase 2A Step 2.3 Badge + ADR Note (F1, F6)

**Files:** 1 modify (`docs/phases/phase-2a-order-kafka.md`).

**Goal:**

1. Thêm badge `✅ Done` (match Step 2.2 pattern)
2. Thêm ADR mini giải thích const-object vs TS enum — để reader không nghĩ là TS `enum`

- [ ] **Step 6.1 — Add `✅ Done` badge sau block Step 2.3 details**

Tìm line sau block `> **Spec & Plan reference:** [...](...)` (line ~135) và append:

```markdown
> **Status:** ✅ Done (2026-04-19, commit `5089d41`) — see [Step 2.3 spec](../superpowers/specs/2026-04-19-step-2.3-shared-types-design.md) and [plan](../superpowers/plans/2026-04-19-step-2.3-shared-types.md)
```

- [ ] **Step 6.2 — Add ADR footnote về const-object**

Thêm sau block code `Realtime/Event Types` (trước `State Machine Matrices` line ~122):

```markdown
> **ADR — Const-object pattern thay vì TS `enum`:** Tất cả 7 enums (`OrderStatus`, `OrderItemStatus`, `BillStatus`, `SessionStatus`, `ServiceRequestType`, `ServiceRequestStatus`, `PaymentMethod`) triển khai bằng `const object + type alias` (`export const X = { ... } as const; export type X = typeof X[keyof typeof X]`) — KHÔNG phải TS `enum`. Lý do: `erasableSyntaxOnly` trong `customer-pwa` tsconfig không chấp nhận TS `enum` (sinh runtime code). `class-validator` `@IsEnum(OrderStatus)` vẫn hoạt động với const-object từ v0.14+. Chi tiết: xem header comment trong `libs/shared/types/src/lib/order.types.ts`.
```

- [ ] **Step 6.3 — Verify**

```bash
rg -n "✅ Done.*2026-04-19.*5089d41|const.object pattern" docs/phases/phase-2a-order-kafka.md
```

Expected: 2 matches (badge + ADR).

---

## Task 7 — Const-Object ADR Footnote in Spec + Plan 2.3 (F9)

**Files:** 2 modify (spec + plan Step 2.3).

**Goal:** Add post-implementation note — thay vì refactor 20+ code blocks, thêm 1 footnote ở đầu section code examples giải thích "actual impl uses const-object, sample below shows TS `enum` for readability".

- [ ] **Step 7.1 — Add note to spec `§6 Detailed Design`**

File: `docs/superpowers/specs/2026-04-19-step-2.3-shared-types-design.md`

Sau heading section `## 6. Detailed Design`, thêm callout:

```markdown
> **Post-implementation note (2026-04-21):** Code snippets trong section này sử dụng cú pháp `export enum X {...}` cho dễ đọc. Implementation thực tế (commit `5089d41`) dùng **const-object + type alias pattern** (`export const X = {...} as const; export type X = typeof X[keyof typeof X]`) vì `erasableSyntaxOnly` trong customer-pwa tsconfig. Semantic tương đương, runtime footprint nhỏ hơn, TypeScript exhaustiveness check tương đương. Xem `libs/shared/types/src/lib/order.types.ts` header để có ADR đầy đủ.
```

- [ ] **Step 7.2 — Add tương tự vào plan**

File: `docs/superpowers/plans/2026-04-19-step-2.3-shared-types.md`

Sau heading `## Task 2: Refactor libs/shared/types/src/lib/order.types.ts`, thêm callout giống Step 7.1 nhưng rút gọn 1 dòng:

```markdown
> **Executed with const-object pattern instead of TS `enum`** (erasableSyntaxOnly constraint). Semantic equivalent. See [audit report §4.2](../reports/2026-04-21-step-2.0-2.3-sync-audit.md#42-const-object-vs-ts-enum--adr-trace).
```

- [ ] **Step 7.3 — Verify**

```bash
rg -n "erasableSyntaxOnly|Post-implementation note" docs/superpowers/specs/2026-04-19-step-2.3-shared-types-design.md docs/superpowers/plans/2026-04-19-step-2.3-shared-types.md
```

Expected: ≥ 2 matches.

---

## Task 8 — `business-logic.md` §8 UPPERCASE Note + `Draft → Canceled` (F10, F11)

**Files:** 1 modify (`docs/business-logic.md`).

**Goal:**

1. Thêm note rằng labels trong §8 là display-friendly (Title Case), enum values thực tế là UPPERCASE
2. Thêm rule `Draft → Canceled` cho customer abandon cart case

- [ ] **Step 8.1 — Thêm note trước Order State Diagram (line ~516)**

Thêm trước block diagram:

```markdown
> **Enum casing convention:** Diagram + rules dưới đây dùng **Title Case** (`Draft`, `Pending`, `Processing`, `Ready`, `Served`, `Completed`, `Canceled`) cho readability. Enum values canonical là **UPPERCASE** (`DRAFT`, `PENDING`, ...) — xem `libs/shared/types/src/lib/order.types.ts` và `docs/phases/phase-2a-order-kafka.md` Step 2.3. 1-1 mapping (`Draft` ↔ `DRAFT`, etc.).
```

- [ ] **Step 8.2 — Add `Draft → Canceled` rule trong §8.B (sau `Draft → Pending` block, line ~562)**

Thêm:

```yaml
Draft → Canceled:
  Trigger: Customer đóng trình duyệt / bỏ cart / explicit clear
  Actor: Customer (self, implicit)
  Condition:
    - cart chưa submit (order chưa tồn tại như record)
  Action:
    - Release Redis cart key (TTL expiry hoặc explicit DEL)
    - KHÔNG tạo order record (nothing to cancel formally)
  Note: Transition này KHÔNG persist vì Draft chưa tạo DB row; code-level check trong ALLOWED_ORDER_TRANSITIONS cho FE disable "Submit" + cho BE reject replay nếu cart đã clear.
```

- [ ] **Step 8.3 — Verify**

```bash
rg -n "DRAFT|Draft → Canceled|UPPERCASE" docs/business-logic.md
```

Expected: ≥ 3 matches.

---

## Task 9 — Review Modified Phase Docs (F13)

**Files:** 3 inspect (`phase-3-payment.md`, `phase-4a-saga-hardening.md`, `erd_explanation.md`).

**Goal:** Verify các file đang modified (chưa commit) tham chiếu đúng Step 2.3 types và không tạo drift mới.

- [ ] **Step 9.1 — Diff check**

```bash
git diff docs/phases/phase-3-payment.md | head -100
git diff docs/phases/phase-4a-saga-hardening.md | head -100
git diff docs/architecture/erd_explanation.md | head -100
```

- [ ] **Step 9.2 — Checklist cho mỗi file**

Mỗi file phải:

- [ ] Sử dụng UPPERCASE cho enum values (OrderStatus.CANCELED không phải "Canceled")
- [ ] Không còn reference tới `PaymentStatus` (đã loại bỏ, merged vào BillStatus)
- [ ] Không reference `ACCOUNTANT/ADMINISTRATOR` role
- [ ] Bills nằm Order Service (không Payment Service)
- [ ] `order.confirmed` là Kafka topic duy nhất trong Phase 2A; `order.created`/`service.requested` đi BFF Direct
- [ ] `PaymentMethod` chỉ `CASH` cho Phase 2A; Phase 3 extension riêng

- [ ] **Step 9.3 — Fix bất kỳ drift nào phát hiện**

Nếu diff có content sai → sửa từng file, verify bằng grep:

```bash
rg -n "Accountant|Administrator|PaymentStatus|[Bb]ills.*Payment.Service" docs/phases/phase-3-payment.md docs/phases/phase-4a-saga-hardening.md docs/architecture/erd_explanation.md
```

Expected: 0 matches (trừ historical "Phase 3 extends PaymentMethod" kiểu forward reference, acceptable).

- [ ] **Step 9.4 — Note cho owner**

Nếu 3 file đã đúng → có thể batch commit cùng các fix từ tasks 2-8 trong plan này.

---

## Task 10 — Final Acceptance Verification (F12)

**Files:** None (operational — run verify scripts).

**Goal:** Chạy cả 2 layer verification + checklist đóng Step 2.0 + 2.3.

- [x] **Step 10.1 — Run Layer 1 unit tests cho Step 2.3** (2026-04-21 — PASS)

```bash
npx nx test shared-types --skip-nx-cache 2>&1 | tail -20
```

Expected: `Tests: X passed, X total` — cả `enum-completeness.spec.ts` và `transitions.spec.ts` run và PASS.

Nếu "No tests found": check `libs/shared/types/jest.config.ts` hoặc `project.json` `testMatch`/`testPathIgnorePatterns`. Fix config để jest discover `__tests__/**`.

- [x] **Step 10.2 — Run Layer 2 unit test cho Step 2.0** (2026-04-21 — `user-access` PASS)

```bash
npx nx test user-access --skip-nx-cache 2>&1 | tail -20
```

Expected: role.json matrix validation PASS.

- [x] **Step 10.3 — Run Layer 1 unit test cho PermissionGuard** (2026-04-21 — `bff` test suites PASS)

```bash
npx nx test bff --skip-nx-cache 2>&1 | grep -E "Tests:|permission.guard" | head -10
```

Expected: `permission.guard.spec.ts` PASS.

- [x] **Step 10.4 — Run Layer 3 integration script (cần stack BFF+Authorizer chạy)** (2026-04-21 — PASS)

```bash
# Terminal 1 (background): pnpm dev:bff-auth
bash tools/verify-permission-matrix.sh 2>&1 | tail -30
```

Expected: tất cả 6 roles login + `/authorizer/me` trả đúng permissions theo canonical matrix.

Nếu stack chưa chạy → skip và đánh dấu "manual verify khi start Step 2.4 stack".

- [x] **Step 10.5 — Re-query MongoDB + Keycloak state** (2026-04-21)

```bash
docker exec qrtable-provider-mongodb-1 mongosh "mongodb://root:password@localhost:27017/qrtable?authSource=admin" --quiet --eval '
print("Roles: " + JSON.stringify(db.role.find({},{name:1}).toArray().map(r=>r.name)));
print("Role counts: " + db.role.countDocuments({}));
print("User counts: " + db.user.countDocuments({}));
'

docker exec qrtable-provider-keycloak-1 /opt/keycloak/bin/kcadm.sh get roles -r qrtable --fields name 2>/dev/null | grep '"name"' | wc -l
```

Expected:

- 6 roles, 7 users in Mongo
- 9 lines (6 custom uppercase + 3 built-ins) from Keycloak

- [x] **Step 10.6 — Update audit report với status CLOSED** (2026-04-21)

Edit `docs/superpowers/reports/2026-04-21-step-2.0-2.3-sync-audit.md`:

- §1 Executive Summary: đổi `🟡 Active` → `✅ Closed (YYYY-MM-DD)`
- §6 Consolidated Findings Table: mark F1-F11 `✅ Closed`
- §10 Changelog: add row "2026-MM-DD | <executor> | Executed fix plan; all 12 findings resolved"

- [ ] **Step 10.7 — Final checklist (15 findings)**

- [x] **F2** ✅ (2026-04-21) — Spec Step 2.2 đã drop khỏi `git status` (via `git rm --cached`) + 0 broken link `2026-04-19-step-2.2-mock-ui` trong canonical docs
- [x] **F15** ✅ (2026-04-21) — `phase-2a-order-kafka.md:88` badge `✅ Done` → `⬜ Not Started` cho Step 2.2 + prose thứ tự `2.0 → 2.3 → (2.2 pending) → 2.4 → 2.5`
- [x] F7 — Keycloak realm `qrtable` có đúng 6 uppercase + 3 built-ins (không còn `owner/manager/waiter/chef/barista` lowercase) ✅ 2026-04-21
- [x] F8 — `auth-system-reference.md` ToC + H2 "6 Roles" + cross-ref CUSTOMER ✅ 2026-04-21
- [x] F5, F14 — `business-logic.md` §9 synced ✅ 2026-04-21
- [x] F1, F6 — `phase-2a-order-kafka.md` Step 2.3 badge + ADR ✅ 2026-04-21
- [x] F9 — Spec + plan Step 2.3 post-implementation note ✅ 2026-04-21
- [x] F10, F11 — `business-logic.md` §8 ✅ 2026-04-21
- [x] F13 — `git diff` sạch 3 file ✅ 2026-04-21
- [x] F12 — nx tests + `verify-permission-matrix.sh` PASS ✅ 2026-04-21
- [x] MongoDB: 6 roles + 7 users; Keycloak: 9 roles sau cleanup ✅ 2026-04-21
- [x] Audit report §1 + §6 + §8 + §10 updated ✅ Closed 2026-04-21

---

## Rollback Strategy

Nếu task bất kỳ fail catastrophically:

| Task                     | Rollback                                                                                                                                                                                                                 |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Task 2 (Record revert)   | Muốn khôi phục staged state cũ: `git reset docs/superpowers/specs/2026-04-19-step-2.2-mock-ui-design.md` (đưa về state `??` hoặc `AD` tuỳ trước đó); edit doc dùng `git checkout -- docs/phases/phase-2a-order-kafka.md` |
| Task 3 (Keycloak delete) | Re-create role: `kcadm.sh create roles -r qrtable -s name=<rolename>` (permissions cache may need FLUSHDB)                                                                                                               |
| Tasks 4-9 (Doc edits)    | `git checkout -- <file>` trước khi edit; hoặc `git stash` toàn bộ doc changes                                                                                                                                            |

Không task nào chạm production code (`apps/**`, `libs/**/src/**`) → rollback an toàn.

---

## Estimated Timeline

**Progress:** Toàn bộ 10 tasks hoàn tất 2026-04-21 (Keycloak + docs + verify).

| Scenario                                | Total remaining |
| --------------------------------------- | --------------- |
| All remaining tasks smooth              | ~1h15m          |
| + Keycloak có users attached (Task 3.2) | +15 min         |
| + F13 phát sinh drift cần fix           | +30 min         |
| + Layer 3 integration debug             | +30 min         |

---

## Execution Order Notes

- **Tasks 1-3** nên chạy tuần tự (pre-flight → restore → IAM)
- **Tasks 4-9** có thể parallel (pure doc edits, độc lập file)
- **Task 10** phải cuối cùng (verify PAS tất cả)

---

## Changelog

| Date       | Author                  | Change                                                                                                                                                                                                                                  |
| ---------- | ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-04-21 | Superpowers audit agent | Initial fix plan — 10 tasks, ~1.5–2h, map 14 findings                                                                                                                                                                                   |
| 2026-04-21 | Superpowers audit agent | Rev 2 — Owner xác nhận Step 2.2 revert; thêm §0 Context & Orientation (audience = owner + agent); Task 2 rewritten hoàn toàn (record revert thay vì restore, merge F15); Task 1.3 + Task 10.7 cập nhật theo; 15 findings tổng           |
| 2026-04-21 | Superpowers audit agent | Rev 3 — Task 2 executed: `git rm --cached` spec + fix `phase-2a:88` badge. Sub-steps 2.1–2.5 đánh dấu `[x]` kèm Execution Summary; Task Map thêm cột Status; Task 1.3 + §10.7 checklist sync; F2 + F15 đã CLOSED; 4/15 done, 11 pending |
| 2026-04-21 | Cursor agent            | Rev 4 — Tasks 1,3–10 executed: Keycloak cleanup + Redis FLUSHDB; doc sync Tasks 4–9; nx + verify script; audit CLOSED; plan Task Map + checklists synced **15/15 findings**                                                             |
