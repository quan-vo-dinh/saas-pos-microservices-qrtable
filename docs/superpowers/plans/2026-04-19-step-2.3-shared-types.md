# Step 2.3 — Shared Types & Realtime Contract Implementation Plan

> **For agentic workers:** Steps use checkbox (`- [ ]`) syntax for tracking. Execute task-by-task, verify after each step. **No `git commit` steps included** (per project owner's instruction — commits handled separately).

**Goal:** Refactor `libs/shared/types/` để trở thành single source of truth cho FE+BE về domain Order. Define 6 entities + 6 enums + 5 event/data types + 3 transition matrices. Update mock data + constants + 6 documentation files. Verify 3-layer (unit + cross-tsc + mock conformance).

**Architecture:** TypeScript types-first approach — refactor types lib + cascade dependent libs (mock-data, constants). Customer-pwa sẽ tạm break TypeScript (8 files documented), accepted scope-wise vì sẽ fix ở Step 2.2.

**Tech Stack:** TypeScript 5.x, Jest 30, Nx monorepo, pnpm.

**Spec reference:** [`docs/superpowers/specs/2026-04-19-step-2.3-shared-types-design.md`](../specs/2026-04-19-step-2.3-shared-types-design.md) — đọc spec TRƯỚC khi execute plan này.

---

## Pre-flight Reading (REQUIRED)

Trước khi bắt đầu, agent PHẢI đọc các file sau để có context đầy đủ:

1. **Spec file:** `docs/superpowers/specs/2026-04-19-step-2.3-shared-types-design.md` (toàn bộ — đặc biệt §4 Decisions Log, §6 Detailed Design, §7 Migration Impact)
2. **Phase doc:** `docs/phases/phase-2a-order-kafka.md` (đặc biệt Step 2.3 lines 90-119 — sẽ được update theo Task 19)
3. **Existing types lib:**
   - `libs/shared/types/src/lib/order.types.ts` (sẽ refactor)
   - `libs/shared/types/src/index.ts` (barrel — sẽ update)
4. **Existing mock-data lib:**
   - `libs/shared/mock-data/src/lib/orders.ts` (sẽ refactor)
   - `libs/shared/mock-data/src/lib/sessions.ts` (sẽ refactor)
   - `libs/shared/mock-data/src/lib/helpers.ts` (sẽ extend)
   - `libs/shared/mock-data/src/index.ts` (sẽ update barrel)
5. **Existing constants lib:**
   - `libs/shared/constants/src/lib/status.ts` (sẽ refactor)
6. **Existing test patterns** (reference cho writing new tests):
   - `libs/shared/types/src/lib/__tests__/` (nếu chưa có folder, tạo mới)
7. **Existing FE consumers** (cần verify TS errors sau refactor):
   - `apps/customer-pwa/src/features/order/components/order-summary-card.tsx`
   - `apps/customer-pwa/src/features/order/components/order-status-timeline.tsx`
   - `apps/customer-pwa/src/features/order/components/order-items-list.tsx`
   - `apps/customer-pwa/src/features/order/services/order.service.ts`
   - `apps/customer-pwa/src/features/order/hooks/use-order-query.ts`
   - `apps/customer-pwa/src/features/payment/components/payment-method-selector.tsx`
   - `apps/customer-pwa/src/features/payment/components/payment-summary-card.tsx`
   - `apps/customer-pwa/src/pages/request-payment-page.tsx`

---

## Task Map (24 tasks, ~0.5-1 day total)

| #   | Task                                                                                                   | Files               | Est. time |
| --- | ------------------------------------------------------------------------------------------------------ | ------------------- | --------- |
| 1   | Pre-flight: verify project state + create backup branch                                                | None (read-only)    | 5 min     |
| 2   | Refactor `order.types.ts` (entities + enums + transitions)                                             | 1 modify            | 15 min    |
| 3   | Create `bill.types.ts`                                                                                 | 1 create            | 10 min    |
| 4   | Create `session.types.ts` (Session + CartItem)                                                         | 1 create            | 10 min    |
| 5   | Create `service-request.types.ts`                                                                      | 1 create            | 10 min    |
| 6   | Create `realtime-events.types.ts` (5 event types)                                                      | 1 create            | 15 min    |
| 7   | Update `index.ts` barrel + delete empty `types.ts`                                                     | 1 modify, 1 delete  | 5 min     |
| 8   | Verify types lib compiles (intermediate checkpoint)                                                    | None (operational)  | 3 min     |
| 9   | Refactor `constants/lib/status.ts`                                                                     | 1 modify            | 10 min    |
| 10  | Refactor mock-data `orders.ts`                                                                         | 1 modify            | 15 min    |
| 11  | Refactor mock-data `sessions.ts`                                                                       | 1 modify            | 10 min    |
| 12  | Create mock-data `bills.ts`                                                                            | 1 create            | 10 min    |
| 13  | Create mock-data `carts.ts`                                                                            | 1 create            | 5 min     |
| 14  | Create mock-data `service-requests.ts`                                                                 | 1 create            | 10 min    |
| 15  | Update mock-data `helpers.ts` + `index.ts`                                                             | 2 modify            | 10 min    |
| 16  | Verify mock-data lib compiles (intermediate checkpoint)                                                | None (operational)  | 3 min     |
| 17  | Write Layer 1 — `enum-completeness.spec.ts`                                                            | 1 create            | 20 min    |
| 18  | Write Layer 1 — `transitions.spec.ts`                                                                  | 1 create            | 25 min    |
| 19  | Write Layer 3 — `data-conformance.spec.ts`                                                             | 1 create            | 20 min    |
| 20  | Write Layer 2 — `verify-step-2.3-types.sh` script                                                      | 1 create            | 15 min    |
| 21  | Run all 3-layer verifications (full integration)                                                       | None (operational)  | 10 min    |
| 22  | Sync `phase-2a-order-kafka.md` (Step 2.2 + Step 2.3 sections)                                          | 1 modify            | 15 min    |
| 23  | Sync `phase-4a-saga-hardening.md` + `phase-3-payment.md` + `phase-1-catalog.md` + `erd_explanation.md` | 4 modify            | 15 min    |
| 24  | Final acceptance criteria verification                                                                 | Run all tests + tsc | 15 min    |

---

## Task 1: Pre-flight Verification

**Files:** None (read-only verification)

**Goal:** Đảm bảo workspace clean, dependencies installed, baseline tsc state OK trước khi modify.

- [ ] **Step 1: Verify git status clean**

```bash
cd /Users/vodinhquan/Developer/Graduation-Thesis/graduation-thesis/qr-order
git status
```

Expected: Working tree clean hoặc only docs/specs/plans pending (chấp nhận được nếu chỉ là docs cũ chưa commit).

Nếu có code changes pending → stash hoặc commit trước khi bắt đầu Step 2.3.

- [ ] **Step 2: Create backup branch (safety net)**

```bash
git checkout -b backup/pre-step-2.3-$(date +%s)
git checkout -  # quay về branch hiện tại
```

Branch backup giữ as snapshot — không cần checkout lại trừ khi cần rollback.

- [ ] **Step 3: Verify dependencies installed**

```bash
pnpm install --frozen-lockfile
```

Expected: `Done in <X>s`. Nếu fail → check Node version (>= 18), pnpm version (>= 8).

- [ ] **Step 4: Baseline tsc — verify all projects compile clean BEFORE refactor**

```bash
npx nx run-many --target=build --projects=types,mock-data,constants 2>&1 | tail -20
```

Expected: 3 builds PASS.

```bash
npx nx build customer-pwa 2>&1 | tail -10
```

Expected: PASS (baseline — chưa refactor).

```bash
npx nx build management-app 2>&1 | tail -10
```

Expected: PASS.

> **Lý do verify baseline:** Sau Task 8, customer-pwa sẽ FAIL — cần biết baseline đang PASS để confirm regression đến từ Step 2.3 chứ không phải pre-existing issue.

---

## Task 2: Refactor `libs/shared/types/src/lib/order.types.ts`

**Files:** 1 modify (`libs/shared/types/src/lib/order.types.ts`)

**Goal:** Replace existing string-union types với new shape: TS enums UPPERCASE + entity types với audit/denormalization fields + state transition matrix.

- [ ] **Step 1: Open file and replace entire content**

File: `libs/shared/types/src/lib/order.types.ts`

Replace toàn bộ nội dung hiện tại với:

```typescript
/**
 * Order domain types — Phase 2A QRTable.
 * Single source of truth cho FE+BE về Order aggregate.
 *
 * @see docs/phases/phase-2a-order-kafka.md §8 (state machine)
 * @see docs/superpowers/specs/2026-04-19-step-2.3-shared-types-design.md
 */

// ─── Enums ──────────────────────────────────────────

export enum OrderStatus {
  /** Cart chưa submit (status implicit cho cart aggregate) */
  DRAFT = 'DRAFT',
  /** Submitted, chờ staff confirm */
  PENDING = 'PENDING',
  /** Confirmed, bếp đang làm */
  PROCESSING = 'PROCESSING',
  /** Bếp xong, chờ phục vụ bàn */
  READY = 'READY',
  /** Đã đem ra bàn */
  SERVED = 'SERVED',
  /** Bill paid (terminal happy path) */
  COMPLETED = 'COMPLETED',
  /** Huỷ (terminal failure path) */
  CANCELED = 'CANCELED',
}

export enum OrderItemStatus {
  /** Bếp/bar đang làm */
  PROCESSING = 'PROCESSING',
  /** Xong, chờ phục vụ */
  READY = 'READY',
  /** Đã đem ra bàn */
  SERVED = 'SERVED',
  /** Item bị huỷ (vd: hết stock đột xuất) */
  CANCELED = 'CANCELED',
}

// ─── Entity types ───────────────────────────────────

export type OrderItem = {
  id: string;
  orderId: string;
  menuItemId: string;
  /** Denormalized for display — avoids JOIN in POS/KDS rendering */
  menuItemName: string;
  quantity: number;
  /** VND integer (no decimals) */
  unitPrice: number;
  note?: string;
  status: OrderItemStatus;
  /** ISO 8601 */
  createdAt: string;
  /** ISO 8601 */
  updatedAt: string;
};

export type Order = {
  id: string;
  tenantId: string;
  tableId: string;
  /** Denormalized for display */
  tableName: string;
  sessionId: string;
  items: OrderItem[];
  status: OrderStatus;
  /** VND integer = sum(items.unitPrice * quantity) */
  totalAmount: number;
  /** UUID v4 generated by FE for dedup at submit */
  idempotencyKey: string;
  /** Customer notes (vd: "không cay") */
  notes?: string;
  /** Audit — set khi PENDING → PROCESSING */
  confirmedAt?: string;
  /** Audit — staff Keycloak sub */
  confirmedByUserId?: string;
  /** Audit — set khi → CANCELED */
  cancelledAt?: string;
  /** Audit — staff Keycloak sub hoặc null nếu customer cancel */
  cancelledByUserId?: string;
  /** Optional cancellation reason */
  cancelReason?: string;
  /** ISO 8601 */
  createdAt: string;
  /** ISO 8601 */
  updatedAt: string;
};

// ─── State transition matrix ────────────────────────

/**
 * Allowed Order state transitions per Phase 2A spec §8.
 *
 * Used by:
 *   - BE Order Service (Step 2.4): validate transition trước khi update DB
 *   - FE POS (Step 2.5): disable invalid action buttons
 *
 * @example
 *   if (!ALLOWED_ORDER_TRANSITIONS[currentStatus].includes(targetStatus)) {
 *     throw new InvalidOrderTransitionException(currentStatus, targetStatus);
 *   }
 */
export const ALLOWED_ORDER_TRANSITIONS: Record<OrderStatus, readonly OrderStatus[]> = {
  [OrderStatus.DRAFT]: [OrderStatus.PENDING, OrderStatus.CANCELED],
  [OrderStatus.PENDING]: [OrderStatus.PROCESSING, OrderStatus.CANCELED],
  [OrderStatus.PROCESSING]: [OrderStatus.READY, OrderStatus.CANCELED],
  [OrderStatus.READY]: [OrderStatus.SERVED],
  [OrderStatus.SERVED]: [OrderStatus.COMPLETED],
  [OrderStatus.COMPLETED]: [],
  [OrderStatus.CANCELED]: [],
} as const;
```

> **Removed exports**: `PaymentMethod`, `PaymentStatus` (move to `bill.types.ts`).

- [ ] **Step 2: Verify file syntax**

```bash
npx tsc --noEmit --target es2020 --module esnext --moduleResolution node libs/shared/types/src/lib/order.types.ts 2>&1 | head -10
```

Expected: 0 errors. Nếu có error → re-read step 1, copy lại toàn bộ content.

---

## Task 3: Create `libs/shared/types/src/lib/bill.types.ts`

**Files:** 1 create (`libs/shared/types/src/lib/bill.types.ts`)

**Goal:** Define Bill aggregate + BillStatus + PaymentMethod + ALLOWED_BILL_TRANSITIONS.

- [ ] **Step 1: Create file**

```bash
touch libs/shared/types/src/lib/bill.types.ts
```

- [ ] **Step 2: Write content**

File: `libs/shared/types/src/lib/bill.types.ts`

```typescript
/**
 * Bill domain types — Phase 2A QRTable.
 * Bill aggregate roll-up từ nhiều Orders trong cùng Session.
 *
 * Phase 3 sẽ EXTEND file này (thêm paidAmount, change, refund references).
 * Phase 3 sẽ EXTEND PaymentMethod enum (thêm CARD, MOMO, ZALOPAY, BANK_TRANSFER).
 *
 * @see docs/superpowers/specs/2026-04-19-step-2.3-shared-types-design.md
 */

// ─── Enums ──────────────────────────────────────────

export enum BillStatus {
  /** Session active, accepting orders */
  OPEN = 'OPEN',
  /** Customer triggered REQUEST_BILL, chờ staff process payment */
  PENDING_PAYMENT = 'PENDING_PAYMENT',
  /** Payment confirmed (Phase 3 sẽ trigger transition này) */
  PAID = 'PAID',
}

export enum PaymentMethod {
  CASH = 'CASH',
  // Phase 3 sẽ thêm:
  // CARD = 'CARD',
  // MOMO = 'MOMO',
  // ZALOPAY = 'ZALOPAY',
  // BANK_TRANSFER = 'BANK_TRANSFER',
}

// ─── Entity types ───────────────────────────────────

export type Bill = {
  id: string;
  tenantId: string;
  sessionId: string;
  /** Aggregate references — bill tổng hợp từ nhiều orders trong session */
  orderIds: string[];
  /** VND integer */
  subtotal: number;
  /** VND integer = subtotal + roundingAmount */
  total: number;
  /** VND integer (positive or negative) — VND rounding rule */
  roundingAmount: number;
  /** Set khi staff process payment (Phase 3) */
  paymentMethod?: PaymentMethod;
  status: BillStatus;
  /** Audit — set khi OPEN → PENDING_PAYMENT */
  closedAt?: string;
  /** Audit — set khi PENDING_PAYMENT → PAID (Phase 3) */
  paidAt?: string;
  /** ISO 8601 */
  createdAt: string;
  /** ISO 8601 */
  updatedAt: string;
};

// ─── State transition matrix ────────────────────────

/**
 * Allowed Bill state transitions per Phase 2A spec.
 *
 * PENDING_PAYMENT → OPEN allowed: customer triggered REQUEST_BILL rồi đổi ý
 * (vd muốn order thêm món) — staff revert bill về OPEN.
 */
export const ALLOWED_BILL_TRANSITIONS: Record<BillStatus, readonly BillStatus[]> = {
  [BillStatus.OPEN]: [BillStatus.PENDING_PAYMENT],
  [BillStatus.PENDING_PAYMENT]: [BillStatus.PAID, BillStatus.OPEN],
  [BillStatus.PAID]: [],
} as const;
```

- [ ] **Step 3: Verify file syntax**

```bash
npx tsc --noEmit --target es2020 --module esnext --moduleResolution node libs/shared/types/src/lib/bill.types.ts 2>&1 | head -10
```

Expected: 0 errors.

---

## Task 4: Create `libs/shared/types/src/lib/session.types.ts`

**Files:** 1 create (`libs/shared/types/src/lib/session.types.ts`)

**Goal:** Define Session entity + CartItem (cùng file vì cart chỉ tồn tại trong session scope) + SessionStatus.

- [ ] **Step 1: Create + write file**

File: `libs/shared/types/src/lib/session.types.ts`

```typescript
/**
 * Session + Cart domain types — Phase 2A QRTable.
 *
 * Session scope: customer scan QR → session bắt đầu, accumulates orders + cart.
 * Cart scope: per-session ephemeral state (Redis Hash với optimistic version).
 *
 * @see docs/phases/phase-2a-order-kafka.md §8 (auto-close rules)
 * @see docs/superpowers/specs/2026-04-19-step-2.3-shared-types-design.md
 */

// ─── Enums ──────────────────────────────────────────

export enum SessionStatus {
  ACTIVE = 'ACTIVE',
  CLOSED = 'CLOSED',
  // Note: IDLE là internal Redis TTL state, KHÔNG expose enum.
  // Auto-close logic: nếu lastActivity > 30 min AND orderCount == 0 → CLOSED.
}

// ─── Entity types ───────────────────────────────────

export type Session = {
  /** sessionId (UUID generated khi customer scan QR) */
  id: string;
  tenantId: string;
  tableId: string;
  /** Denormalized for display */
  tableName: string;
  status: SessionStatus;
  /** ISO 8601 — khi customer scan QR */
  startedAt: string;
  /** ISO 8601 — refresh mỗi action customer/staff trong session */
  lastActivity: string;
  /** Audit — set khi → CLOSED */
  closedAt?: string;
  /** Denormalized — tránh JOIN khi check auto-close rule (orderCount == 0?) */
  orderCount: number;
};

export type CartItem = {
  menuItemId: string;
  /** Denormalized for display */
  menuItemName: string;
  quantity: number;
  /** VND integer */
  unitPrice: number;
  note?: string;
  /**
   * Optimistic lock version for Redis Hash conflicts.
   * Incrementing integer; client phải send matching version trước khi update.
   * Conflict → BE trả lỗi → client refresh + retry.
   */
  version: number;
};
```

- [ ] **Step 2: Verify**

```bash
npx tsc --noEmit --target es2020 --module esnext --moduleResolution node libs/shared/types/src/lib/session.types.ts 2>&1 | head -10
```

Expected: 0 errors.

---

## Task 5: Create `libs/shared/types/src/lib/service-request.types.ts`

**Files:** 1 create (`libs/shared/types/src/lib/service-request.types.ts`)

**Goal:** Define ServiceRequest entity + ServiceRequestType + ServiceRequestStatus + ALLOWED_SERVICE_REQUEST_TRANSITIONS.

- [ ] **Step 1: Create + write file**

File: `libs/shared/types/src/lib/service-request.types.ts`

```typescript
/**
 * ServiceRequest domain types — Phase 2A QRTable.
 * Customer-initiated requests phục vụ (call staff, request bill, general help).
 *
 * @see docs/phases/phase-2a-order-kafka.md §8 (state flow)
 * @see docs/superpowers/specs/2026-04-19-step-2.3-shared-types-design.md
 */

// ─── Enums ──────────────────────────────────────────

export enum ServiceRequestType {
  /** Gọi nhân viên đến bàn */
  CALL_STAFF = 'CALL_STAFF',
  /** Yêu cầu thanh toán — trigger Bill OPEN → PENDING_PAYMENT */
  REQUEST_BILL = 'REQUEST_BILL',
  /** Hỗ trợ chung (vd: hỏi wifi, đổi bàn, etc.) */
  GENERAL_HELP = 'GENERAL_HELP',
}

export enum ServiceRequestStatus {
  /** Mới tạo, chưa có staff xác nhận */
  PENDING = 'PENDING',
  /** Staff đã xác nhận thấy notification */
  ACKNOWLEDGED = 'ACKNOWLEDGED',
  /** Staff đã xử lý xong */
  RESOLVED = 'RESOLVED',
}

// ─── Entity types ───────────────────────────────────

export type ServiceRequest = {
  id: string;
  tenantId: string;
  tableId: string;
  sessionId: string;
  type: ServiceRequestType;
  status: ServiceRequestStatus;
  /** Customer message (vd: "thêm 1 ly nước") */
  note?: string;
  /** Audit — set khi PENDING → ACKNOWLEDGED */
  acknowledgedAt?: string;
  /** Audit — staff Keycloak sub */
  acknowledgedByUserId?: string;
  /** Audit — set khi ACKNOWLEDGED → RESOLVED */
  resolvedAt?: string;
  /** ISO 8601 */
  createdAt: string;
  /** ISO 8601 */
  updatedAt: string;
};

// ─── State transition matrix ────────────────────────

/**
 * Allowed ServiceRequest state transitions per Phase 2A spec §8.
 */
export const ALLOWED_SERVICE_REQUEST_TRANSITIONS: Record<ServiceRequestStatus, readonly ServiceRequestStatus[]> = {
  [ServiceRequestStatus.PENDING]: [ServiceRequestStatus.ACKNOWLEDGED],
  [ServiceRequestStatus.ACKNOWLEDGED]: [ServiceRequestStatus.RESOLVED],
  [ServiceRequestStatus.RESOLVED]: [],
} as const;
```

- [ ] **Step 2: Verify**

```bash
npx tsc --noEmit --target es2020 --module esnext --moduleResolution node libs/shared/types/src/lib/service-request.types.ts 2>&1 | head -10
```

Expected: 0 errors.

---

## Task 6: Create `libs/shared/types/src/lib/realtime-events.types.ts`

**Files:** 1 create (`libs/shared/types/src/lib/realtime-events.types.ts`)

**Goal:** Define 5 event/data types — 3 BFF Direct WebSocket events + 1 Kafka payload + 1 derived data shape.

- [ ] **Step 1: Create + write file**

File: `libs/shared/types/src/lib/realtime-events.types.ts`

```typescript
/**
 * Realtime event payloads — Phase 2A QRTable.
 * Includes BFF Direct WebSocket events + Kafka topic payload + derived data shapes.
 *
 * BFF Direct (4P+2AP — Anti-Pattern 1, no Kafka):
 *   - OrderCreatedEvent       → emit khi customer submit order thành công
 *   - OrderStatusChangedEvent → emit khi order status thay đổi
 *   - ServiceRequestedEvent   → emit khi customer trigger service request
 *
 * Kafka (4 Producers — domain events post-confirmation):
 *   - OrderConfirmedEvent     → topic `order.confirmed` cho cross-service consumers
 *
 * Data shapes (not events themselves):
 *   - KDSTicket               → derived view cho UI render kitchen/bar tickets
 *
 * @see docs/guides/kafka-qrtable.md (4P+2AP rationale)
 * @see docs/superpowers/specs/2026-04-19-step-2.3-shared-types-design.md
 */

import type { OrderItem } from './order.types';
import { OrderStatus } from './order.types';
import { ServiceRequestType } from './service-request.types';

// Re-import OrderStatus as type for usage in event payloads (workaround for forward ref)
type OrderStatusType = OrderStatus;

// ─── BFF Direct WebSocket events ────────────────────

/**
 * BFF Direct → WebSocket emit khi customer submit order thành công.
 * Channel: per-tenant POS dashboard subscription.
 */
export type OrderCreatedEvent = {
  tenantId: string;
  orderId: string;
  tableId: string;
  tableName: string;
  sessionId: string;
  items: OrderItem[];
  totalAmount: number;
  /** ISO 8601 — emit time */
  timestamp: string;
};

/**
 * BFF Direct → WebSocket emit khi order status thay đổi (confirm/cancel/etc).
 * Channels:
 *   - per-session subscription (customer tracking page)
 *   - per-tenant POS dashboard (staff live view)
 */
export type OrderStatusChangedEvent = {
  tenantId: string;
  orderId: string;
  fromStatus: OrderStatusType;
  toStatus: OrderStatusType;
  /** Staff Keycloak sub, undefined nếu customer hành động (vd: customer cancel DRAFT) */
  changedByUserId?: string;
  timestamp: string;
};

/**
 * BFF Direct → WebSocket emit khi customer trigger service request.
 * Channel: per-tenant POS dashboard.
 */
export type ServiceRequestedEvent = {
  tenantId: string;
  requestId: string;
  tableId: string;
  tableName: string;
  sessionId: string;
  type: ServiceRequestType;
  note?: string;
  timestamp: string;
};

// ─── Kafka topic payload ────────────────────────────

/**
 * Kafka topic `order.confirmed` payload.
 * Producer: Order Service (Step 2.4).
 * Consumers:
 *   - Kitchen Service (Phase 2B) — render KDS tickets
 *   - Notification Service (Phase 4C) — staff notifications
 *   - Analytics Service (future) — revenue tracking
 *
 * Partition key: tenantId (per Kafka ADR partition strategy).
 */
export type OrderConfirmedEvent = {
  tenantId: string;
  orderId: string;
  sessionId: string;
  items: OrderItem[];
  totalAmount: number;
  /** ISO 8601 — confirmation time */
  confirmedAt: string;
  /** Staff Keycloak sub */
  confirmedByUserId: string;
};

// ─── Derived data shapes (not events) ───────────────

/**
 * Data structure cho KDS render (kitchen + bar tickets).
 * Derived view từ Order + Menu metadata; không phải standalone event ở Phase 2A.
 *
 * Phase 2B sẽ promote thành Kafka event độc lập (`kitchen.ticket.created`,
 * `bar.ticket.created`) nếu cần per-station routing.
 */
export type KDSTicket = {
  /** Initially = orderId; Phase 2B có thể split per-station (kitchen-{orderId}, bar-{orderId}) */
  ticketId: string;
  tenantId: string;
  orderId: string;
  tableId: string;
  tableName: string;
  items: OrderItem[];
  /** VIP/rush flag, Phase 2B logic */
  priority: boolean;
  /** ISO 8601 */
  createdAt: string;
  /** SLA timer threshold per item category — Phase 2B determines per item type */
  slaSeconds: number;
};
```

- [ ] **Step 2: Verify**

```bash
npx tsc --noEmit --target es2020 --module esnext --moduleResolution node libs/shared/types/src/lib/realtime-events.types.ts 2>&1 | head -10
```

Expected: 0 errors.

> **Note**: file dùng `OrderStatusType` alias để tránh circular import nếu enum import bị TypeScript flag. Nếu compile clean với direct `OrderStatus` import → đơn giản hoá lại.

---

## Task 7: Update Barrel + Delete Empty `types.ts`

**Files:** 1 modify (`libs/shared/types/src/index.ts`), 1 delete (`libs/shared/types/src/lib/types.ts`)

**Goal:** Export 20 new symbols từ barrel + cleanup empty placeholder file.

- [ ] **Step 1: Replace `libs/shared/types/src/index.ts` with new content**

```typescript
// ─── Common ─────────────────────────────────────────
export type { ApiResponse, ApiErrorResponse, PaginatedResponse, PaginationParams, SortParams } from './lib/api.types';

// ─── User & Auth ────────────────────────────────────
export type { UserProfile, UserSession } from './lib/user.types';

// ─── Menu ───────────────────────────────────────────
export type { Category, MenuItem, CategoryStatus, MenuItemStatus } from './lib/menu.types';

// ─── Table ──────────────────────────────────────────
export type { Area, RestaurantTable, TableStatus } from './lib/table.types';

// ─── Order (Phase 2A) ───────────────────────────────
export type { Order, OrderItem } from './lib/order.types';
export { OrderStatus, OrderItemStatus, ALLOWED_ORDER_TRANSITIONS } from './lib/order.types';

// ─── Bill (Phase 2A) ────────────────────────────────
export type { Bill } from './lib/bill.types';
export { BillStatus, PaymentMethod, ALLOWED_BILL_TRANSITIONS } from './lib/bill.types';

// ─── Session & Cart (Phase 2A) ──────────────────────
export type { Session, CartItem } from './lib/session.types';
export { SessionStatus } from './lib/session.types';

// ─── Service Request (Phase 2A) ─────────────────────
export type { ServiceRequest } from './lib/service-request.types';
export {
  ServiceRequestType,
  ServiceRequestStatus,
  ALLOWED_SERVICE_REQUEST_TRANSITIONS,
} from './lib/service-request.types';

// ─── Realtime Events (Phase 2A) ─────────────────────
export type {
  OrderCreatedEvent,
  OrderStatusChangedEvent,
  ServiceRequestedEvent,
  OrderConfirmedEvent,
  KDSTicket,
} from './lib/realtime-events.types';

// ─── Public Menu (Customer PWA) ─────────────────────
export type { PublicMenuResponse, PublicMenuCategory, PublicMenuItem } from './lib/public-menu.types';

// ─── QR Validation ──────────────────────────────────
export type { ValidateQrRequest, ValidateQrResponse } from './lib/qr-validation.types';
```

> **Removed**: `PaymentStatus` export (enum loại bỏ; replaced by `BillStatus`).

- [ ] **Step 2: Delete empty placeholder**

```bash
rm libs/shared/types/src/lib/types.ts
```

> File chỉ chứa empty comment, outdated.

- [ ] **Step 3: Verify barrel imports**

```bash
npx nx build types 2>&1 | tail -5
```

Expected: `Successfully ran target build for project types`.

---

## Task 8: Verify Types Lib Compiles (Intermediate Checkpoint)

**Files:** None (operational)

**Goal:** Confirm types lib compile clean trước khi cascade sang mock-data + constants.

- [ ] **Step 1: Build types lib**

```bash
npx nx build types
```

Expected: SUCCESS.

- [ ] **Step 2: Quick smoke test — import từ barrel**

```bash
node -e "
const types = require('./dist/libs/shared/types');
console.log('Exports:', Object.keys(types).sort());
"
```

Expected output includes (sorted):

```
[ 'ALLOWED_BILL_TRANSITIONS', 'ALLOWED_ORDER_TRANSITIONS', 'ALLOWED_SERVICE_REQUEST_TRANSITIONS',
  'BillStatus', 'OrderItemStatus', 'OrderStatus', 'PaymentMethod', 'ServiceRequestStatus',
  'ServiceRequestType', 'SessionStatus' ]
```

(types-only exports như `Order`, `Bill`, `OrderItem` không xuất hiện ở runtime — bình thường).

Nếu thiếu enum nào → check Task 7 barrel.

---

## Task 9: Refactor `libs/shared/constants/src/lib/status.ts`

**Files:** 1 modify (`libs/shared/constants/src/lib/status.ts`)

**Goal:** Replace hardcoded status arrays bằng `Object.values(EnumName)` để always-in-sync với types lib. Loại bỏ `PAYMENT_STATUSES` (redundant).

- [ ] **Step 1: Replace file content**

File: `libs/shared/constants/src/lib/status.ts`

```typescript
import type { TableStatus, CategoryStatus, MenuItemStatus } from '@einvoice/types';
import {
  OrderStatus,
  OrderItemStatus,
  BillStatus,
  SessionStatus,
  ServiceRequestType,
  ServiceRequestStatus,
  PaymentMethod,
} from '@einvoice/types';

// ─── Out-of-scope (kept as-is — table/menu/category are string unions) ───
export const TABLE_STATUSES: readonly TableStatus[] = ['available', 'occupied', 'billing', 'cleaning'];
export const CATEGORY_STATUSES: readonly CategoryStatus[] = ['active', 'inactive'];
export const MENU_ITEM_STATUSES: readonly MenuItemStatus[] = ['available', 'out_of_stock'];

// ─── Phase 2A — derive từ enum để always-in-sync ─────
export const ORDER_STATUSES = Object.values(OrderStatus);
export const ORDER_ITEM_STATUSES = Object.values(OrderItemStatus);
export const BILL_STATUSES = Object.values(BillStatus);
export const SESSION_STATUSES = Object.values(SessionStatus);
export const SERVICE_REQUEST_TYPES = Object.values(ServiceRequestType);
export const SERVICE_REQUEST_STATUSES = Object.values(ServiceRequestStatus);
export const PAYMENT_METHODS = Object.values(PaymentMethod);

// REMOVED: PAYMENT_STATUSES (PaymentStatus enum loại bỏ — replaced by BillStatus)
```

- [ ] **Step 2: Verify constants build**

```bash
npx nx build constants 2>&1 | tail -5
```

Expected: SUCCESS.

- [ ] **Step 3: Search for any remaining `PaymentStatus` references**

```bash
rg "PaymentStatus|PAYMENT_STATUSES" libs/ apps/ --type ts
```

Expected: 0 matches. Nếu có → manual cleanup remaining references.

---

## Task 10: Refactor `libs/shared/mock-data/src/lib/orders.ts`

**Files:** 1 modify (`libs/shared/mock-data/src/lib/orders.ts`)

**Goal:** Update 4 mock orders với new shape: tenantId, idempotencyKey, audit fields, UPPERCASE enum status, OrderItemStatus per item.

- [ ] **Step 1: Replace file content**

File: `libs/shared/mock-data/src/lib/orders.ts`

```typescript
import type { Order } from '@einvoice/types';
import { OrderStatus, OrderItemStatus } from '@einvoice/types';

const TENANT_A = 'tenant_a';
const WAITER_USER_ID = '8ec2f6e3-0000-0000-0000-000000000004';

export const orders: Order[] = [
  // Order 001 — multi-item, mix item statuses (PROCESSING + SERVED)
  {
    id: 'order-001',
    tenantId: TENANT_A,
    tableId: 'tbl-001',
    tableName: 'T1',
    sessionId: 'session-001',
    items: [
      {
        id: 'oi-001',
        orderId: 'order-001',
        menuItemId: 'item-001',
        menuItemName: 'Gỏi cuốn tôm thịt',
        quantity: 2,
        unitPrice: 45000,
        status: OrderItemStatus.PROCESSING,
        createdAt: '2026-04-06T10:30:00Z',
        updatedAt: '2026-04-06T10:35:00Z',
      },
      {
        id: 'oi-002',
        orderId: 'order-001',
        menuItemId: 'item-005',
        menuItemName: 'Cơm tấm sườn bì',
        quantity: 1,
        unitPrice: 75000,
        note: 'Thêm trứng ốp la',
        status: OrderItemStatus.PROCESSING,
        createdAt: '2026-04-06T10:30:00Z',
        updatedAt: '2026-04-06T10:35:00Z',
      },
      {
        id: 'oi-003',
        orderId: 'order-001',
        menuItemId: 'item-011',
        menuItemName: 'Trà đá',
        quantity: 3,
        unitPrice: 10000,
        status: OrderItemStatus.SERVED,
        createdAt: '2026-04-06T10:30:00Z',
        updatedAt: '2026-04-06T10:32:00Z',
      },
    ],
    status: OrderStatus.PROCESSING,
    totalAmount: 195000, // 2*45000 + 1*75000 + 3*10000 = 195000
    idempotencyKey: '11111111-1111-4111-8111-111111111111',
    confirmedAt: '2026-04-06T10:32:00Z',
    confirmedByUserId: WAITER_USER_ID,
    createdAt: '2026-04-06T10:30:00Z',
    updatedAt: '2026-04-06T10:35:00Z',
  },
  // Order 002 — same session as 001, all SERVED
  {
    id: 'order-002',
    tenantId: TENANT_A,
    tableId: 'tbl-001',
    tableName: 'T1',
    sessionId: 'session-001',
    items: [
      {
        id: 'oi-004',
        orderId: 'order-002',
        menuItemId: 'item-015',
        menuItemName: 'Chè ba màu',
        quantity: 2,
        unitPrice: 30000,
        status: OrderItemStatus.SERVED,
        createdAt: '2026-04-06T10:45:00Z',
        updatedAt: '2026-04-06T11:00:00Z',
      },
    ],
    status: OrderStatus.SERVED,
    totalAmount: 60000,
    idempotencyKey: '22222222-2222-4222-8222-222222222222',
    confirmedAt: '2026-04-06T10:46:00Z',
    confirmedByUserId: WAITER_USER_ID,
    createdAt: '2026-04-06T10:45:00Z',
    updatedAt: '2026-04-06T11:00:00Z',
  },
  // Order 003 — different session, PROCESSING (was 'confirmed' in old data)
  {
    id: 'order-003',
    tenantId: TENANT_A,
    tableId: 'tbl-005',
    tableName: 'T5',
    sessionId: 'session-002',
    items: [
      {
        id: 'oi-005',
        orderId: 'order-003',
        menuItemId: 'item-007',
        menuItemName: 'Phở bò tái',
        quantity: 2,
        unitPrice: 85000,
        note: 'Không hành',
        status: OrderItemStatus.PROCESSING,
        createdAt: '2026-04-06T11:15:00Z',
        updatedAt: '2026-04-06T11:20:00Z',
      },
      {
        id: 'oi-006',
        orderId: 'order-003',
        menuItemId: 'item-012',
        menuItemName: 'Cà phê sữa đá',
        quantity: 2,
        unitPrice: 29000,
        status: OrderItemStatus.READY,
        createdAt: '2026-04-06T11:15:00Z',
        updatedAt: '2026-04-06T11:20:00Z',
      },
    ],
    status: OrderStatus.PROCESSING,
    totalAmount: 228000, // 2*85000 + 2*29000 = 228000
    idempotencyKey: '33333333-3333-4333-8333-333333333333',
    confirmedAt: '2026-04-06T11:16:00Z',
    confirmedByUserId: WAITER_USER_ID,
    createdAt: '2026-04-06T11:15:00Z',
    updatedAt: '2026-04-06T11:20:00Z',
  },
  // Order 004 — COMPLETED (was 'served' + paid in old data)
  {
    id: 'order-004',
    tenantId: TENANT_A,
    tableId: 'tbl-008',
    tableName: 'L3',
    sessionId: 'session-003',
    items: [
      {
        id: 'oi-007',
        orderId: 'order-004',
        menuItemId: 'item-003',
        menuItemName: 'Súp hải sản',
        quantity: 1,
        unitPrice: 65000,
        status: OrderItemStatus.SERVED,
        createdAt: '2026-04-06T09:00:00Z',
        updatedAt: '2026-04-06T09:30:00Z',
      },
      {
        id: 'oi-008',
        orderId: 'order-004',
        menuItemId: 'item-006',
        menuItemName: 'Bún bò Huế',
        quantity: 3,
        unitPrice: 80000,
        status: OrderItemStatus.SERVED,
        createdAt: '2026-04-06T09:00:00Z',
        updatedAt: '2026-04-06T09:30:00Z',
      },
    ],
    status: OrderStatus.COMPLETED,
    totalAmount: 305000, // 1*65000 + 3*80000 = 305000
    idempotencyKey: '44444444-4444-4444-8444-444444444444',
    confirmedAt: '2026-04-06T09:01:00Z',
    confirmedByUserId: WAITER_USER_ID,
    createdAt: '2026-04-06T09:00:00Z',
    updatedAt: '2026-04-06T09:45:00Z',
  },
];
```

> **Removed fields**: `paymentMethod`, `paymentStatus` (move to Bill aggregate, sẽ define ở Task 12).

- [ ] **Step 2: Quick math verification**

Verify totalAmount khớp sum(items.unitPrice \* quantity):

- order-001: 2*45000 + 1*75000 + 3\*10000 = 90000 + 75000 + 30000 = **195000** ✓
- order-002: 2\*30000 = **60000** ✓
- order-003: 2*85000 + 2*29000 = 170000 + 58000 = **228000** ✓
- order-004: 1*65000 + 3*80000 = 65000 + 240000 = **305000** ✓

---

## Task 11: Refactor `libs/shared/mock-data/src/lib/sessions.ts`

**Files:** 1 modify (`libs/shared/mock-data/src/lib/sessions.ts`)

**Goal:** Replace `MockSession` (local type) với `Session` (từ types lib) + extend với mock-only fields (qrToken, etc.).

- [ ] **Step 1: Replace file content**

File: `libs/shared/mock-data/src/lib/sessions.ts`

```typescript
import type { Session } from '@einvoice/types';
import { SessionStatus } from '@einvoice/types';

const TENANT_A = 'tenant_a';

/**
 * Mock-only extension to Session type — adds fields needed for mock UI rendering
 * (areaName, restaurantName) and QR validation mocking (qrToken).
 *
 * In production, qrToken validation thuộc Catalog/Tenant module; restaurantName
 * lookup from Tenant entity. Mock data inlines for convenience.
 */
export type MockSessionExtended = Session & {
  areaName: string;
  capacity: number;
  restaurantName: string;
  qrToken: string;
};

export const sessions: MockSessionExtended[] = [
  {
    id: 'session-001',
    tenantId: TENANT_A,
    tableId: 'tbl-001',
    tableName: 'T1',
    status: SessionStatus.ACTIVE,
    startedAt: '2026-04-06T10:00:00Z',
    lastActivity: '2026-04-06T11:00:00Z',
    orderCount: 2,
    areaName: 'Tầng trệt',
    capacity: 4,
    restaurantName: 'Nhà hàng QR Table Demo',
    qrToken: 'hmac_t1_abc123',
  },
  {
    id: 'session-002',
    tenantId: TENANT_A,
    tableId: 'tbl-005',
    tableName: 'T5',
    status: SessionStatus.ACTIVE,
    startedAt: '2026-04-06T11:00:00Z',
    lastActivity: '2026-04-06T11:30:00Z',
    orderCount: 1,
    areaName: 'Tầng trệt',
    capacity: 6,
    restaurantName: 'Nhà hàng QR Table Demo',
    qrToken: 'hmac_t5_def456',
  },
  {
    id: 'session-003',
    tenantId: TENANT_A,
    tableId: 'tbl-008',
    tableName: 'L3',
    status: SessionStatus.CLOSED,
    startedAt: '2026-04-06T08:30:00Z',
    lastActivity: '2026-04-06T09:45:00Z',
    closedAt: '2026-04-06T09:50:00Z',
    orderCount: 1,
    areaName: 'Lầu 1',
    capacity: 4,
    restaurantName: 'Nhà hàng QR Table Demo',
    qrToken: 'hmac_l3_ghi789',
  },
];
```

> **Renamed**: `MockSession` → `MockSessionExtended` (clearer; signals it's an extension of core `Session` type).

---

## Task 12: Create `libs/shared/mock-data/src/lib/bills.ts`

**Files:** 1 create

**Goal:** 3 mock bills tương ứng 3 sessions, status mix (OPEN, PENDING_PAYMENT, PAID).

- [ ] **Step 1: Create + write file**

File: `libs/shared/mock-data/src/lib/bills.ts`

```typescript
import type { Bill } from '@einvoice/types';
import { BillStatus, PaymentMethod } from '@einvoice/types';

const TENANT_A = 'tenant_a';

export const bills: Bill[] = [
  // Bill cho session-001 (active, đang accumulate orders)
  {
    id: 'bill-001',
    tenantId: TENANT_A,
    sessionId: 'session-001',
    orderIds: ['order-001', 'order-002'],
    subtotal: 255000, // 195000 + 60000
    total: 255000,
    roundingAmount: 0,
    status: BillStatus.OPEN,
    createdAt: '2026-04-06T10:00:00Z',
    updatedAt: '2026-04-06T11:00:00Z',
  },
  // Bill cho session-002 (customer requested bill, awaiting staff)
  {
    id: 'bill-002',
    tenantId: TENANT_A,
    sessionId: 'session-002',
    orderIds: ['order-003'],
    subtotal: 228000,
    total: 228000,
    roundingAmount: 0,
    status: BillStatus.PENDING_PAYMENT,
    closedAt: '2026-04-06T11:30:00Z',
    createdAt: '2026-04-06T11:15:00Z',
    updatedAt: '2026-04-06T11:30:00Z',
  },
  // Bill cho session-003 (paid + closed — historical)
  {
    id: 'bill-003',
    tenantId: TENANT_A,
    sessionId: 'session-003',
    orderIds: ['order-004'],
    subtotal: 305000,
    total: 305000,
    roundingAmount: 0,
    paymentMethod: PaymentMethod.CASH,
    status: BillStatus.PAID,
    closedAt: '2026-04-06T09:40:00Z',
    paidAt: '2026-04-06T09:45:00Z',
    createdAt: '2026-04-06T09:00:00Z',
    updatedAt: '2026-04-06T09:45:00Z',
  },
];
```

---

## Task 13: Create `libs/shared/mock-data/src/lib/carts.ts`

**Files:** 1 create

**Goal:** Mock carts cho 3 sessions, demonstrate version field + denormalized menuItemName.

- [ ] **Step 1: Create + write file**

File: `libs/shared/mock-data/src/lib/carts.ts`

```typescript
import type { CartItem } from '@einvoice/types';

/**
 * Mock cart structure — wraps CartItem array per session.
 *
 * In production, cart lưu Redis Hash key `cart:{tenantId}:{sessionId}`
 * với optimistic version. Mock data simulate this shape.
 */
export type MockCart = {
  sessionId: string;
  items: CartItem[];
};

export const carts: MockCart[] = [
  // Session 001 — đã submit hết, cart trống
  {
    sessionId: 'session-001',
    items: [],
  },
  // Session 002 — đang gom thêm món (version 2 sau optimistic update)
  {
    sessionId: 'session-002',
    items: [
      {
        menuItemId: 'item-014',
        menuItemName: 'Sinh tố bơ',
        quantity: 1,
        unitPrice: 45000,
        version: 2,
      },
    ],
  },
  // Session 003 — đã COMPLETED, cart trống (session closed)
  {
    sessionId: 'session-003',
    items: [],
  },
];
```

---

## Task 14: Create `libs/shared/mock-data/src/lib/service-requests.ts`

**Files:** 1 create

**Goal:** 3 mock service requests, status mix (PENDING, ACKNOWLEDGED, RESOLVED).

- [ ] **Step 1: Create + write file**

File: `libs/shared/mock-data/src/lib/service-requests.ts`

```typescript
import type { ServiceRequest } from '@einvoice/types';
import { ServiceRequestType, ServiceRequestStatus } from '@einvoice/types';

const TENANT_A = 'tenant_a';
const WAITER_USER_ID = '8ec2f6e3-0000-0000-0000-000000000004';

export const serviceRequests: ServiceRequest[] = [
  // Request 1 — Customer call staff, PENDING
  {
    id: 'sr-001',
    tenantId: TENANT_A,
    tableId: 'tbl-001',
    sessionId: 'session-001',
    type: ServiceRequestType.CALL_STAFF,
    status: ServiceRequestStatus.PENDING,
    createdAt: '2026-04-06T10:50:00Z',
    updatedAt: '2026-04-06T10:50:00Z',
  },
  // Request 2 — Customer request bill, ACKNOWLEDGED by waiter
  {
    id: 'sr-002',
    tenantId: TENANT_A,
    tableId: 'tbl-005',
    sessionId: 'session-002',
    type: ServiceRequestType.REQUEST_BILL,
    status: ServiceRequestStatus.ACKNOWLEDGED,
    acknowledgedAt: '2026-04-06T11:31:00Z',
    acknowledgedByUserId: WAITER_USER_ID,
    createdAt: '2026-04-06T11:30:00Z',
    updatedAt: '2026-04-06T11:31:00Z',
  },
  // Request 3 — General help, RESOLVED
  {
    id: 'sr-003',
    tenantId: TENANT_A,
    tableId: 'tbl-008',
    sessionId: 'session-003',
    type: ServiceRequestType.GENERAL_HELP,
    status: ServiceRequestStatus.RESOLVED,
    note: 'Hỏi wifi',
    acknowledgedAt: '2026-04-06T09:15:00Z',
    acknowledgedByUserId: WAITER_USER_ID,
    resolvedAt: '2026-04-06T09:18:00Z',
    createdAt: '2026-04-06T09:14:00Z',
    updatedAt: '2026-04-06T09:18:00Z',
  },
];
```

---

## Task 15: Update `libs/shared/mock-data/src/lib/helpers.ts` + `index.ts`

**Files:** 2 modify

**Goal:** Add 4 new helpers; update barrel để re-export new files.

- [ ] **Step 1: Update `helpers.ts`** (replace imports + extend)

File: `libs/shared/mock-data/src/lib/helpers.ts`

```typescript
import type { Bill, CartItem, Category, MenuItem, Order, ServiceRequest, Session } from '@einvoice/types';
import { ServiceRequestStatus } from '@einvoice/types';
import { categories } from './categories';
import { menuItems } from './menu-items';
import { tables } from './tables';
import { orders } from './orders';
import { sessions, type MockSessionExtended } from './sessions';
import { bills } from './bills';
import { carts } from './carts';
import { serviceRequests } from './service-requests';

export type CategoryWithItems = Category & { items: MenuItem[] };

// ─── Existing helpers (kept) ────────────────────────

export function getMenuByCategory(): CategoryWithItems[] {
  return categories
    .filter((c) => c.status === 'active')
    .sort((a, b) => a.sortOrder - b.sortOrder)
    .map((category) => ({
      ...category,
      items: menuItems.filter((item) => item.categoryId === category.id).sort((a, b) => a.sortOrder - b.sortOrder),
    }));
}

export function getAllMenuItems(): MenuItem[] {
  return menuItems
    .filter((item) => item.status === 'available' || item.status === 'out_of_stock')
    .sort((a, b) => a.sortOrder - b.sortOrder);
}

export function getTableByQrToken(token: string) {
  return tables.find((t) => t.qrToken === token);
}

export function getSessionByQrToken(token: string): MockSessionExtended | undefined {
  return sessions.find((s) => s.qrToken === token);
}

export function getOrdersBySession(sessionId: string): Order[] {
  return orders.filter((o) => o.sessionId === sessionId);
}

export function getMockSession(tableId: string): MockSessionExtended | undefined {
  return sessions.find((s) => s.tableId === tableId);
}

// ─── NEW helpers (Step 2.3) ─────────────────────────

export function getBillBySession(sessionId: string): Bill | undefined {
  return bills.find((b) => b.sessionId === sessionId);
}

export function getCartBySession(sessionId: string): CartItem[] {
  const cart = carts.find((c) => c.sessionId === sessionId);
  return cart ? cart.items : [];
}

export function getActiveServiceRequests(tenantId: string): ServiceRequest[] {
  return serviceRequests.filter((sr) => sr.tenantId === tenantId && sr.status !== ServiceRequestStatus.RESOLVED);
}

export function getMockSessionById(sessionId: string): Session | undefined {
  return sessions.find((s) => s.id === sessionId);
}
```

- [ ] **Step 2: Update barrel `libs/shared/mock-data/src/index.ts`**

```typescript
// Raw data
export { categories } from './lib/categories';
export { menuItems } from './lib/menu-items';
export { areas } from './lib/areas';
export { tables } from './lib/tables';
export { orders } from './lib/orders';
export { sessions, type MockSessionExtended } from './lib/sessions';
export { bills } from './lib/bills';
export { carts, type MockCart } from './lib/carts';
export { serviceRequests } from './lib/service-requests';

// Helper functions
export {
  getMenuByCategory,
  getAllMenuItems,
  getTableByQrToken,
  getSessionByQrToken,
  getOrdersBySession,
  getMockSession,
  getBillBySession,
  getCartBySession,
  getActiveServiceRequests,
  getMockSessionById,
  type CategoryWithItems,
} from './lib/helpers';
```

> **Removed export**: `MockSession` (renamed to `MockSessionExtended`).

---

## Task 16: Verify Mock-Data Lib Compiles (Intermediate Checkpoint)

**Files:** None (operational)

**Goal:** Confirm mock-data lib + constants compile clean trước khi viết tests.

- [ ] **Step 1: Build mock-data**

```bash
npx nx build mock-data 2>&1 | tail -10
```

Expected: SUCCESS.

- [ ] **Step 2: Build constants**

```bash
npx nx build constants 2>&1 | tail -10
```

Expected: SUCCESS.

- [ ] **Step 3: Run-many sanity check**

```bash
npx nx run-many --target=build --projects=types,mock-data,constants
```

Expected: 3 SUCCESS.

Nếu fail → đối chiếu spec §6.3 + Task 10-15 step output. Common issues:

- Missing import của enum (vd quên import `OrderStatus` trong `orders.ts`)
- Wrong field name (vd `quantity` typo `qty`)
- Wrong enum casing (vd `OrderStatus.pending` thay vì `OrderStatus.PENDING`)

---

## Task 17: Write Layer 1 — `enum-completeness.spec.ts`

**Files:** 1 create

**Goal:** Catch accidental addition/removal/typo của enum values.

- [ ] **Step 1: Create test directory if not exists**

```bash
mkdir -p libs/shared/types/src/lib/__tests__
```

- [ ] **Step 2: Create + write test file**

File: `libs/shared/types/src/lib/__tests__/enum-completeness.spec.ts`

```typescript
import {
  OrderStatus,
  OrderItemStatus,
  BillStatus,
  SessionStatus,
  ServiceRequestType,
  ServiceRequestStatus,
  PaymentMethod,
} from '../../index';

describe('Enum completeness — canonical values per Step 2.3 spec', () => {
  it('OrderStatus has exactly 7 values per phase doc §8', () => {
    expect(Object.values(OrderStatus).sort()).toEqual(
      ['CANCELED', 'COMPLETED', 'DRAFT', 'PENDING', 'PROCESSING', 'READY', 'SERVED'].sort(),
    );
  });

  it('OrderItemStatus has exactly 4 values (kitchen subset, no DRAFT/PENDING/COMPLETED)', () => {
    expect(Object.values(OrderItemStatus).sort()).toEqual(['CANCELED', 'PROCESSING', 'READY', 'SERVED'].sort());
  });

  it('BillStatus has exactly 3 values (Phase 2A scope, PAID forward-compat)', () => {
    expect(Object.values(BillStatus).sort()).toEqual(['OPEN', 'PAID', 'PENDING_PAYMENT'].sort());
  });

  it('SessionStatus has exactly 2 values (IDLE is internal Redis TTL, not exposed)', () => {
    expect(Object.values(SessionStatus).sort()).toEqual(['ACTIVE', 'CLOSED'].sort());
  });

  it('ServiceRequestType has exactly 3 values per phase doc', () => {
    expect(Object.values(ServiceRequestType).sort()).toEqual(['CALL_STAFF', 'GENERAL_HELP', 'REQUEST_BILL'].sort());
  });

  it('ServiceRequestStatus has exactly 3 values per phase doc §8', () => {
    expect(Object.values(ServiceRequestStatus).sort()).toEqual(['ACKNOWLEDGED', 'PENDING', 'RESOLVED'].sort());
  });

  it('PaymentMethod has exactly 1 value (Phase 2A — CASH only; Phase 3 mở rộng)', () => {
    expect(Object.values(PaymentMethod).sort()).toEqual(['CASH']);
  });

  it('All enum values are UPPERCASE strings (no lowercase, no kebab-case)', () => {
    const allEnums = [
      OrderStatus,
      OrderItemStatus,
      BillStatus,
      SessionStatus,
      ServiceRequestType,
      ServiceRequestStatus,
      PaymentMethod,
    ];
    allEnums.forEach((enumObj) => {
      Object.values(enumObj).forEach((value) => {
        expect(typeof value).toBe('string');
        expect(value).toBe(String(value).toUpperCase());
      });
    });
  });

  it('All enum keys === values (no accidental key/value mismatch)', () => {
    const allEnums = [
      OrderStatus,
      OrderItemStatus,
      BillStatus,
      SessionStatus,
      ServiceRequestType,
      ServiceRequestStatus,
      PaymentMethod,
    ];
    allEnums.forEach((enumObj) => {
      Object.entries(enumObj).forEach(([key, value]) => {
        expect(key).toBe(value);
      });
    });
  });
});
```

- [ ] **Step 3: Run test**

```bash
npx nx test types --testFile=enum-completeness 2>&1 | tail -20
```

Expected: 9 tests PASS.

Nếu test fail → check enum definitions trong types files.

---

## Task 18: Write Layer 1 — `transitions.spec.ts`

**Files:** 1 create

**Goal:** Verify 3 transition matrices structurally + business rules per phase doc §8.

- [ ] **Step 1: Create + write test file**

File: `libs/shared/types/src/lib/__tests__/transitions.spec.ts`

```typescript
import {
  ALLOWED_ORDER_TRANSITIONS,
  ALLOWED_BILL_TRANSITIONS,
  ALLOWED_SERVICE_REQUEST_TRANSITIONS,
  OrderStatus,
  BillStatus,
  ServiceRequestStatus,
} from '../../index';

describe('State transition matrices — Phase 2A spec §8', () => {
  describe('ALLOWED_ORDER_TRANSITIONS', () => {
    it('every OrderStatus appears as key', () => {
      Object.values(OrderStatus).forEach((status) => {
        expect(ALLOWED_ORDER_TRANSITIONS).toHaveProperty(status);
      });
    });

    it('every transition target is a valid OrderStatus', () => {
      Object.values(ALLOWED_ORDER_TRANSITIONS).forEach((targets) => {
        targets.forEach((target) => {
          expect(Object.values(OrderStatus)).toContain(target);
        });
      });
    });

    it('terminal states (COMPLETED, CANCELED) have no outgoing transitions', () => {
      expect(ALLOWED_ORDER_TRANSITIONS[OrderStatus.COMPLETED]).toEqual([]);
      expect(ALLOWED_ORDER_TRANSITIONS[OrderStatus.CANCELED]).toEqual([]);
    });

    it('canonical happy path: DRAFT → PENDING → PROCESSING → READY → SERVED → COMPLETED', () => {
      expect(ALLOWED_ORDER_TRANSITIONS[OrderStatus.DRAFT]).toContain(OrderStatus.PENDING);
      expect(ALLOWED_ORDER_TRANSITIONS[OrderStatus.PENDING]).toContain(OrderStatus.PROCESSING);
      expect(ALLOWED_ORDER_TRANSITIONS[OrderStatus.PROCESSING]).toContain(OrderStatus.READY);
      expect(ALLOWED_ORDER_TRANSITIONS[OrderStatus.READY]).toContain(OrderStatus.SERVED);
      expect(ALLOWED_ORDER_TRANSITIONS[OrderStatus.SERVED]).toContain(OrderStatus.COMPLETED);
    });

    it('CANCELED reachable từ DRAFT, PENDING, PROCESSING (per phase doc §8)', () => {
      expect(ALLOWED_ORDER_TRANSITIONS[OrderStatus.DRAFT]).toContain(OrderStatus.CANCELED);
      expect(ALLOWED_ORDER_TRANSITIONS[OrderStatus.PENDING]).toContain(OrderStatus.CANCELED);
      expect(ALLOWED_ORDER_TRANSITIONS[OrderStatus.PROCESSING]).toContain(OrderStatus.CANCELED);
    });

    it('CANNOT cancel after READY (business rule: order đã ra món, không revert)', () => {
      expect(ALLOWED_ORDER_TRANSITIONS[OrderStatus.READY]).not.toContain(OrderStatus.CANCELED);
      expect(ALLOWED_ORDER_TRANSITIONS[OrderStatus.SERVED]).not.toContain(OrderStatus.CANCELED);
    });

    it('CANNOT skip states (vd: PENDING không thể nhảy thẳng READY)', () => {
      expect(ALLOWED_ORDER_TRANSITIONS[OrderStatus.PENDING]).not.toContain(OrderStatus.READY);
      expect(ALLOWED_ORDER_TRANSITIONS[OrderStatus.PENDING]).not.toContain(OrderStatus.SERVED);
      expect(ALLOWED_ORDER_TRANSITIONS[OrderStatus.PENDING]).not.toContain(OrderStatus.COMPLETED);
    });
  });

  describe('ALLOWED_BILL_TRANSITIONS', () => {
    it('every BillStatus appears as key', () => {
      Object.values(BillStatus).forEach((status) => {
        expect(ALLOWED_BILL_TRANSITIONS).toHaveProperty(status);
      });
    });

    it('OPEN → PENDING_PAYMENT → PAID', () => {
      expect(ALLOWED_BILL_TRANSITIONS[BillStatus.OPEN]).toContain(BillStatus.PENDING_PAYMENT);
      expect(ALLOWED_BILL_TRANSITIONS[BillStatus.PENDING_PAYMENT]).toContain(BillStatus.PAID);
    });

    it('PENDING_PAYMENT → OPEN allowed (revert request_bill nếu customer order thêm)', () => {
      expect(ALLOWED_BILL_TRANSITIONS[BillStatus.PENDING_PAYMENT]).toContain(BillStatus.OPEN);
    });

    it('PAID is terminal', () => {
      expect(ALLOWED_BILL_TRANSITIONS[BillStatus.PAID]).toEqual([]);
    });

    it('OPEN cannot skip directly to PAID (must go via PENDING_PAYMENT)', () => {
      expect(ALLOWED_BILL_TRANSITIONS[BillStatus.OPEN]).not.toContain(BillStatus.PAID);
    });
  });

  describe('ALLOWED_SERVICE_REQUEST_TRANSITIONS', () => {
    it('every ServiceRequestStatus appears as key', () => {
      Object.values(ServiceRequestStatus).forEach((status) => {
        expect(ALLOWED_SERVICE_REQUEST_TRANSITIONS).toHaveProperty(status);
      });
    });

    it('PENDING → ACKNOWLEDGED → RESOLVED (per phase doc §8)', () => {
      expect(ALLOWED_SERVICE_REQUEST_TRANSITIONS[ServiceRequestStatus.PENDING]).toEqual([
        ServiceRequestStatus.ACKNOWLEDGED,
      ]);
      expect(ALLOWED_SERVICE_REQUEST_TRANSITIONS[ServiceRequestStatus.ACKNOWLEDGED]).toEqual([
        ServiceRequestStatus.RESOLVED,
      ]);
      expect(ALLOWED_SERVICE_REQUEST_TRANSITIONS[ServiceRequestStatus.RESOLVED]).toEqual([]);
    });

    it('PENDING cannot skip directly to RESOLVED (must acknowledge first)', () => {
      expect(ALLOWED_SERVICE_REQUEST_TRANSITIONS[ServiceRequestStatus.PENDING]).not.toContain(
        ServiceRequestStatus.RESOLVED,
      );
    });
  });
});
```

- [ ] **Step 2: Run test**

```bash
npx nx test types --testFile=transitions 2>&1 | tail -20
```

Expected: 14 tests PASS.

---

## Task 19: Write Layer 3 — `data-conformance.spec.ts`

**Files:** 1 create

**Goal:** Verify mock data conforms to types tại runtime (defense-in-depth ngoài tsc).

- [ ] **Step 1: Create test directory if not exists**

```bash
mkdir -p libs/shared/mock-data/src/lib/__tests__
```

- [ ] **Step 2: Create + write test file**

File: `libs/shared/mock-data/src/lib/__tests__/data-conformance.spec.ts`

```typescript
import { orders, sessions, bills, carts, serviceRequests } from '../../index';
import {
  OrderStatus,
  OrderItemStatus,
  BillStatus,
  SessionStatus,
  ServiceRequestStatus,
  ServiceRequestType,
} from '@einvoice/types';

describe('Mock data conforms to type contracts', () => {
  describe('orders mock data', () => {
    it('every order has valid OrderStatus value', () => {
      orders.forEach((order) => {
        expect(Object.values(OrderStatus)).toContain(order.status);
      });
    });

    it('every order has non-empty tenantId and idempotencyKey', () => {
      orders.forEach((order) => {
        expect(order.tenantId).toBeTruthy();
        expect(order.idempotencyKey).toBeTruthy();
        expect(order.idempotencyKey.length).toBeGreaterThan(10); // UUID-like
      });
    });

    it('every order item has valid OrderItemStatus', () => {
      orders.forEach((order) => {
        order.items.forEach((item) => {
          expect(Object.values(OrderItemStatus)).toContain(item.status);
        });
      });
    });

    it('every order item orderId matches parent order id', () => {
      orders.forEach((order) => {
        order.items.forEach((item) => {
          expect(item.orderId).toBe(order.id);
        });
      });
    });

    it('totalAmount = sum(items.unitPrice * quantity) for every order', () => {
      orders.forEach((order) => {
        const calculated = order.items.reduce((sum, item) => sum + item.unitPrice * item.quantity, 0);
        expect(order.totalAmount).toBe(calculated);
      });
    });

    it('confirmed orders have confirmedAt + confirmedByUserId set', () => {
      orders
        .filter((o) => o.status !== OrderStatus.DRAFT && o.status !== OrderStatus.PENDING)
        .forEach((order) => {
          expect(order.confirmedAt).toBeTruthy();
          expect(order.confirmedByUserId).toBeTruthy();
        });
    });
  });

  describe('sessions mock data', () => {
    it('every session has valid SessionStatus', () => {
      sessions.forEach((session) => {
        expect(Object.values(SessionStatus)).toContain(session.status);
      });
    });

    it('every session has tenantId, id, tableId, tableName', () => {
      sessions.forEach((session) => {
        expect(session.tenantId).toBeTruthy();
        expect(session.id).toBeTruthy();
        expect(session.tableId).toBeTruthy();
        expect(session.tableName).toBeTruthy();
      });
    });

    it('orderCount matches actual orders for that session', () => {
      sessions.forEach((session) => {
        const actualCount = orders.filter((o) => o.sessionId === session.id).length;
        expect(session.orderCount).toBe(actualCount);
      });
    });

    it('CLOSED sessions have closedAt set', () => {
      sessions
        .filter((s) => s.status === SessionStatus.CLOSED)
        .forEach((session) => {
          expect(session.closedAt).toBeTruthy();
        });
    });
  });

  describe('bills mock data', () => {
    it('every bill references existing session', () => {
      const sessionIds = new Set(sessions.map((s) => s.id));
      bills.forEach((bill) => {
        expect(sessionIds.has(bill.sessionId)).toBe(true);
      });
    });

    it('every bill has valid BillStatus', () => {
      bills.forEach((bill) => {
        expect(Object.values(BillStatus)).toContain(bill.status);
      });
    });

    it('every bill orderId references existing order', () => {
      const orderIds = new Set(orders.map((o) => o.id));
      bills.forEach((bill) => {
        bill.orderIds.forEach((orderId) => {
          expect(orderIds.has(orderId)).toBe(true);
        });
      });
    });

    it('PAID bills have paidAt + paymentMethod set', () => {
      bills
        .filter((b) => b.status === BillStatus.PAID)
        .forEach((bill) => {
          expect(bill.paidAt).toBeTruthy();
          expect(bill.paymentMethod).toBeTruthy();
        });
    });

    it('total = subtotal + roundingAmount for every bill', () => {
      bills.forEach((bill) => {
        expect(bill.total).toBe(bill.subtotal + bill.roundingAmount);
      });
    });
  });

  describe('carts mock data', () => {
    it('every cart item has version >= 1', () => {
      carts.forEach((cart) => {
        cart.items.forEach((item) => {
          expect(item.version).toBeGreaterThanOrEqual(1);
        });
      });
    });

    it('every cart sessionId references existing session', () => {
      const sessionIds = new Set(sessions.map((s) => s.id));
      carts.forEach((cart) => {
        expect(sessionIds.has(cart.sessionId)).toBe(true);
      });
    });
  });

  describe('serviceRequests mock data', () => {
    it('every service request has valid type and status', () => {
      serviceRequests.forEach((req) => {
        expect(Object.values(ServiceRequestType)).toContain(req.type);
        expect(Object.values(ServiceRequestStatus)).toContain(req.status);
      });
    });

    it('ACKNOWLEDGED requests have acknowledgedAt + acknowledgedByUserId', () => {
      serviceRequests
        .filter((r) => r.status !== ServiceRequestStatus.PENDING)
        .forEach((req) => {
          expect(req.acknowledgedAt).toBeTruthy();
          expect(req.acknowledgedByUserId).toBeTruthy();
        });
    });

    it('RESOLVED requests have resolvedAt set', () => {
      serviceRequests
        .filter((r) => r.status === ServiceRequestStatus.RESOLVED)
        .forEach((req) => {
          expect(req.resolvedAt).toBeTruthy();
        });
    });
  });
});
```

- [ ] **Step 3: Run test**

```bash
npx nx test mock-data --testFile=data-conformance 2>&1 | tail -25
```

Expected: ~17 tests PASS.

Nếu test fail → check mock data files (Tasks 10-14) khớp với expected data layout.

---

## Task 20: Write Layer 2 — `tools/verify-step-2.3-types.sh`

**Files:** 1 create

**Goal:** Cross-project tsc verification script. Verify libraries PASS, customer-pwa FAIL với expected error count.

- [ ] **Step 1: Verify `tools/` directory exists**

```bash
ls tools/
```

Expected: directory exists với `seed.js`, `keycloak-bootstrap.sh`, etc.

- [ ] **Step 2: Create script**

File: `tools/verify-step-2.3-types.sh`

```bash
#!/usr/bin/env bash
# Step 2.3 — Shared Types verification script
# Verifies cross-project compilation expectations after Step 2.3 refactor.
#
# Usage: bash tools/verify-step-2.3-types.sh
# Exit 0: All expectations met
# Exit 1: Unexpected failure (regression)

set -e

echo ""
echo "============================================================"
echo "  Step 2.3 — Shared Types Verification (3-layer)"
echo "============================================================"
echo ""

# ─── Layer 2: Library compiles (must PASS) ─────────────
echo "→ Layer 2.1: Library builds..."

for lib in types mock-data constants; do
  echo "  • Building @einvoice/$lib..."
  if ! npx nx build "$lib" > /tmp/step-2.3-build-"$lib".log 2>&1; then
    echo "  ✗ FAIL: $lib build failed"
    cat /tmp/step-2.3-build-"$lib".log | tail -20
    exit 1
  fi
  echo "  ✓ $lib OK"
done

# ─── Layer 2: BE services compile (must PASS — zero refs) ─────
echo ""
echo "→ Layer 2.2: BE services tsc (expect PASS, zero refs)..."

BE_SERVICES=("bff" "catalog" "user-access" "authorizer" "invoice" "product" "saas")
for svc in "${BE_SERVICES[@]}"; do
  echo "  • Building $svc..."
  if ! npx nx build "$svc" > /tmp/step-2.3-build-"$svc".log 2>&1; then
    echo "  ✗ UNEXPECTED FAIL: $svc"
    cat /tmp/step-2.3-build-"$svc".log | tail -20
    exit 1
  fi
  echo "  ✓ $svc OK"
done

# ─── Layer 2: management-app compiles (must PASS — zero refs) ─
echo ""
echo "→ Layer 2.3: management-app tsc (expect PASS)..."
if ! npx nx build management-app > /tmp/step-2.3-build-management-app.log 2>&1; then
  echo "  ✗ UNEXPECTED FAIL: management-app"
  cat /tmp/step-2.3-build-management-app.log | tail -20
  exit 1
fi
echo "  ✓ management-app OK"

# ─── Layer 2: customer-pwa expected to FAIL with documented errors ───
echo ""
echo "→ Layer 2.4: customer-pwa tsc (expect FAIL with 5-20 errors in 8 files)..."

CUSTOMER_PWA_LOG=/tmp/step-2.3-build-customer-pwa.log
npx nx build customer-pwa > "$CUSTOMER_PWA_LOG" 2>&1 || true

# Count TS errors (exact format may vary by Nx version; grep both common patterns)
ERROR_COUNT=$(grep -cE "error TS[0-9]+" "$CUSTOMER_PWA_LOG" || echo "0")

if [ "$ERROR_COUNT" -lt 5 ]; then
  echo "  ✗ UNEXPECTED: customer-pwa errors = $ERROR_COUNT (expected 5-20)"
  echo "  → Either fewer breaks than expected, OR tsc output format changed."
  echo "  → Inspect log: $CUSTOMER_PWA_LOG"
  exit 1
fi

if [ "$ERROR_COUNT" -gt 20 ]; then
  echo "  ✗ UNEXPECTED: customer-pwa errors = $ERROR_COUNT (expected 5-20)"
  echo "  → More breaks than documented in spec §7.2 — investigate regression."
  echo "  → Inspect log: $CUSTOMER_PWA_LOG"
  exit 1
fi

echo "  ✓ customer-pwa errors = $ERROR_COUNT (within expected range 5-20)"

# Verify errors are in expected files only
EXPECTED_FILES=(
  "features/order/components/order-summary-card.tsx"
  "features/order/components/order-status-timeline.tsx"
  "features/order/components/order-items-list.tsx"
  "features/order/services/order.service.ts"
  "features/order/hooks/use-order-query.ts"
  "features/payment/components/payment-method-selector.tsx"
  "features/payment/components/payment-summary-card.tsx"
  "pages/request-payment-page.tsx"
)

UNEXPECTED_FILES=$(grep -E "error TS" "$CUSTOMER_PWA_LOG" | grep -oE "[a-zA-Z0-9_/-]+\.(ts|tsx)" | sort -u | grep -v -E "($(IFS='|'; echo "${EXPECTED_FILES[*]}"))" || true)

if [ -n "$UNEXPECTED_FILES" ]; then
  echo "  ✗ UNEXPECTED: errors trong files NGOÀI documented list:"
  echo "$UNEXPECTED_FILES" | sed 's/^/      /'
  echo "  → Spec §7.2 chỉ allow errors trong 8 files documented. Investigate regression."
  exit 1
fi

echo "  ✓ All errors confined to documented 8 files"

# ─── Layer 1: Unit tests ─────────────────────────────
echo ""
echo "→ Layer 1.1: types unit tests..."
if ! npx nx test types > /tmp/step-2.3-test-types.log 2>&1; then
  echo "  ✗ FAIL: types tests"
  cat /tmp/step-2.3-test-types.log | tail -20
  exit 1
fi
echo "  ✓ types tests PASS"

# ─── Layer 3: Mock data conformance ──────────────────
echo ""
echo "→ Layer 3: mock-data conformance tests..."
if ! npx nx test mock-data > /tmp/step-2.3-test-mock-data.log 2>&1; then
  echo "  ✗ FAIL: mock-data tests"
  cat /tmp/step-2.3-test-mock-data.log | tail -20
  exit 1
fi
echo "  ✓ mock-data tests PASS"

echo ""
echo "============================================================"
echo "  ✓ Step 2.3 VERIFICATION PASSED"
echo "============================================================"
echo ""
echo "Summary:"
echo "  • Library builds: types, mock-data, constants — PASS"
echo "  • BE services tsc — PASS (7 services)"
echo "  • management-app tsc — PASS"
echo "  • customer-pwa tsc — FAIL with $ERROR_COUNT errors (expected; fix Step 2.2)"
echo "  • Layer 1 unit tests (enums + transitions) — PASS"
echo "  • Layer 3 mock data conformance — PASS"
echo ""
```

- [ ] **Step 3: Make script executable**

```bash
chmod +x tools/verify-step-2.3-types.sh
```

---

## Task 21: Run Full 3-Layer Verification

**Files:** None (operational)

**Goal:** End-to-end run of all verifications.

- [ ] **Step 1: Run verification script**

```bash
bash tools/verify-step-2.3-types.sh
```

Expected: Exit 0 với output `✓ Step 2.3 VERIFICATION PASSED`.

Nếu fail tại bất kỳ step nào → đọc error output, debug theo:

- Library build fail → check Tasks 2-7 + 10-15
- BE service / management-app fail → check zero-refs assumption (`rg "from '@einvoice/types'"` trong file gặp lỗi)
- customer-pwa errors NGOÀI 8 files documented → có thể spec §5.2 audit thiếu, update spec
- Unit tests fail → check Tasks 17-19

- [ ] **Step 2: Manual verification — list customer-pwa errors**

```bash
npx nx build customer-pwa 2>&1 | grep -E "error TS" | head -20
```

Verify errors khớp expected per spec §7.2:

- `Order.paymentMethod` no longer exists
- `Order.paymentStatus` no longer exists
- lowercase status values not assignable to enum
- `PaymentMethod` chỉ còn `CASH`

- [ ] **Step 3: Verify zero PaymentStatus refs in codebase**

```bash
rg "PaymentStatus" libs/ apps/ --type ts
```

Expected: 0 matches (đã loại bỏ enum + constants).

---

## Task 22: Sync `docs/phases/phase-2a-order-kafka.md`

**Files:** 1 modify

**Goal:** Update Step 2.2 line 70 (UPPERCASE casing) + Step 2.3 lines 90-119 (full block replace).

- [ ] **Step 1: Update Step 2.2 line 70**

Open `docs/phases/phase-2a-order-kafka.md`. Find line ~70 (Step 2.2 section, Customer PWA — Cart & Ordering bullet "Order Tracking page"):

**Before:**

```
  - Order Tracking page: status timeline (Pending → Processing → Ready → Served)
```

**After:**

```
  - Order Tracking page: status timeline (PENDING → PROCESSING → READY → SERVED)
```

- [ ] **Step 2: Update Step 2.3 (lines 90-119)**

Locate Step 2.3 section. Replace the entire `**Yêu cầu cxhính:**` block (note typo "cxhính" — preserve as historical, focus on technical content) and the code block lines ~102-117.

**New content for Step 2.3 section:**

```markdown
### Step 2.3 — Shared types & hợp đồng realtime

**Mục tiêu:** Một nguồn sự thật cho FE/BE về đơn, dòng món, hóa đơn, phiên, giỏ, yêu cầu phục vụ và payload WebSocket — tránh drift khi Order Service và BFF phát sự kiện.

**Yêu cầu chính:**

- Enum `OrderStatus` với nhánh chính: **DRAFT → PENDING → PROCESSING → READY → SERVED → COMPLETED** và nhánh **CANCELED** (chuyển từ các trạng thái cho phép theo business-logic §8)
- `ServiceRequestType` và các type domain (drop I-prefix per Step 2.3 ADR): `Order`, `OrderItem`, `Bill`, `Session`, `CartItem`, `ServiceRequest`
- 4 status enums bổ sung: `OrderItemStatus`, `BillStatus`, `SessionStatus`, `ServiceRequestStatus` + `PaymentMethod` (CASH only Phase 2A)
- 3 transition matrices (`ALLOWED_ORDER_TRANSITIONS`, `ALLOWED_BILL_TRANSITIONS`, `ALLOWED_SERVICE_REQUEST_TRANSITIONS`) làm shared FE+BE state machine
- Định nghĩa tập **WebSocket event types** tương thích với BFF Direct (`order.created` → `OrderCreatedEvent`, `service.requested` → `ServiceRequestedEvent`, `OrderStatusChangedEvent`) + **Kafka payload type** (`OrderConfirmedEvent` cho topic `order.confirmed`) + data shape `KDSTicket` cho UI render

**Chi tiết field các entity/type:**
```

Order { id, tenantId, tableId, tableName, sessionId, items: OrderItem[], status, totalAmount, idempotencyKey,
notes?, confirmedAt?, confirmedByUserId?, cancelledAt?, cancelledByUserId?, cancelReason?, createdAt, updatedAt }
OrderItem { id, orderId, menuItemId, menuItemName, quantity, unitPrice, note?, status, createdAt, updatedAt }
Bill { id, tenantId, sessionId, orderIds[], subtotal, total, roundingAmount, paymentMethod?, status, closedAt?, paidAt?, createdAt, updatedAt }
Session { id, tenantId, tableId, tableName, status, startedAt, lastActivity, closedAt?, orderCount }
CartItem { menuItemId, menuItemName, quantity, unitPrice, note?, version }
ServiceRequest { id, tenantId, tableId, sessionId, type, status, note?, acknowledgedAt?, acknowledgedByUserId?, resolvedAt?, createdAt, updatedAt }

Enums:
OrderStatus { DRAFT, PENDING, PROCESSING, READY, SERVED, COMPLETED, CANCELED }
OrderItemStatus { PROCESSING, READY, SERVED, CANCELED }
BillStatus { OPEN, PENDING_PAYMENT, PAID }
SessionStatus { ACTIVE, CLOSED }
ServiceRequestType { CALL_STAFF, REQUEST_BILL, GENERAL_HELP }
ServiceRequestStatus { PENDING, ACKNOWLEDGED, RESOLVED }
PaymentMethod { CASH } // Phase 3 sẽ thêm CARD, MOMO, ZALOPAY, BANK_TRANSFER

Realtime/Event Types:
OrderCreatedEvent { tenantId, orderId, tableId, tableName, sessionId, items, totalAmount, timestamp } // BFF Direct
OrderStatusChangedEvent { tenantId, orderId, fromStatus, toStatus, changedByUserId?, timestamp } // BFF Direct
ServiceRequestedEvent { tenantId, requestId, tableId, tableName, sessionId, type, note?, timestamp } // BFF Direct
OrderConfirmedEvent { tenantId, orderId, sessionId, items, totalAmount, confirmedAt, confirmedByUserId } // Kafka order.confirmed
KDSTicket { ticketId, tenantId, orderId, tableId, tableName, items, priority, createdAt, slaSeconds } // Data shape

State Machine Matrices (encoded trong types lib, shared FE+BE):
ALLOWED_ORDER_TRANSITIONS, ALLOWED_BILL_TRANSITIONS, ALLOWED_SERVICE_REQUEST_TRANSITIONS

```

**Verify:** Frontend và backend compile/import cùng bộ type; transition matrices import được; contract event có thể liệt kê trong review (tên event + payload tối thiểu)

> **Spec & Plan reference:** [`docs/superpowers/specs/2026-04-19-step-2.3-shared-types-design.md`](../superpowers/specs/2026-04-19-step-2.3-shared-types-design.md)
```

---

## Task 23: Sync Other Doc Updates (4 docs)

**Files:** 4 modify

**Goal:** Sync remaining docs để loại bỏ type drift.

- [ ] **Step 1: Update `docs/phases/phase-4a-saga-hardening.md` line 84**

Find line:

```
| Delete constraints       | Không xóa **Category** còn **MenuItem**; không xóa **MenuItem** còn **OrderItem** đang active (status IN Pending, Processing, Ready) | ...
```

Replace với:

```
| Delete constraints       | Không xóa **Category** còn **MenuItem**; không xóa **MenuItem** còn **OrderItem** đang active (status IN PROCESSING, READY) | ...
```

> **Note**: removed `Pending` because OrderItemStatus không có PENDING value (4 values: PROCESSING, READY, SERVED, CANCELED).

- [ ] **Step 2: Update `docs/phases/phase-3-payment.md` lines 51-55**

Find:

```
**Shared types (thư viện dùng chung):** `IPayment`, enum `IPaymentMethod`, `IRefund`, `IBillFinal` (và các field cần thiết để rounding / trạng thái / tham chiếu Stripe) — phản ánh semantics nghiệp vụ, không dư thừa field Order.
```

Replace với:

```
**Shared types (thư viện dùng chung):** `Payment`, `PaymentMethod` (enum đã defined ở Step 2.3 với CASH only — Phase 3 EXTEND thêm `CARD, MOMO, ZALOPAY, BANK_TRANSFER`), `Refund`, và mở rộng `Bill` (đã defined Step 2.3) với các field cần thiết để rounding / paid amount / change / Stripe reference — phản ánh semantics nghiệp vụ, không dư thừa field Order. Drop I-prefix per Step 2.3 ADR.
```

- [ ] **Step 3: Update `docs/phases/phase-1-catalog.md` (add convention note)**

Find first heading after the title (likely `## Mục tiêu` or `## Step ...`). Insert BEFORE first heading:

```markdown
> **Convention note (post-Step 2.3, 2026-04-19):** Phase 1 docs còn dùng I-prefix cho interface (vd `ICategory`, `IMenuItem`).
> Sau Step 2.3, convention dropped I-prefix per ADR ([`docs/superpowers/specs/2026-04-19-step-2.3-shared-types-design.md`](../superpowers/specs/2026-04-19-step-2.3-shared-types-design.md)).
> Phase 1 docs giữ historical naming as-is; code và types lib đã follow convention mới (`Category`, `MenuItem`).
```

- [ ] **Step 4: Verify + update `docs/architecture/erd_explanation.md`**

Read file:

```bash
cat docs/architecture/erd_explanation.md | head -60
```

Check for:

- References to `Order.paymentMethod` / `Order.paymentStatus` → update to `Bill.paymentMethod` / `Bill.status`
- Lowercase status values (`'pending'`, `'confirmed'`) → update UPPERCASE
- I-prefix type names → drop I-prefix

If no relevant references found → no change needed. Document the no-op:

```bash
echo "  ✓ erd_explanation.md verified — no Order/Bill type references requiring update" >> /tmp/step-2.3-doc-sync.log
```

If updates needed → manually edit per the issues above.

---

## Task 24: Final Acceptance Criteria Verification

**Files:** None (operational)

**Goal:** Run all verification one more time + check spec acceptance criteria checklist.

- [ ] **Step 1: Run full verification script**

```bash
bash tools/verify-step-2.3-types.sh
```

Expected: `✓ Step 2.3 VERIFICATION PASSED`.

- [ ] **Step 2: Verify all spec §9 acceptance criteria**

Open `docs/superpowers/specs/2026-04-19-step-2.3-shared-types-design.md` §9. Check off each item:

**§9.1 Functional completeness (12 items):**

- [ ] 6 entities defined ✓
- [ ] 6 enums UPPERCASE ✓
- [ ] 5 event/data types ✓
- [ ] 3 transition matrices `as const` ✓
- [ ] All exports added to barrel ✓
- [ ] `types.ts` deleted ✓
- [ ] `status.ts` refactored ✓
- [ ] `PaymentStatus` REMOVED ✓ (verify with `rg "PaymentStatus" libs/ apps/`)
- [ ] mock-data files updated/created ✓
- [ ] `helpers.ts` 4 new helpers ✓
- [ ] `orders.ts` 4 orders status mix ✓
- [ ] `bills.ts` 3 bills status mix ✓
- [ ] `service-requests.ts` 3 requests status mix ✓

**§9.2 Quality gates (8 items):**

- [ ] Layer 1 enum-completeness tests green ✓
- [ ] Layer 1 transitions tests green ✓
- [ ] Layer 3 data-conformance tests green ✓
- [ ] types build PASS ✓
- [ ] mock-data build PASS ✓
- [ ] constants build PASS ✓
- [ ] BE services + management-app build PASS ✓
- [ ] customer-pwa FAIL with errors in 8 documented files only ✓
- [ ] verify-step-2.3-types.sh exit 0 ✓

**§9.3 Documentation sync (7 items):**

- [ ] phase-2a Step 2.2 + Step 2.3 updated ✓
- [ ] phase-4a line 84 UPPERCASE ✓
- [ ] phase-3 drop I-prefix + EXTEND note ✓
- [ ] phase-1 convention note ✓
- [ ] erd_explanation verified ✓
- [ ] spec file created ✓
- [ ] plan file created (THIS FILE) ✓

**§9.4 Hand-off readiness (4 items):**

- [ ] Step 2.2 ready (types frozen) ✓
- [ ] Step 2.4 ready (parallel work) ✓
- [ ] Phase 2B consumer interface clear (`OrderConfirmedEvent`) ✓
- [ ] Phase 3 extension points clear ✓

- [ ] **Step 3: Lint + format check**

```bash
npx nx lint types --fix
npx nx lint mock-data --fix
npx nx lint constants --fix
```

Expected: 0 errors. Auto-fixed where possible.

- [ ] **Step 4: Print final summary**

```bash
echo ""
echo "============================================================"
echo "  Step 2.3 IMPLEMENTATION COMPLETE"
echo "============================================================"
echo ""
echo "Code changes:"
echo "  • libs/shared/types/: 5 files (1 modified, 4 new) + 1 deleted"
echo "  • libs/shared/types/src/lib/__tests__/: 2 new test files"
echo "  • libs/shared/mock-data/: 6 files (2 modified, 3 new, 1 helpers extended)"
echo "  • libs/shared/mock-data/src/lib/__tests__/: 1 new test file"
echo "  • libs/shared/constants/: 1 file modified"
echo "  • tools/: 1 new verification script"
echo ""
echo "Doc updates:"
echo "  • docs/phases/phase-2a-order-kafka.md (Step 2.2 + 2.3)"
echo "  • docs/phases/phase-4a-saga-hardening.md (line 84)"
echo "  • docs/phases/phase-3-payment.md (lines 51-55)"
echo "  • docs/phases/phase-1-catalog.md (convention note)"
echo "  • docs/architecture/erd_explanation.md (verified)"
echo "  • docs/superpowers/specs/2026-04-19-step-2.3-shared-types-design.md (NEW)"
echo "  • docs/superpowers/plans/2026-04-19-step-2.3-shared-types.md (NEW)"
echo ""
echo "Verification:"
echo "  • Layer 1 unit tests (enum + transitions): PASS"
echo "  • Layer 2 cross-tsc verification: PASS (customer-pwa breaks confined)"
echo "  • Layer 3 mock data conformance: PASS"
echo ""
echo "Next steps:"
echo "  → Step 2.2: Brainstorm + spec + plan for Mock UI rebuild (uses types from this step)"
echo "  → Step 2.4: Brainstorm + spec + plan for Order Service backend (parallel with 2.2)"
echo ""
```

---

## Rollback Procedure

Nếu Step 2.3 cần rollback (vd: critical regression không debug được trong session):

### Rollback Steps

```bash
# Option A: Hard reset to backup branch (if not pushed)
git checkout backup/pre-step-2.3-<timestamp>
git checkout -b main-rollback
git checkout main
git reset --hard main-rollback
git branch -D main-rollback

# Option B: Revert commits (if pushed)
git log --oneline | grep -E "step.2.3|shared.types"
git revert <commit_hash_n>...<commit_hash_1>
```

### Post-Rollback Verification

```bash
# Verify pre-Step 2.3 state restored
npx nx run-many --target=build --projects=types,mock-data,constants,customer-pwa,management-app
# Expected: All 5 PASS (baseline pre-Step 2.3 state)
```

### Post-Mortem

- [ ] KEEP spec + plan files (do NOT revert) — historical ADR for re-attempt
- [ ] Document blockers gặp phải trong post-mortem doc:

  ```bash
  cat > docs/superpowers/postmortems/$(date +%Y-%m-%d)-step-2.3-rollback.md <<EOF
  # Step 2.3 Rollback Post-Mortem — $(date +%Y-%m-%d)

  ## What happened
  <describe symptoms>

  ## Root cause
  <if known>

  ## Action items
  - [ ] Fix root cause
  - [ ] Update spec §X with new constraint
  - [ ] Re-run plan
  EOF
  ```

---

## Glossary

- **BFF Direct**: Backend-for-Frontend pattern; BFF directly emits WebSocket events without Kafka (Anti-Pattern AP1 cho live UI updates)
- **Kafka Producer**: Order Service publishes to topic `order.confirmed`; consumers subscribe per consumer group
- **Optimistic locking**: cart `version` field; client sends version match before update; conflict → BE error → client refresh + retry
- **Denormalized field**: vd `tableName` trong `Order` (duplicates data từ Table entity) để tránh JOIN khi render UI
- **Audit field**: vd `confirmedAt`, `confirmedByUserId` để track state changes cho compliance + Phase 4A saga compensation
- **Idempotency key**: UUID v4 generated by FE for order submit; BE dedup based on this key (prevent double-submit từ retry)
- **Transition matrix**: `Record<Status, readonly Status[]>` defining allowed FROM → TO transitions; encoded `as const` for type narrowing
- **MockSessionExtended**: extension of `Session` core type với mock-only fields (qrToken, restaurantName) cho UI mock convenience
