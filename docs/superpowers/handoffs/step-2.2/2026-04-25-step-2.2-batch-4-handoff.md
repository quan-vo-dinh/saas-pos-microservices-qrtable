# Step 2.2 — Batch 4 handoff (POS density + KDS core)

**Date:** 2026-04-25  
**Branch:** `main`  
**Scope:** Plan Tasks **22–28** (notifications + tenant switcher, POS tab sync / service inspector, KDS layout through ticket sheet + DnD). Mapping aligns with execution plan numbering; Batch 3 had already delivered Tasks 15–21 (tables, ⌘K, KPI charts in sidebar).

---

## SESSION SUMMARY

### Tiến độ

| Task   | Nội dung                                                                                                                                                                                                    | Trạng thái |
| ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| **22** | `NotificationsPopover` (Tabs All / Đơn / DV, HoverCard, ScrollArea) trên topbar khi `pathname` bắt đầu bằng `/pos`; `TenantSwitcherPopover` (Command + 3 tenant mock) thay dropdown sidebar                 | Hoàn thành |
| **23** | `PosAppShell`: `useEffect` theo `pathname` để **xoá selection** không thuộc tab hiện tại (`selectRow` / `selectTable` / `selectBill` / `selectServiceRequest`); `ResizablePanelGroup` giữ nguyên từ Batch 3 | Hoàn thành |
| **24** | `(kds)/layout.tsx`: bỏ `AppShell`, full-bleed `min-h-screen` + `data-surface="kds"`; điều hướng qua header KDS                                                                                              | Hoàn thành |
| **25** | `KdsHeader`: đồng hồ, stats, switch Bếp/Bar, Recall + Settings stub, link về POS                                                                                                                            | Hoàn thành |
| **26** | `KdsColumn` (droppable) + `KdsTicketCard` (SLA band + glyph + kéo Grip, long-press Xong, checkbox món)                                                                                                      | Hoàn thành |
| **27** | `KdsDndWrapper` (`@dnd-kit/core`, `closestCorners`, `DragOverlay`, `setKdsTicketColumn`)                                                                                                                    | Hoàn thành |
| **28** | `KdsTicketSheet`: bảng món + Select trạng thái, timeline mock, **RadialBarChart** SLA, Ready / Recall / AlertDialog hết hàng, HoverCard order                                                               | Hoàn thành |

**Bổ sung POS (đồng bộ tab / density)**

- `ServiceRequestDetailPanel`: panel phải khi chọn dòng inbox + **AreaChart** mock mật độ.
- `service-request-table.tsx`: click hàng → `selectServiceRequest`.
- `pos-right-inspector.tsx`: route service-requests hiển thị detail thay vì empty state.
- `store.ts`: `selectedServiceRequestId`, `selectServiceRequest`, `activeMockTenantName`, `setActiveMockTenantName`, `setKdsTicketColumn`, `updateKdsTicketItemStatus`.

### Blockers

- **Không có.** MCP Database/Keycloak không được gọi trong session này (không có server MCP trong môi trường agent); mapping trạng thái bàn / nhân viên vẫn theo **Zustand mock** + `@einvoice/types` như Batch 1–3.

### Dependencies mới

| Gói                  | Vị trí                                                         |
| -------------------- | -------------------------------------------------------------- |
| `@dnd-kit/core`      | `apps/management-app/package.json`                             |
| `@dnd-kit/utilities` | `apps/management-app/package.json` (CSS.Translate cho thẻ kéo) |

`cmdk` đã có sẵn (Command palette + tenant Command). `recharts` đã có (sidebar charts Batch 3 + Area/Radial trong Batch 4).

### Key exports / paths

| Thành phần                       | Đường dẫn                                                                                                |
| -------------------------------- | -------------------------------------------------------------------------------------------------------- |
| Thông báo POS                    | `apps/management-app/src/components/pos/notifications-popover.tsx`                                       |
| Tenant mock                      | `apps/management-app/src/components/layout/tenant-switcher-popover.tsx`                                  |
| Chi tiết yêu cầu + chart         | `apps/management-app/src/components/pos/service-request-detail-panel.tsx`                                |
| KDS board                        | `apps/management-app/src/components/kds/kds-board.tsx`                                                   |
| Header / cột / thẻ / DnD / sheet | `kds-header.tsx`, `kds-column.tsx`, `kds-ticket-card.tsx`, `kds-dnd-wrapper.tsx`, `kds-ticket-sheet.tsx` |
| Store mở rộng                    | `apps/management-app/src/mocks/store.ts`                                                                 |

### Lưu ý cho Batch cuối (KDS & Perf — Tasks 29–33+)

1. **KDS recall/settings đầy đủ:** Task 30 `station-settings-popover` + persist; recall log sheet có thể thay stub trong `KdsBoard`.
2. **Recharts SSG:** Build vẫn log `width(-1) height(-1)` khi prerender; cân nhắc `dynamic(..., { ssr: false })` cho chart hoặc chỉ render sau `useEffect` mount (đã ghi ở Batch 3 handoff).
3. **Perf / virtualize:** Task 33 `@tanstack/react-virtual` cho live orders khi > 50 dòng.
4. **A11y / keyboard KDS:** Task 32 phím tắt toàn board.
5. **RBAC KDS:** `KdsBoard` cho phép **không có role** (`roles.length === 0`) để dev/demo; production nên thắt lại theo session thật.
6. **Giữ import paths:** Chỉ dùng alias đã có (`@/`, `@einvoice/*`, mocks đã thiết lập).

---

## Verification (Batch 4)

```bash
pnpm --dir apps/management-app exec tsc --noEmit
pnpm nx run management-app:lint
pnpm nx run management-app:build
```

---

## END SUMMARY
