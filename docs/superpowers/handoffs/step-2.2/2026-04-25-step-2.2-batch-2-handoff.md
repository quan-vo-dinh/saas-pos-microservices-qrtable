# Step 2.2 — Batch 2 handoff (PWA Tasks 8–14)

**Date:** 2026-04-25  
**Branch:** `main`  
**Scope:** Tasks 8–14 from `docs/superpowers/plans/2026-04-24-step-2.2-mock-ui.md`

---

## Progress

| Task   | Summary                                         | Status                                                                                                                               |
| ------ | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| **8**  | `data-surface="pwa"` + `pwa-tokens.css` @import | Already satisfied in Batch 1; verified unchanged                                                                                     |
| **9**  | `SessionTimer`, `PresenceAvatars`               | Added under `apps/customer-pwa/src/components/session/`; wired on `landing-page.tsx`                                                 |
| **10** | `MenuItemCard`, `MenuItemDialog`; menu grid     | `src/components/menu/*`; `menu-page.tsx` refactored to **Zustand mock** menu + cards + dialog                                        |
| **11** | Cart drawer, Cart pill, submit → tracking       | `src/pages/cart/cart-drawer.tsx`, `src/components/menu/cart-pill.tsx`; vaul `Drawer`, confetti (`framer-motion`), idempotency helper |
| **12** | Order stepper + journey + tracking page         | `src/components/order/order-tracking-stepper.tsx`, `order-journey-sheet.tsx`; `order-tracking-page.tsx` uses store `order`           |
| **13** | Service request drawer + global FAB             | `src/pages/service-request-drawer.tsx`; FAB in `mobile-shell.tsx`; store flag `serviceRequestOpen`                                   |
| **14** | Bill lock alert + disable add                   | `menu-page.tsx` `Alert` + `MenuItemCard` `disabled`; `window.__mock__.toggleBillLock` unchanged from Batch 1                         |

---

## New / touched paths (customer-pwa)

- `src/lib/idempotency.ts`
- `src/components/session/session-timer.tsx`, `presence-avatars.tsx`
- `src/components/menu/menu-item-card.tsx`, `menu-item-dialog.tsx`, `cart-pill.tsx`
- `src/components/order/order-tracking-stepper.tsx`, `order-journey-sheet.tsx`
- `src/pages/cart/cart-drawer.tsx`, `service-request-drawer.tsx`
- `src/pages/menu-page.tsx`, `landing-page.tsx`, `order-tracking-page.tsx`
- `src/components/layout/mobile-shell.tsx`
- `src/mocks/store.ts` (`serviceRequestOpen`, `setServiceRequestOpen`, `advanceOrderStatus` timestamps)
- `src/features/cart/context/cart-provider.tsx` (`version`, `bumpVersion` — legacy Context cart)
- `src/App.tsx` (type-only import cleanup)
- **shadcn CLI:** `src/components/ui/hover-card.tsx`, `alert.tsx`, `sheet.tsx`, `button.tsx`
- **deps:** `framer-motion` on `customer-pwa`

---

## Deviations / tech debt

1. **Task 13 RHF + zod:** `@hookform/resolvers` + **Zod 4** resolver mismatch in this workspace → service form implemented with **controlled state + `maxLength`/toast** (same UX, simpler types). Batch 3 can reintroduce RHF when resolver stack is aligned.
2. **Menu data source:** `/menu` no longer uses `useFullMenuQuery` / `features/menu/*` grid; old components remain for Step 2.5 wiring reference.
3. **Auto session on menu:** `MenuPage` calls `startSession` from **mock seed** when Context session inactive so nav + tracking work without QR in dev.
4. **`usePwaFakeRealtime`:** mounted once in **`MobileShell`** (avoids duplicate intervals vs per-page).
5. **Alert variant:** local shadcn `Alert` has `default` \| `destructive` only — bill lock uses **`default` + amber border** classes (no `warning` variant in generated file).

---

## Internal review (prep for Batch 3 — POS Tasks 15+)

- **Spec gaps to watch:** POS live orders will need the same pattern: single fake-realtime mount in layout, TanStack Table + store selectors.
- **Quality:** Run `pnpm nx run customer-pwa:typecheck` + `lint` + `build` after Batch 3 edits; keep Lucide-only icons (no emoji affordances).
- **Imports:** PWA continues `@einvoice/frontend-ui` + `@/components/ui/*` for CLI-added primitives; do not invent `@qrtable/*` paths.

---

## Verification (this batch)

```bash
pnpm nx run customer-pwa:typecheck
pnpm nx run customer-pwa:lint
pnpm nx run customer-pwa:build
```

---

## END SUMMARY
