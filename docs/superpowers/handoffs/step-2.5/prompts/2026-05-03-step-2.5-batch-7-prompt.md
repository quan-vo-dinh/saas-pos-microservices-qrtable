# Prompt — Step 2.5 Batch 7: POS Tables Real Integration

You are implementing Step 2.5 Batch 7 for the QRTable Nx/NestJS monorepo.

## Mission

Close the `/pos/tables` frontend integration gap left after Step 2.5 Batch 6:

```txt
Management App /pos/tables
→ real Catalog-backed table map
→ real selected table detail
→ real active order summary by table
→ safe transfer using backend session/table ids
→ mark clean through Catalog status API
```

Do not expand this into payment, KDS, notification history, or analytics work.

## Required Inputs

Input from Batch 6 is required. Read the Batch 6 final output before editing.

Before editing, read:

- `AGENTS.md`
- `docs/superpowers/handoffs/step-2.5/2026-05-01-step-2.5-batch-6-handoff.md`
- `docs/superpowers/handoffs/step-2.5/2026-05-03-step-2.5-batch-7-handoff.md`
- `docs/superpowers/plans/2026-05-03-step-2.5-batch-7-pos-tables-real-integration-plan.md`
- `docs/superpowers/plans/step-2.5-fe-be-integration-plan.md`
- `docs/superpowers/specs/2026-04-30-step-2.5-fe-be-integration-design.md`
- `docs/superpowers/plans/2026-04-24-step-2.2-mock-ui.md`
- `docs/phases/phase-2a-order-kafka.md`
- `docs/business-logic-step-2.4-spec.vi.md`
- `docs/technical-architecture.md`
- `apps/management-app/src/components/pos/table-map-grid.tsx`
- `apps/management-app/src/components/pos/table-detail-panel.tsx`
- `apps/management-app/src/components/pos/non-order-right-inspector.tsx`
- `apps/management-app/src/components/pos/non-order-route-reset.tsx`
- `apps/management-app/src/components/pos/transfer-table-dialog.tsx`
- `apps/management-app/src/features/tables/hooks/use-tables-query.ts`
- `apps/management-app/src/features/tables/hooks/use-tables-mutations.ts`
- `apps/management-app/src/features/order/hooks/use-order-query.ts`
- `apps/management-app/src/features/order/services/order.service.ts`

## Implementation Rules

- Work on `main`.
- Do not commit.
- Do not modify backend code unless an existing documented endpoint is missing
  and blocks the batch. Stop and ask before adding any new backend route.
- Do not implement cash payment confirmation.
- Do not integrate KDS with real backend.
- Do not add new permissions.
- Do not add automatic mock fallback when Catalog/Order APIs fail.
- Do not invent `sessionId`, `fromTableId`, or `toTableId`.
- `/pos/bills` remains intentionally mock/out-of-scope for Phase 3.
- `/pos/payment` remains Phase 3 placeholder.
- Notifications/mini charts may remain mock/deferred unless directly required
  by `/pos/tables`.

## Expected Work

1. Create `usePosTableUiState` in
   `apps/management-app/src/features/tables/hooks/use-pos-table-ui-state.ts`.
2. Add a focused store test for selected table set/clear.
3. Update `NonOrderRouteReset` so table selection clears through
   `usePosTableUiState`, while bill/service selected ids remain unchanged.
4. Update `NonOrderRightInspector` so `/pos/tables` reads selected table id from
   `usePosTableUiState`.
5. Replace `TableMapGrid` table source with `useTablesQuery()`.
6. Replace table-map mock live order totals with `useOrdersQuery({ limit: 200 })`
   and terminal-status filtering.
7. Remove fake presence rendering from `TableMapGrid`.
8. Add loading/error/empty states to `TableMapGrid`.
9. Replace `TableDetailPanel` table source with `useTablesQuery()`.
10. Replace detail mock orders with `useOrdersQuery({ tableId, limit: 50 })`.
11. Remove fake presence and fixed fake timeline from `TableDetailPanel`.
12. Wire transfer with real `fromTableId={table.id}` and
    `sessionId={table.sessionId}`.
13. Keep transfer disabled when the selected table has no `sessionId`.
14. Remove mock `Đóng phiên` state mutation because session close belongs to
    Phase 3/payment lifecycle.
15. Wire `Đánh dấu sạch` to `useUpdateTableStatusMutation()` with
    `{ id: table.id, status: 'available' }` only when table status is
    `cleaning`.
16. Add component tests for `TableMapGrid` and `TableDetailPanel`.
17. Append a Batch 7 addendum and deferred tech debt list to
    `docs/superpowers/plans/step-2.5-fe-be-integration-plan.md`.
18. Verify no `useMockStore` import remains in:
    - `apps/management-app/src/components/pos/table-map-grid.tsx`
    - `apps/management-app/src/components/pos/table-detail-panel.tsx`

## Required Verification

Run the strongest valid subset:

```bash
npx nx test management-app --testPathPattern=table-map-grid.spec.tsx
npx nx test management-app --testPathPattern=table-detail-panel.spec.tsx
npx nx test management-app --testPathPattern=use-pos-table-ui-state.spec.ts
npx nx test management-app
npx nx lint management-app
```

If a target fails because of local Nx/Jest setup rather than this batch, record
the exact error and run the strongest equivalent command, such as:

```bash
pnpm --dir apps/management-app build
```

Manual demo checklist:

1. Open `http://localhost:3000/pos/tables`.
2. Confirm table names/statuses match `http://localhost:3000/dashboard/tables`.
3. Select a table and confirm the right inspector shows real table detail.
4. Submit a PWA order for a table and confirm `/pos/tables` shows active order
   summary after polling/refetch.
5. For a table with real `sessionId`, open transfer and confirm destination
   tables are real available Catalog tables.
6. Confirm missing `sessionId` keeps transfer unavailable.
7. Confirm mark-clean works only for `cleaning` tables.
8. Confirm `/pos/bills` still remains mock/out-of-scope.
9. Confirm `/pos`, `/pos/service-requests`, and Batch 6 QR dialog still load.

## Required Batch Output

End with the final Batch 7 summary containing:

- files changed,
- selected table UI state store name,
- real query hooks used by `/pos/tables`,
- transfer behavior and disabled cases,
- mark-clean behavior,
- mock dependencies intentionally left for Bills/KDS/notifications/analytics,
- automated command results,
- manual demo result or blockers,
- follow-up tech debt.

Stop and ask if real table DTOs do not include the fields required for transfer,
or if implementing the requested UX would require payment/session-close backend
logic.
