# Prompt — Step 2.5 Batch 6: QR Demo, Dynamic Tenant, Session Rejoin

You are implementing Step 2.5 Batch 6 for the QRTable Nx/NestJS monorepo.

## Mission

Make the QR demo real and tenant-dynamic after Step 2.5 Batch 5:

```txt
Management App renders a real table QR
→ QR URL contains tenant slug, table id, token
→ Customer PWA resolves tenant slug
→ QR validates and joins session
→ repeated scans join the same active table session
→ regenerate invalidates old QR URLs
```

## Required Inputs

Input from Batch 5 is required. Read the Batch 5 final output before editing.

Before editing, read:

- `AGENTS.md`
- `docs/superpowers/handoffs/step-2.5/2026-04-30-step-2.5-batch-5-handoff.md`
- `docs/superpowers/handoffs/step-2.5/2026-05-01-step-2.5-batch-6-handoff.md`
- `docs/superpowers/specs/2026-05-01-step-2.5-batch-6-qr-demo-dynamic-tenant-design.md`
- `docs/superpowers/plans/2026-05-01-step-2.5-batch-6-qr-demo-dynamic-tenant-plan.md`
- `docs/superpowers/plans/step-2.5-fe-be-integration-plan.md`
- `docs/phases/phase-1-catalog.md`
- `docs/phases/phase-2a-order-kafka.md`
- `apps/saas/src/services/saas.service.ts`
- `apps/catalog/src/app/modules/table/services/table.service.ts`
- `apps/order/src/app/modules/order/services/order.service.ts`
- `apps/management-app/src/features/tables/components/qr-code-dialog.tsx`
- `apps/customer-pwa/src/features/landing/components/qr-landing-card.tsx`

## Implementation Rules

- Work on `main`.
- Do not commit.
- Do not integrate KDS with real backend.
- Do not implement cash payment confirmation.
- Do not add subdomain routing in this batch.
- Do not upload QR images to Cloudinary.
- Do not add bulk PDF export.
- Do not add a PWA camera scanner.
- Do not add new permissions.
- QR URL must expose tenant slug, not internal tenant id.
- `GET /public/tenants/:slug` must be public and return only public metadata.
- `GET /admin/tenant/current` must be protected and use `CATALOG_GET_LIST`.
- Current dev/auth/catalog seed uses internal tenant UUID
  `023772bb-391b-401c-936a-ed7034b69cec` and public slug `pho-viet`.
  Do not pass legacy string keys into SaaS `GET_BY_ID`; only UUID-shaped values
  should use `GET_BY_ID`. Legacy/public keys must resolve through `GET_BY_SLUG`.
- Local/dev SaaS seed must include an active `tenants` row with
  `id = '023772bb-391b-401c-936a-ed7034b69cec'` and `slug = 'pho-viet'`.
- Regenerate QR must invalidate old QR URLs for future scans.
- Repeated scans of an occupied table with active session must return the same
  session.

## Expected Work

1. Add SaaS TCP `GET_BY_SLUG` contract and implementation.
2. Add BFF `GET /public/tenants/:slug`.
3. Add BFF `GET /admin/tenant/current`.
4. Change Catalog QR token generation to opaque random stored token.
5. Change Catalog QR validation to compare against `table.qrToken`.
6. Change Order join session behavior:
   - available table creates new session,
   - occupied table with active session returns existing session,
   - billing/cleaning states reject clearly.
7. Install `qrcode.react` for `management-app`.
8. Add Management App current tenant service/hook.
9. Add Management App QR URL builder.
10. Replace QR placeholder dialog with real QR, copy/open/download/print, and
    regenerate.
11. Add Customer PWA tenant resolver service/hook.
12. Update Customer PWA landing flow to resolve `tenant` before QR validation and
    session join.
13. Preserve `VITE_TENANT_ID` fallback only for legacy dev URLs without
    `tenant`.
14. Add focused tests for backend contracts, QR token behavior, QR URL builder,
    tenant resolver flow, and Order session rejoin. If a UI component lacks a
    usable test harness, add pure helper/service tests and report the skipped
    component coverage with the reason.

## Required Verification

Run the strongest valid subset:

```bash
npx nx test saas
npx nx test catalog
npx nx test order
npx nx test bff
npx nx test management-app
npx nx test customer-pwa
npx nx lint management-app
npx nx lint customer-pwa
```

If any target is missing, record exact error and replacement command.

Manual demo checklist:

1. Management App table QR dialog displays a real QR.
2. QR URL contains `tenant`, `table`, and `token`.
3. Opening/scanning QR starts Customer PWA tenant resolution.
4. PWA validates QR and joins session.
5. A second browser/device joins the same active session for occupied table.
6. Regenerate QR changes token.
7. Old QR URL fails validation for future scans.
8. New QR URL works.

## Required Batch Output

End with the final Batch 6 summary containing:

- public/current tenant endpoint paths,
- QR token generation and validation behavior,
- Order join behavior for available/occupied/billing/cleaning tables,
- Management App QR URL builder and QR dialog behavior,
- Customer PWA tenant resolver and fallback behavior,
- installed package and lockfile status,
- automated command results,
- manual demo result or blockers,
- follow-up backlog items.

Stop and ask if tenant slug data is unavailable, public resolver cannot be safely
exposed, or Order cannot safely return existing active sessions for occupied
tables.
