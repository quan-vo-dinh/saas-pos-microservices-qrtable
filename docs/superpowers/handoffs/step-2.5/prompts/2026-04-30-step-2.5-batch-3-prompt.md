# Prompt — Step 2.5 Batch 3: Customer PWA Order, Service, Bill, Realtime

You are implementing Step 2.5 Batch 3 for the QRTable Nx/NestJS monorepo.

## Mission

Complete Customer PWA real ordering after Batch 2 has session/cart:

```txt
submit order
→ track order
→ create service request
→ request bill
→ refetch from Socket.IO hints
```

## Required Inputs

Input from Batch 2 is required. Read the Batch 2 final output before editing.

Before editing, read:

- `AGENTS.md`
- `docs/superpowers/handoffs/step-2.5/2026-04-30-step-2.5-batch-2-handoff.md`
- `docs/superpowers/handoffs/step-2.5/2026-04-30-step-2.5-batch-3-handoff.md`
- `docs/superpowers/specs/2026-04-30-step-2.5-fe-be-integration-design.md`
- `docs/superpowers/plans/step-2.5-fe-be-integration-plan.md`
- `docs/superpowers/plans/2026-04-24-step-2.2-mock-ui.md`
- `apps/bff/src/app/modules/realtime/gateways/order-events.gateway.ts`
- `apps/bff/src/app/modules/realtime/services/realtime-events.service.ts`
- `apps/customer-pwa/src/features/order/services/order.service.ts`
- `apps/customer-pwa/src/features/order/hooks/use-order-query.ts`
- `apps/customer-pwa/src/pages/cart/cart-drawer.tsx`
- `apps/customer-pwa/src/pages/order-tracking-page.tsx`
- `apps/customer-pwa/src/pages/service-request-drawer.tsx`

## Implementation Rules

- Work on `main`.
- Do not commit.
- Do not modify Management App.
- Do not send item arrays to `POST /customer/orders`.
- Submit payload is `{ expectedCartVersion, idempotencyKey, notes? }`.
- Do not implement cash confirmation or payment collection.
- Do not use automatic mock fallback.
- Socket.IO events are invalidation hints only; REST remains source of truth.
- Ignore non-matching tenant/session/order events.

## Expected Work

1. Implement submit order service/mutation.
2. Generate and use idempotency key per submit click.
3. Preserve success/loading UX and route to real `order.id`.
4. Implement real order detail query for tracking page.
5. Implement pending order cancel if the UI supports customer cancel.
6. Wire service-request drawer to `POST /customer/service-requests`.
7. Wire bill request/current bill to real APIs.
8. Invalidate cart/bill/order queries after relevant mutations.
9. Add customer Socket.IO hook for `/orders` and `join.session`.
10. Invalidate/refetch matching queries on customer realtime events.
11. Remove primary PWA order/service/bill imports from fake realtime/mock store.
12. Add focused tests for submit payload and realtime invalidation where feasible.

## Required Verification

Run:

```bash
npx nx test customer-pwa
npx nx lint customer-pwa
```

If lint is not configured, record the exact error and run the strongest available
customer-pwa check.

## Required Batch Output

End with a concise handoff summary for Batch 4 containing:

- submit order payload and method name,
- order tracking route behavior,
- service request and bill API method names,
- Socket.IO hook name and event invalidation mapping,
- tests/lint run and results,
- remaining customer mock dependencies intentionally left.

Stop and ask if the router cannot safely represent real `order.id`, or if BFF
Socket.IO event names differ from Step 2.4 code.
