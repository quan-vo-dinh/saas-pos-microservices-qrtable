# Prompt — Step 2.5 Batch 2: Customer PWA Session And Cart

You are implementing Step 2.5 Batch 2 for the QRTable Nx/NestJS monorepo.

## Mission

Replace the Customer PWA mock session/cart source with real BFF session and cart
APIs while preserving the Step 2.2 PWA UI shell.

## Required Inputs

Input from Batch 1 is required. Read the Batch 1 final output before editing.

Before editing, read:

- `AGENTS.md`
- `docs/superpowers/handoffs/step-2.5/2026-04-30-step-2.5-batch-1-handoff.md`
- `docs/superpowers/handoffs/step-2.5/2026-04-30-step-2.5-batch-2-handoff.md`
- `docs/superpowers/specs/2026-04-30-step-2.5-fe-be-integration-design.md`
- `docs/superpowers/plans/step-2.5-fe-be-integration-plan.md`
- `docs/superpowers/plans/2026-04-24-step-2.2-mock-ui.md`
- `apps/customer-pwa/src/lib/api-client.ts`
- `apps/customer-pwa/src/constants/api.ts`
- `apps/customer-pwa/src/features/session/context/session-provider.tsx`
- `apps/customer-pwa/src/features/landing/services/session.service.ts`
- `apps/customer-pwa/src/features/landing/hooks/use-verify-qr.ts`
- `apps/customer-pwa/src/features/order/services/order.service.ts`
- `apps/customer-pwa/src/features/order/hooks/use-order-query.ts`
- `apps/customer-pwa/src/pages/menu-page.tsx`
- `apps/customer-pwa/src/pages/cart/cart-drawer.tsx`
- `apps/customer-pwa/src/components/menu/cart-pill.tsx`
- `apps/customer-pwa/src/mocks/store.ts`

## Implementation Rules

- Work on `main`.
- Do not commit.
- Do not modify Management App or backend code unless Batch 1 output requires a
  small contract fix.
- Do not submit orders in this batch.
- Do not use mock fallback for failed join/cart calls.
- Keep mock files in place, but remove them from the primary session/cart path.
- Customer APIs must send `x-tenant-id` and `x-session-id` after join.
- Backend `CartSnapshot` is source of truth.
- Cart mutations must use `expectedCartVersion` and rollback/refetch on 409.

## Expected Work

1. Update Customer PWA endpoint constants for session and cart.
2. Extend `customerApi` so active session id is sent as `x-session-id`.
3. Persist and hydrate real session metadata in `localStorage`.
4. Replace mock auto-start with real `POST /customer/sessions/join`.
5. Add real cart service methods for get, mutate, and clear.
6. Add tenant/session-scoped cart query keys.
7. Wire menu add buttons to real cart mutation.
8. Wire cart drawer quantity/note/remove/clear controls to real cart mutation.
9. Wire cart pill count/total to real cart query data.
10. Add tests for header injection, session persistence, and cart conflict
    rollback where feasible.

## Required Verification

Run:

```bash
npx nx test customer-pwa
npx nx lint customer-pwa
```

If lint is not configured, record the exact error and run the strongest available
customer-pwa check.

## Required Batch Output

End with a concise handoff summary for Batch 3 containing:

- session storage key/name,
- `x-session-id` injection location,
- cart service methods and query keys,
- conflict rollback/refetch behavior,
- tests/lint run and results,
- mock imports still present in primary customer order paths, if any.

Stop and ask if the join UI cannot provide `tableId` and `qrToken`, or if the
backend session response lacks an id usable as `x-session-id`.
