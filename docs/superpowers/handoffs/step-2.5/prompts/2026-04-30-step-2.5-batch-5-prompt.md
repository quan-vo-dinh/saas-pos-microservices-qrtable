# Prompt — Step 2.5 Batch 5: Management Service Requests, Transfer, Realtime, Verification

You are implementing Step 2.5 Batch 5 for the QRTable Nx/NestJS monorepo.

## Mission

Finish Step 2.5 by wiring Management POS service requests, safe table transfer,
staff realtime invalidation, and final verification.

## Required Inputs

Input from Batch 4 is required. Read the Batch 4 final output before editing.

Before editing, read:

- `AGENTS.md`
- `docs/superpowers/handoffs/step-2.5/2026-04-30-step-2.5-batch-4-handoff.md`
- `docs/superpowers/handoffs/step-2.5/2026-04-30-step-2.5-batch-5-handoff.md`
- `docs/superpowers/specs/2026-04-30-step-2.5-fe-be-integration-design.md`
- `docs/superpowers/plans/step-2.5-fe-be-integration-plan.md`
- `docs/superpowers/plans/2026-04-24-step-2.2-mock-ui.md`
- Batch 1 final output for `GET /admin/service-requests`
- `apps/bff/src/app/modules/realtime/gateways/order-events.gateway.ts`
- `apps/bff/src/app/modules/realtime/services/realtime-events.service.ts`
- `apps/management-app/src/components/pos/service-request-table.tsx`
- `apps/management-app/src/components/pos/service-request-detail-panel.tsx`
- `apps/management-app/src/components/pos/transfer-table-dialog.tsx`
- `apps/management-app/src/mocks/use-fake-realtime.ts`

## Implementation Rules

- Work on `main`.
- Do not commit.
- Do not modify Customer PWA or backend code unless Batch 1 contract is missing
  and blocks the batch.
- Use `GET /admin/service-requests` from Batch 1.
- Do not add new permissions.
- Do not invent missing transfer ids.
- Staff socket emits `join.staff` with `{ tenantId }`.
- WebSocket events only invalidate/refetch REST queries.
- Do not wire KDS to real backend.
- Do not implement cash payment confirmation.

## Expected Work

1. Add service-request endpoint constants.
2. Add service-request API service and React Query hooks.
3. Replace POS service-request mock table data with real polling.
4. Wire acknowledge and resolve buttons to real mutations.
5. Invalidate service-request list after ack/resolve.
6. Wire transfer dialog to `POST /admin/tables/transfer` only when required ids
   exist.
7. Keep transfer disabled or use existing unavailable state when ids are missing.
8. Add staff Socket.IO hook for `/orders` and `join.staff`.
9. Invalidate order, service-request, and table queries on matching staff events.
10. Remove primary POS service-request/order fake realtime dependency.
11. Run final automated verification and record manual demo readiness.

## Required Verification

Run the strongest valid subset:

```bash
npx nx test order
npx nx test bff
npx nx test customer-pwa
npx nx test management-app
npx nx lint customer-pwa
npx nx lint management-app
```

If any target is missing, record the exact error and replacement command.

Manual demo checklist:

1. Customer joins a table session.
2. Customer adds cart item and submits order.
3. POS sees pending order by polling.
4. Staff confirms or cancels order.
5. Customer creates service request.
6. POS inbox lists the request.
7. Staff acknowledges and resolves request.
8. Customer requests bill and ordering locks where backend status requires.
9. Cross-tenant data is not visible.
10. KDS remains mock and still loads.

## Required Batch Output

End with the final Step 2.5 summary containing:

- files changed by Batch 5,
- service-request API and query key names,
- transfer behavior and disabled cases,
- staff realtime hook and event invalidation mapping,
- mock dependencies intentionally left for KDS/cash/analytics,
- automated command results,
- manual demo result or blockers,
- follow-up documentation needed.

Stop and ask if Batch 1 did not provide `GET /admin/service-requests`, or if a
verification failure looks like a Step 2.4 backend regression.
