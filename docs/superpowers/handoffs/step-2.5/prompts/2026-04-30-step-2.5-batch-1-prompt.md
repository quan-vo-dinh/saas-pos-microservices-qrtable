# Prompt — Step 2.5 Batch 1: Backend Service-Request Inbox Contract

You are implementing Step 2.5 Batch 1 for the QRTable Nx/NestJS monorepo.

## Mission

Add the missing backend contract for the Management POS service-request inbox:

```txt
GET /admin/service-requests?status=&limit=&offset=
```

This is the only backend gap required before frontend Step 2.5 integration can
fully replace the Step 2.2 mock service-request inbox.

## Required Inputs

There is no previous Step 2.5 batch output for Batch 1.

Before editing, read:

- `AGENTS.md`
- `docs/phases/phase-2a-order-kafka.md`
- `docs/business-logic-step-2.4-spec.vi.md`
- `docs/superpowers/specs/2026-04-30-step-2.5-fe-be-integration-design.md`
- `docs/superpowers/plans/step-2.5-fe-be-integration-plan.md`
- `docs/superpowers/handoffs/step-2.5/2026-04-30-step-2.5-batch-1-handoff.md`
- `docs/architecture/permission-matrix.md`
- `apps/bff/src/app/modules/order/controllers/staff-order.controller.ts`
- `apps/order/src/app/modules/order/controllers/order.controller.ts`
- `apps/order/src/app/modules/order/services/service-request.service.ts`
- `apps/order/src/app/modules/order/repositories/service-request.repository.ts`
- `libs/constants/src/lib/enum/tcp-request-message.ts`
- `libs/constants/src/lib/enum/role.enum.ts`
- `libs/interfaces/src/lib/tcp/order/order-request.interface.ts`
- `libs/interfaces/src/lib/tcp/order/order-response.interface.ts`

## Implementation Rules

- Work on `main`.
- Do not commit.
- Do not edit frontend apps in this batch.
- Do not add a new permission enum.
- Use `SERVICE_REQUEST_ACKNOWLEDGE` for the staff inbox list route.
- Every Order DB query must filter by `tenant_id`.
- Match the style of existing staff order list and service-request action code.
- Do not emit realtime events for reads.

## Expected Work

1. Add `TCP_REQUEST_MESSAGE.ORDER.SERVICE_REQUEST_GET_LIST`.
2. Add `ListServiceRequestsTcpRequest`.
3. Add `ServiceRequestListTcpResponse`.
4. Add a tenant-scoped repository list method with optional status, limit, and
   offset.
5. Add `ServiceRequestService.list(...)`.
6. Add Order TCP handler for `SERVICE_REQUEST_GET_LIST`.
7. Add BFF `GET /admin/service-requests`.
8. Protect route with `@Authorization({ secured: true })` and
   `@Permissions([PERMISSION.SERVICE_REQUEST_ACKNOWLEDGE])`.
9. Add or update tests for tenant filtering, status filtering, and BFF/TCP
   contract wiring where the current test setup supports it.

## Required Verification

Run:

```bash
npx nx test order
npx nx test bff
```

If a target name differs, run `npx nx show projects` and use the closest
equivalent. Record exact commands and results.

## Required Batch Output

End with a concise handoff summary for Batch 2 containing:

- TCP constant added,
- request/response interfaces added,
- repository/service/controller methods added,
- BFF route and permission used,
- tests run and results,
- blockers or contract changes for Customer PWA work.

Stop and ask if the permission matrix contradicts
`SERVICE_REQUEST_ACKNOWLEDGE`, or if tenant context is missing from the staff BFF
route.
