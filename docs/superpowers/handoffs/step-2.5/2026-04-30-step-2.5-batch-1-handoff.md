# Step 2.5 — Batch 1 Handoff (Backend Service-Request Inbox Contract)

**Date:** 2026-04-30  
**Branch:** `main`  
**Status:** Planned handoff, not executed yet  
**Scope:** Backend gap from Task 1 in
`docs/superpowers/plans/step-2.5-fe-be-integration-plan.md`

---

## Execution Goal

Add the missing staff service-request list contract required by the real
Management POS inbox:

```txt
GET /admin/service-requests?status=&limit=&offset=
```

This batch must keep tenant isolation and reuse existing Step 2.4 BFF/Order TCP
patterns.

## Required Reading

- `AGENTS.md`
- `docs/phases/phase-2a-order-kafka.md`
- `docs/business-logic-step-2.4-spec.vi.md`
- `docs/superpowers/specs/2026-04-30-step-2.5-fe-be-integration-design.md`
- `docs/superpowers/plans/step-2.5-fe-be-integration-plan.md`
- `docs/superpowers/plans/2026-04-24-step-2.2-mock-ui.md`
- `docs/architecture/permission-matrix.md`
- `apps/bff/src/app/modules/order/controllers/staff-order.controller.ts`
- `apps/order/src/app/modules/order/controllers/order.controller.ts`
- `apps/order/src/app/modules/order/services/service-request.service.ts`
- `apps/order/src/app/modules/order/repositories/service-request.repository.ts`
- `libs/constants/src/lib/enum/tcp-request-message.ts`
- `libs/constants/src/lib/enum/role.enum.ts`
- `libs/interfaces/src/lib/tcp/order/order-request.interface.ts`
- `libs/interfaces/src/lib/tcp/order/order-response.interface.ts`

## Batch Tasks

| Deliverable      | Required result                                                                                         |
| ---------------- | ------------------------------------------------------------------------------------------------------- |
| TCP constant     | Add `TCP_REQUEST_MESSAGE.ORDER.SERVICE_REQUEST_GET_LIST`.                                               |
| TCP interfaces   | Add `ListServiceRequestsTcpRequest` and `ServiceRequestListTcpResponse`.                                |
| Order repository | Add tenant-scoped list method with optional `status`, `limit`, and `offset`.                            |
| Order service    | Add list method that clamps pagination and maps entity to shared `ServiceRequest` DTO.                  |
| Order controller | Add `@MessagePattern(TCP_REQUEST_MESSAGE.ORDER.SERVICE_REQUEST_GET_LIST)`.                              |
| BFF staff route  | Add `GET /admin/service-requests` in `StaffOrderController`.                                            |
| Permission guard | Use `@Authorization({ secured: true })` and `@Permissions([PERMISSION.SERVICE_REQUEST_ACKNOWLEDGE])`.   |
| Tests            | Cover tenant filtering, status filtering, TCP pattern wiring, and BFF permission choice where feasible. |

## File Ownership

This batch may modify:

- `libs/constants/src/lib/enum/tcp-request-message.ts`
- `libs/interfaces/src/lib/tcp/order/order-request.interface.ts`
- `libs/interfaces/src/lib/tcp/order/order-response.interface.ts`
- `apps/order/src/app/modules/order/repositories/service-request.repository.ts`
- `apps/order/src/app/modules/order/services/service-request.service.ts`
- `apps/order/src/app/modules/order/controllers/order.controller.ts`
- `apps/order/src/app/modules/order/tests/service-request.service.spec.ts`
- `apps/bff/src/app/modules/order/controllers/staff-order.controller.ts`
- Existing BFF/order tests if needed for controller coverage

Do not modify Customer PWA or Management App frontend code in this batch.

## Implementation Constraints

- Do not add a new permission enum for service-request read/list in Step 2.5.
- `SERVICE_REQUEST_ACKNOWLEDGE` is the minimum permission for the inbox list.
- Every Order DB query must filter by `tenant_id`.
- Do not implement service-request pagination metadata unless the existing order
  list pattern already returns metadata. Match existing BFF list style first.
- Do not emit realtime events for list reads.
- Keep route under existing `@Controller('admin')`.
- Keep response wrapping consistent with `GET /admin/orders`.

## Verification

Run at minimum:

```bash
npx nx test order
npx nx test bff
```

If test project names differ, run `npx nx show projects` and use the equivalent
targets. Record exact commands and results.

## Handoff Output For Batch 2

Before ending this batch, report:

- TCP constant name added,
- request/response type names added,
- BFF route path and permission used,
- Order repository/service methods added,
- tests run and results,
- any compile/test failures or API shape changes Batch 2 must know.

## Stop Conditions

Stop and ask before continuing if:

- the existing service-request entity cannot be mapped to `@einvoice/types`
  without changing shared types,
- the permission matrix contradicts using `SERVICE_REQUEST_ACKNOWLEDGE` for
  inbox read,
- adding the list endpoint reveals missing tenant context in BFF staff routes.

---

## END HANDOFF
