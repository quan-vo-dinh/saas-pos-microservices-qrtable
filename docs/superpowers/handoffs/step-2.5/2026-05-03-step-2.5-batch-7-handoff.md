# Step 2.5 — Batch 7 Handoff (POS Tables Real Integration)

**Date:** 2026-05-03  
**Branch:** `main`  
**Status:** Planned handoff, not executed yet  
**Scope:** Close the `/pos/tables` Step 2.5 integration gap by replacing mock
table map/detail data with real Catalog table APIs and existing POS order
polling.

---

## Execution Goal

Finish the missing Management POS Tables integration after Batch 6:

```txt
Management App /pos/tables
→ table grid reads real Catalog tables
→ selected table detail reads real table + active admin orders by table
→ transfer uses real session/table ids when available
→ mark clean uses real Catalog status mutation
→ Bills/payment/KDS stay out of scope
```

## Required Reading

- `AGENTS.md`
- Batch 6 final output
- `docs/superpowers/handoffs/step-2.5/2026-05-01-step-2.5-batch-6-handoff.md`
- `docs/superpowers/plans/2026-05-03-step-2.5-batch-7-pos-tables-real-integration-plan.md`
- `docs/superpowers/plans/step-2.5-fe-be-integration-plan.md`
- `docs/superpowers/specs/2026-04-30-step-2.5-fe-be-integration-design.md`
- `docs/superpowers/plans/2026-04-24-step-2.2-mock-ui.md`
- `docs/phases/phase-2a-order-kafka.md`
- `docs/business-logic-step-2.4-spec.vi.md`
- `docs/technical-architecture.md`
- `apps/management-app/src/components/pos/table-map-grid.tsx`
- `apps/management-app/src/components/pos/table-detail-panel.tsx`
- `apps/management-app/src/components/pos/non-order-right-inspector.tsx`
- `apps/management-app/src/components/pos/non-order-route-reset.tsx`
- `apps/management-app/src/features/tables/hooks/use-tables-query.ts`
- `apps/management-app/src/features/tables/hooks/use-tables-mutations.ts`
- `apps/management-app/src/features/order/hooks/use-order-query.ts`
- `apps/management-app/src/features/order/services/order.service.ts`

## Batch Tasks

| Deliverable        | Required result                                                                  |
| ------------------ | -------------------------------------------------------------------------------- |
| POS table UI state | Selected table id uses focused UI store, not `mocks/store`.                      |
| Table map data     | `TableMapGrid` uses `useTablesQuery()` and no table mock fallback.               |
| Table order totals | Table map/detail derive active order totals from `useOrdersQuery()`.             |
| Table detail       | Detail panel reads real table and active orders; no fake presence/timeline.      |
| Transfer guardrail | Transfer passes real `fromTableId` and `sessionId`; stays disabled when missing. |
| Mark clean         | Cleaning → available uses existing Catalog status mutation.                      |
| Scope preservation | Bills/payment/KDS/analytics/notifications remain intentionally mock or deferred. |
| Documentation      | Step 2.5 plan records Batch 7 addendum and tech debt.                            |

## File Ownership

This batch may modify:

- `apps/management-app/src/features/tables/hooks/use-pos-table-ui-state.ts`
- `apps/management-app/src/features/tables/hooks/use-pos-table-ui-state.spec.ts`
- `apps/management-app/src/components/pos/table-map-grid.tsx`
- `apps/management-app/src/components/pos/table-detail-panel.tsx`
- `apps/management-app/src/components/pos/non-order-right-inspector.tsx`
- `apps/management-app/src/components/pos/non-order-route-reset.tsx`
- `apps/management-app/src/components/pos/__tests__/table-map-grid.spec.tsx`
- `apps/management-app/src/components/pos/__tests__/table-detail-panel.spec.tsx`
- `docs/superpowers/plans/step-2.5-fe-be-integration-plan.md`

Do not modify backend code unless a real API contract used by an existing hook is
missing at runtime and blocks the batch. Stop and ask before adding a new backend
endpoint.

## Implementation Constraints

- Work on `main`.
- Do not commit.
- Do not implement cash payment confirmation.
- Do not integrate KDS with real backend.
- Do not add new permissions.
- Do not add automatic mock fallback for `/pos/tables`.
- Do not invent missing session ids.
- Use existing `useTablesQuery()` for table data.
- Use existing `useOrdersQuery({ tableId })` for active table order summary.
- Use existing `useTransferTableMutation()` through `TransferTableDialog`.
- Use existing `useUpdateTableStatusMutation()` for mark-clean.
- Keep `/pos/bills` mock/out-of-scope and clearly report it as intentional.

## Verification

Run the strongest valid subset:

```bash
npx nx test management-app --testPathPattern=table-map-grid.spec.tsx
npx nx test management-app --testPathPattern=table-detail-panel.spec.tsx
npx nx test management-app --testPathPattern=use-pos-table-ui-state.spec.ts
npx nx test management-app
npx nx lint management-app
```

If a target is missing or incompatible with the current Jest/Nx setup, record
the exact error and run the strongest equivalent command, such as:

```bash
pnpm --dir apps/management-app build
```

Manual demo checklist:

1. `/pos/tables` loads real table names/statuses matching `/dashboard/tables`.
2. Selecting a table opens real table detail in the right inspector.
3. A table with active backend orders shows real active order summary after
   polling/refetch.
4. Transfer opens only when the selected table has a real `sessionId`.
5. Available transfer destinations come from real Catalog tables.
6. Mark clean updates a `cleaning` table through the backend.
7. `/pos/bills` remains mock/out-of-scope.
8. `/pos`, `/pos/service-requests`, and Batch 6 QR dialog still load.

## Final Batch Output

Before ending this batch, report:

- files changed,
- table UI state hook name,
- query hooks used for table map and active order summary,
- transfer enabled/disabled behavior,
- mark-clean behavior,
- mock dependencies intentionally left for Bills/KDS/notifications/analytics,
- automated command results,
- manual demo result or blockers,
- follow-up tech debt.

## Stop Conditions

Stop and ask before continuing if:

- `useTablesQuery()` cannot provide `sessionId` for occupied tables and transfer
  is expected to be part of the demo,
- real table/order DTO shape differs from the shared types enough to require a
  backend contract change,
- marking a table `available` from `cleaning` violates current Catalog business
  rules,
- implementing this batch would require payment/session-close logic.

---

## END HANDOFF
