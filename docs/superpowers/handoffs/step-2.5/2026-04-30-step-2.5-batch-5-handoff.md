# Step 2.5 — Batch 5 Handoff (Management Service Requests, Transfer, Realtime, Verification)

**Date:** 2026-04-30  
**Branch:** `main`  
**Status:** Planned handoff, not executed yet  
**Scope:** Management service requests, table transfer, staff realtime hints, and
final verification from Tasks 8, 9, and 10 in
`docs/superpowers/plans/step-2.5-fe-be-integration-plan.md`

---

## Execution Goal

Finish Step 2.5 by wiring the remaining Management POS service-request inbox,
safe table transfer integration, staff WebSocket invalidation, and the final
demo verification checklist.

Target outcome:

```txt
Customer service request
→ POS sees request via real list polling
→ staff acknowledges/resolves
→ staff socket events invalidate/refetch
→ Step 2.5 demo checklist is executable
```

## Required Reading

- `AGENTS.md`
- Batch 4 final output
- `docs/superpowers/handoffs/step-2.5/2026-04-30-step-2.5-batch-4-handoff.md`
- `docs/superpowers/specs/2026-04-30-step-2.5-fe-be-integration-design.md`
- `docs/superpowers/plans/step-2.5-fe-be-integration-plan.md`
- `docs/superpowers/plans/2026-04-24-step-2.2-mock-ui.md`
- Batch 1 output for `GET /admin/service-requests`
- `apps/bff/src/app/modules/realtime/gateways/order-events.gateway.ts`
- `apps/bff/src/app/modules/realtime/services/realtime-events.service.ts`
- `apps/management-app/src/components/pos/service-request-table.tsx`
- `apps/management-app/src/components/pos/service-request-detail-panel.tsx`
- `apps/management-app/src/components/pos/transfer-table-dialog.tsx`
- `apps/management-app/src/features/tables/services/tables.service.ts`
- `apps/management-app/src/mocks/use-fake-realtime.ts`

## Batch Tasks

| Deliverable            | Required result                                                                 |
| ---------------------- | ------------------------------------------------------------------------------- |
| Service request API    | Add constants/service/hooks for list, acknowledge, and resolve.                 |
| Service request UI     | Table/detail panel read real API polling and call real ack/resolve mutations.   |
| Transfer integration   | Call `POST /admin/tables/transfer` only when required ids are available.        |
| Transfer guardrail     | Keep transfer disabled or show existing unavailable state when ids are missing. |
| Staff realtime         | Socket joins `join.staff`; events only invalidate/refetch matching queries.     |
| Mock cleanup           | Primary POS service-request/order paths no longer depend on fake realtime.      |
| Verification checklist | Run automated checks and record manual demo readiness/blockers.                 |
| Scope preservation     | KDS and cash payment remain mock/out of primary integration path.               |

## File Ownership

This batch may modify:

- `apps/management-app/src/constants/api.ts`
- `apps/management-app/src/features/service-requests/services/service-request.service.ts`
- `apps/management-app/src/features/service-requests/hooks/use-service-request-query.ts`
- `apps/management-app/src/components/pos/service-request-table.tsx`
- `apps/management-app/src/components/pos/service-request-detail-panel.tsx`
- `apps/management-app/src/components/pos/transfer-table-dialog.tsx`
- Management POS realtime hook/provider files if a focused hook is added
- POS page/container files that wire service request or transfer behavior
- Documentation status notes if verification uncovers known limitations

Do not modify Customer PWA or backend code unless Batch 1 contract was not
actually implemented and blocks service-request list calls.

## Implementation Constraints

- Use `GET /admin/service-requests` created by Batch 1.
- Do not add new permissions.
- Staff socket emits `join.staff` with `{ tenantId }`.
- Listen for `events.orderCreated`, `events.orderStatusChanged`,
  `events.serviceRequested`, and `events.tableTransferred`.
- Do not mutate local state directly from WebSocket payloads; invalidate/refetch
  React Query data.
- Do not wire KDS kitchen/bar to real order backend.
- Do not implement cash payment confirmation.
- Do not invent `sessionId`, `fromTableId`, or `toTableId` if the current UI
  does not have them.

## Verification

Run the strongest valid subset:

```bash
npx nx test order
npx nx test bff
npx nx test customer-pwa
npx nx test management-app
npx nx lint customer-pwa
npx nx lint management-app
```

If any target is missing, record the exact error and the replacement command
used.

Manual demo checklist:

1. Customer joins a table session.
2. Customer adds cart item and submits order.
3. POS sees pending order by polling.
4. Staff confirms or cancels order.
5. Customer creates service request.
6. POS inbox lists the request.
7. Staff acknowledges and resolves request.
8. Customer requests bill and ordering locks where backend status requires.
9. Cross-tenant data is not visible.
10. KDS remains mock and still loads.

## Final Step 2.5 Output

Before ending this batch, report:

- files changed by batch,
- service-request API method and query key names,
- transfer behavior and any disabled cases,
- staff realtime hook and event invalidation mapping,
- mock dependencies intentionally left for KDS/cash/analytics,
- automated command results,
- manual demo result or blockers,
- any follow-up documentation needed.

## Stop Conditions

Stop and ask before continuing if:

- Batch 1 did not actually provide `GET /admin/service-requests`,
- service request list shape is incompatible with the existing POS table,
- transfer UI cannot obtain required ids and disabling it would break the
  expected demo path,
- a final verification failure suggests a Step 2.4 backend regression rather
  than a Step 2.5 frontend integration issue.

---

## END HANDOFF
