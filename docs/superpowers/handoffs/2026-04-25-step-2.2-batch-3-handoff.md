# Step 2.2 — Batch 3 handoff (POS Tasks 15–21)

**Date:** 2026-04-25  
**Branch:** `main`  
**Scope:** Tasks 15–21 from `docs/superpowers/plans/2026-04-24-step-2.2-mock-ui.md` (Live Orders, detail pane, filters, KPI, charts, ⌘K, Tables + transfer, Service inbox, Cash bills)

---

## SESSION SUMMARY

### Tiến độ

| Task   | Nội dung chính                                                                                              | Trạng thái |
| ------ | ----------------------------------------------------------------------------------------------------------- | ---------- |
| **15** | `LiveOrdersTable` (TanStack Table), lọc nhanh + `posViewFilter`, `framer-motion` row, HoverCard món/ghi chú | Hoàn thành |
| **16** | `OrderDetailPanel` (Tabs Items / Activity / Customer), `CancelOrderDialog` (zod + Select + textarea)        | Hoàn thành |
| **17** | `OrderRowContextMenu`, `KpiTiles`, `TodayMiniCharts` (Recharts, Collapsible trong sidebar)                  | Hoàn thành |
| **18** | `CommandPalette` (⌘K / Ctrl+K), nhóm Bàn / Đơn / Hành động → `router` + store                               | Hoàn thành |
| **19** | `TableMapGrid` (Grid + SVG Map), `TableDetailPanel`, `TransferTableDialog`                                  | Hoàn thành |
| **20** | `ServiceRequestTable` (ToggleGroup tab PENDING / ACK / RESOLVED + DataTable)                                | Hoàn thành |
| **21** | `CashBillPanel` (zod + chip + tiền thừa), trang Bills (danh sách PENDING trái)                              | Hoàn thành |

**Thêm / nền tảng chung**

- `PosAppShell`: `useFakeRealtime`, `ResizablePanelGroup` (trái outlet + phải `PosRightInspector`), `PosSubNav`, KPI dính đáy, `CommandPalette`.
- `useMockStore` mở rộng: `orderPriority`, `posViewFilter`, `selectedTableId`, `selectedBillId`, `updateOrderItemStatus`, `toggleOrderPriority`, `setPosViewFilter`, `selectTable`, `selectBill`, `setTableStatus`.
- Sửa **import CSS** trong `globals.css`: `@import '../styles/pos-tokens.css'` (trước đó sai `./styles/...` gây lỗi build).
- Trang `/pos/tables`: bọc `TableMapGrid` trong `<Suspense>` (yêu cầu Next khi dùng `useSearchParams`).

### Blockers

Không có.

### Dependencies mới / rõ ràng hoá

| Gói / nguồn                 | Ghi chú                                                                                                                                                                                                    |
| --------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `framer-motion`, `recharts` | Thêm vào `apps/management-app/package.json` (dùng cho layout row, mini charts).                                                                                                                            |
| shadcn CLI                  | Thêm local `src/components/ui/*`: button, badge, dialog, alert-dialog, tabs, toggle-group, hover-card, context-menu, card, input, label, select, separator, scroll-area, alert, tooltip, checkbox, toggle. |
| `lucide-react`              | Đã có; dùng xuyên suốt POS.                                                                                                                                                                                |

**Lưu ý:** types domain vẫn từ **`@einvoice/types`** (đồng bộ handoff Batch 1), không dùng `@qrtable/shared-types`.

### Key exports (import cho Batch 4+)

| Thành phần                 | Đường dẫn                                                                   |
| -------------------------- | --------------------------------------------------------------------------- |
| Live orders + filter store | `posViewFilter`, `setPosViewFilter` trong `mocks/store.ts`                  |
| Live table                 | `components/pos/live-orders-table.tsx`                                      |
| Order detail + cancel      | `order-detail-panel.tsx`, `cancel-order-dialog.tsx`                         |
| Context menu hàng đơn      | `order-row-context-menu.tsx`                                                |
| KPI + charts               | `kpi-tiles.tsx`, `today-mini-charts.tsx`                                    |
| ⌘K                         | `command-palette.tsx`                                                       |
| Shell POS                  | `pos-app-shell.tsx`, `pos-right-inspector.tsx`, `pos-sub-nav.tsx`           |
| Bàn                        | `table-map-grid.tsx`, `table-detail-panel.tsx`, `transfer-table-dialog.tsx` |
| Service                    | `service-request-table.tsx`                                                 |
| Tiền mặt                   | `cash-bill-panel.tsx`                                                       |
| Format VND                 | `lib/format-vnd.ts`                                                         |

### Lưu ý cho Batch 4 (bản thiết kế: Tables Map & ⌘K)

1. **KDS + POS:** Batch 3 đã triển khai POS đầy đủ theo Tasks 15–21; Batch 4 có thể tập trung **KDS** (theo plan Task 22+), `NotificationsPopover`, tenant switcher stub, hoặc tinh chỉnh `PosRightInspector` khi thêm route mới.
2. **Recharts tại SSG:** Build thành công nhưng console vẫn cảnh báo kích thước chart khi prerender; có thể `ssr: false` dynamic import cho `TodayMiniCharts` hoặc chỉ render chart sau `useEffect` mount.
3. **RHF + zod:** Cash panel và cancel dùng zod trực tiếp / controlled; nếu chuẩn hoá RHF toàn POS, cần align resolver với Zod 4 (handoff PWA Batch 2).
4. **⌘K:** `CommandDialog` dùng `@einvoice/frontend-ui` `Dialog` — nếu đổi theme, giữ `DialogHeader` `sr-only` theo mẫu sẵn.
5. **A11y:** Cột wait time dùng màu + số + nhịp pulse khi quá 15′ (không dùng emoji cho trạng thái).

---

## Verification (Batch 3)

```bash
pnpm --dir apps/management-app exec tsc --noEmit
pnpm nx run management-app:lint
pnpm nx run management-app:build
```

---

## END SUMMARY
