# Step 2.2 — Batch 1 handoff (Mock foundations)

**Date:** 2026-04-25  
**Branch:** `main`  
**Scope:** Tasks 0–7 from `docs/superpowers/plans/2026-04-24-step-2.2-mock-ui.md`

---

## SESSION SUMMARY

### Tiến độ

- **Task 0:** Dependencies — root (`framer-motion`, `@dnd-kit/core`, `@dnd-kit/sortable`, `recharts`, `@faker-js/faker`); `customer-pwa` (`zustand`, `react-hook-form`, `@hookform/resolvers`, `zod`).
- **Task 1:** `KDSTicketMock`, `toKDSTicket` — `apps/management-app/src/mocks/kds-ticket.ts`.
- **Task 2:** Faker seed + fixtures — `apps/management-app/src/mocks/seed.ts`.
- **Task 3:** Zustand POS mock store — `apps/management-app/src/mocks/store.ts`.
- **Task 4:** Fake realtime hook (management) — `apps/management-app/src/mocks/use-fake-realtime.ts`.
- **Task 5:** Web Audio helpers — `apps/management-app/src/mocks/audio.ts`, `apps/customer-pwa/src/mocks/audio.ts`.
- **Task 6:** Design tokens + `data-surface` — `pwa-tokens.css`, `pos-tokens.css`, `kds-tokens.css`; `globals.css` / `index.css`; `(pos)` / `(kds)` layouts; PWA `main.tsx` sets `data-surface="pwa"` on `document.documentElement`.
- **Task 7:** PWA seed, Zustand store, fake realtime — `apps/customer-pwa/src/mocks/seed.ts`, `store.ts`, `use-fake-realtime.ts`.

### Blockers

Không có.

**Lệch plan đã xử lý:** Plan ghi `@qrtable/shared-types` — không tồn tại trong monorepo. Toàn bộ domain/realtime types dùng **`@einvoice/types`** (alias trong `tsconfig`). Bàn dùng **`RestaurantTable`** và **`TableStatus`** dạng chữ thường (`available` | `occupied` | `billing` | `cleaning`).

### Dependencies (package mới)

| Vị trí              | Gói                                                                                  |
| ------------------- | ------------------------------------------------------------------------------------ |
| Workspace root      | `framer-motion`, `@dnd-kit/core`, `@dnd-kit/sortable`, `recharts`, `@faker-js/faker` |
| `apps/customer-pwa` | `zustand`, `react-hook-form`, `@hookform/resolvers`, `zod`                           |

Chưa thêm component shadcn mới trong batch này (UI component tasks bắt đầu từ Task 8+).

### Key exports (import cho Batch sau)

| Mục đích                              | Đường dẫn                                                                                                                                       |
| ------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| KDS mock ticket type + strip          | `apps/management-app/src/mocks/kds-ticket.ts`                                                                                                   |
| POS seed / tenant / presence types    | `apps/management-app/src/mocks/seed.ts` — `buildSeed()`, `mockTenantId`, `MockSeed`, …                                                          |
| POS Zustand                           | `apps/management-app/src/mocks/store.ts` — `useMockStore`                                                                                       |
| POS fake WS                           | `apps/management-app/src/mocks/use-fake-realtime.ts` — `useFakeRealtime`                                                                        |
| POS audio                             | `apps/management-app/src/mocks/audio.ts`                                                                                                        |
| PWA menu/session/presence/activity    | `apps/customer-pwa/src/mocks/seed.ts`                                                                                                           |
| PWA Zustand                           | `apps/customer-pwa/src/mocks/store.ts` — `usePwaMockStore`, `CartLine`                                                                          |
| PWA fake realtime + local stock event | `apps/customer-pwa/src/mocks/use-fake-realtime.ts` — `usePwaFakeRealtime`, `MenuItemStockChangedEvent`                                          |
| PWA audio                             | `apps/customer-pwa/src/mocks/audio.ts`                                                                                                          |
| Tokens                                | `apps/customer-pwa/src/styles/pwa-tokens.css`, `apps/management-app/src/styles/pos-tokens.css`, `apps/management-app/src/styles/kds-tokens.css` |

### Notes cho Batch tiếp theo

1. **Hooks chưa mount:** `useFakeRealtime` (management) và `usePwaFakeRealtime` (PWA) cần được gọi trong layout/page khi muốn interval chạy trong dev/demo.
2. **Blueprint Task 6** còn gợi ý inject `data-surface` trong `app-shell.tsx` theo path; hiện chỉ bọc `(pos)` / `(kds)` + PWA trên `<html>`. Nếu cần sidebar/topbar đồng bộ token surface, bổ sung trong `AppShell`.
3. **`window.__mock__.toggleBillLock`** đã gắn qua PWA store (chuẩn Task 14 sau).
4. **Verify đã chạy:** `pnpm nx run customer-pwa:typecheck`; `pnpm --dir apps/management-app exec tsc --noEmit`.
5. **shadcn-component-discovery:** Áp dụng khi bắt đầu Task 8+ (SessionTimer, MenuItemCard, Sheet, …).

---

## END SUMMARY
