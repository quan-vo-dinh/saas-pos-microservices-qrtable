# Step 2.2 — Batch 5 handoff (FINAL SESSION SUMMARY)

**Date:** 2026-04-25  
**Branch:** `main`  
**Scope:** Plan Tasks **29–34** — KDS batching + SLA watch, recall log + station settings (persisted), KDS keyboard shortcuts + selection, POS live orders virtualization, Recharts SLA radial client-only, KDS typography (JetBrains Mono + DM Sans as General Sans stand-in), KDS no-hover polish, verification + handoff.

---

## FINAL SESSION SUMMARY

### Features completed (Batch 5)

| Task   | Deliverable                                 | Notes                                                                                                                                                                                                                                                                       |
| ------ | ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **29** | `KdsBatchingPanel`                          | Gom món (WAITING + IN_PROGRESS) theo tên + progress bar; SLA watch ≥80% cap SLA; tap món toggle highlight outline trên ticket; tap hàng watch → `scrollIntoView` + flash ring.                                                                                              |
| **30** | `RecallLogSheet` + `StationSettingsPopover` | Sheet recall đầy đủ cột (lúc, ticket, người, lý do, trạng thái, nút “Đánh dấu xử lý” → `markRecallResolved`). Cài đặt trạm: Dialog + range SLA 5–20 phút, cỡ chữ 14–22px, checkbox âm thanh → `localStorage` + `setSoundEnabled`; đồng bộ khi mở dialog qua `onOpenChange`. |
| **31** | `KdsBoard` wiring                           | Kanban + batching cột phải (full width mobile, `xl:w-[360px]`); `useFakeRealtime` giữ nguyên.                                                                                                                                                                               |
| **32** | A11y / keyboard KDS                         | Phím **1** (WAITING → IN_PROGRESS), **2** (IN_PROGRESS → DONE), **3** (DONE → recall) khi đã chọn ticket; bỏ qua khi focus trong `[data-kds-ignore-shortcuts]` hoặc input. Chọn ticket bằng tap vùng card; focus board `focus-visible:ring` lime.                           |
| **33** | Performance                                 | `@tanstack/react-virtual` trên `LiveOrdersTable` khi `rows.length > 50` (tbody block + hàng absolute + `measureElement`); `colgroup` + `table-fixed`.                                                                                                                       |
| **34** | Verification                                | Lệnh đã chạy: `pnpm --dir apps/management-app exec tsc --noEmit`, `pnpm nx run management-app:build`, eslint các file đổi; `pnpm nx run customer-pwa:typecheck`.                                                                                                            |

### KDS aesthetic & rules applied

- **High contrast / OLED:** Giữ token `kds-tokens.css`; band SLA dùng cap phút từ cài đặt (`effectiveSlaSeconds`).
- **Typography:** `(kds)/layout.tsx` — `next/font` **JetBrains Mono** (`--font-kds-mono`) + **DM Sans** latin-ext (`--font-kds-body`, thay thế pragmatic cho General Sans trong blueprint).
- **No hover trên KDS chrome:** `KdsHeader`, `KdsTicketSheet` actions, `KdsTicketCard` (title / grip / nút) dùng **`active:`** thay `hover:` cho tương tác chính.
- **Import paths:** Chỉ alias đã có (`@/`, `@einvoice/types`, mocks).

### Store extensions (`mocks/store.ts`)

- `kdsHighlightedItemName`, `kdsSelectedTicketId`
- `setKdsHighlightedItemName`, `selectKdsTicket`, `markRecallResolved`

### New / touched paths

| Path                                                                  | Role                                                                       |
| --------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| `apps/management-app/src/lib/kds-station-prefs.ts`                    | Đọc/ghi cap SLA + font px theo station (`localStorage`).                   |
| `apps/management-app/src/components/kds/kds-batching-panel.tsx`       | Batching + SLA watch.                                                      |
| `apps/management-app/src/components/kds/recall-log-sheet.tsx`         | Recall audit sheet.                                                        |
| `apps/management-app/src/components/kds/station-settings-popover.tsx` | Dialog cài đặt trạm (tên file giữ theo plan Task 30).                      |
| `apps/management-app/src/components/kds/kds-sla-radial.tsx`           | Radial Recharts tách file; import động `ssr: false` từ `kds-ticket-sheet`. |
| `apps/management-app/src/components/kds/kds-board.tsx`                | Layout batching + keyboard + settings/recall + refs scroll.                |
| `apps/management-app/src/components/kds/kds-ticket-card.tsx`          | SLA hiệu chỉnh, highlight món, chọn ticket, ref bề mặt, không hover.       |
| `apps/management-app/src/components/kds/kds-header.tsx`               | `active:` thay hover.                                                      |
| `apps/management-app/src/components/kds/kds-ticket-sheet.tsx`         | SLA theo cap + dynamic radial.                                             |
| `apps/management-app/src/components/pos/live-orders-table.tsx`        | Virtualize khi >50 dòng.                                                   |
| `apps/management-app/src/app/(kds)/layout.tsx`                        | Font KDS.                                                                  |
| `apps/management-app/package.json`                                    | `@tanstack/react-virtual`.                                                 |

### shadcn components installed (Batch 5)

**Không** chạy `shadcn add` mới trong batch này — tái sử dụng `Dialog`, `Checkbox`, `Sheet`, `Table`, `ScrollArea`, `Separator`, `Badge`, `Button`, `Label` đã có.

### MCP Database / Keycloak

Không có MCP server trong session agent; mapping **OrderItem.status** / ticket vẫn bám `@einvoice/types` và store mock như Batch 1–4.

### Known residual / follow-up (Step 2.5+)

- Console **Recharts** `width(-1) height(-1)` vẫn có thể xuất hiện khi prerender các biểu đồ POS khác (`today-mini-charts`, `service-request-detail-panel`); radial KDS đã tách `dynamic(..., { ssr: false })`.
- Virtualized **live orders** dùng `<TableRow ref={measureElement}>` — nếu chỉnh theme row height, cân chỉnh `estimateSize` (28).
- **MCP / thật:** Khi tích hợp BFF, thay `readKdsSlaCapMinutes` / prefs bằng API tenant-station nếu có.

---

## Verification checklist (Blueprint §6 — mock ready for Step 2.5)

Dùng checklist blueprint; trạng thái dưới đây phản ánh **mock UI sau Batch 5** (xác minh tay trên dev server được khuyến nghị trước khi tag).

| #   | Tiêu chí                                                   | Trạng thái                                                 |
| --- | ---------------------------------------------------------- | ---------------------------------------------------------- |
| 1   | PWA: 3 món → cart → submit → animation → tracking timeline | Đã có từ Batch 2 (chưa đổi code PWA trong batch này).      |
| 2   | PWA: Service request 3 nút → toast                         | Batch 2.                                                   |
| 3   | PWA: Out-of-stock mock → card disable                      | Batch 2.                                                   |
| 4   | POS: New order slide-in + bell (fake realtime)             | Batch 3 + 1.                                               |
| 5   | POS: ⌘K tìm bàn / order                                    | Batch 3.                                                   |
| 6   | POS: Confirm / Cancel                                      | Batch 3.                                                   |
| 7   | POS: Transfer table validation                             | Batch 3.                                                   |
| 8   | POS: Cash bill đủ tiền thừa                                | Batch 3.                                                   |
| 9   | POS: Service inbox 3 tab ack/resolve                       | Batch 3–4.                                                 |
| 10  | KDS: Kanban 3 cột + DnD cập nhật status                    | Batch 4 + giữ nguyên.                                      |
| 11  | KDS: SLA color + pulse theo cap phút (settings)            | **Batch 5** (`effectiveSlaSeconds` + ticket card + sheet). |
| 12  | KDS: Batching highlight cùng món                           | **Batch 5** (`kdsHighlightedItemName`).                    |
| 13  | KDS: Recall log sheet audit + resolve                      | **Batch 5** (`RecallLogSheet`, `markRecallResolved`).      |
| 14  | KDS: Ticket detail + radial SLA (client-only chart)        | **Batch 5** (`KdsSlaRadial` dynamic).                      |
| 15  | Mock events khớp `realtime-events.types.ts`                | Không đổi payload hook (Batch 1–4).                        |
| 16  | Themes WCAG AA                                             | Palette blueprint; spot-check tay trên 3 surface.          |
| 17  | Icon policy §5.6 — Lucide, không emoji icon                | Giữ từ các batch; KDS thêm glyph SLA.                      |

**Lệnh đã chạy (evidence):**

```bash
pnpm --dir apps/management-app exec tsc --noEmit
pnpm nx run management-app:build
pnpm nx run customer-pwa:typecheck
pnpm exec eslint src/components/kds/kds-board.tsx src/components/kds/station-settings-popover.tsx  # targeted
```

---

## Proposed single conventional commit (Step 2.2 close-out)

```
feat(management-app): complete step 2.2 batch 5 kds batching, station prefs, and live orders virtualization
```

---

## END SUMMARY
