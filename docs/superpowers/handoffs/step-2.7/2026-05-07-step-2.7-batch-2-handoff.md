# Step 2.7 — Batch 2 Handoff (Customer PWA Realtime)

**Date:** 2026-05-07  
**Branch:** `main`  
**Status:** Planned handoff, not executed yet  
**Scope:** Task 2 and the Customer PWA slice of Task 7 from
`docs/superpowers/plans/2026-05-07-step-2.7-fe-be-realtime.md`

---

## Execution Goal

Move Customer PWA to the finalized Step 2.7 realtime contract:

```txt
Customer PWA connects with auth.tenantId/sessionId
-> no join.session emit
-> session-scoped events invalidate REST snapshots
-> kitchen item ready refreshes tracking
-> reconnect/auth status is visible without blocking UI
```

This batch depends on Batch 1 BFF auth behavior and touches only Customer PWA.

## Required Reading

- Batch 1 final output
- `docs/superpowers/handoffs/step-2.7/2026-05-07-step-2.7-batch-1-handoff.md`
- `docs/specs/business-logic-step-2.7-spec.vi.md`
- `docs/superpowers/plans/2026-05-07-step-2.7-fe-be-realtime.md`
- `docs/specs/business-logic-step-2.6-spec.vi.md`
- `apps/customer-pwa/src/features/order/hooks/use-customer-order-realtime.ts`
- `apps/customer-pwa/src/features/order/hooks/use-customer-order-realtime.spec.tsx`
- `apps/customer-pwa/src/components/layout/mobile-shell.tsx`

## Batch Tasks

| Deliverable        | Required result                                                                                                    |
| ------------------ | ------------------------------------------------------------------------------------------------------------------ |
| Customer hook auth | Customer PWA connects to `/orders` with `auth: { tenantId, sessionId }`.                                           |
| No legacy join     | Customer PWA no longer emits `join.session`.                                                                       |
| Event coverage     | Hook listens to the finalized customer event set, including `events.kitchenItemReady`.                             |
| Query invalidation | Matching session events invalidate customer order/cart/bill snapshots.                                             |
| Reconnect sync     | Connect/reconnect invalidates active customer domains and keeps previous snapshot visible during refetch.          |
| Cleanup            | Socket and manager listeners are removed safely; remount/reconnect must not duplicate listeners.                   |
| Status UI          | Customer PWA renders a small realtime status pill for reconnecting/degraded/auth-error states.                     |
| Tests              | Focused Customer PWA tests cover auth options, no legacy join, event filtering, reconnect, cleanup, and status UI. |

## File Ownership

This batch may create/modify:

- `apps/customer-pwa/src/features/order/hooks/use-customer-order-realtime.ts`
- `apps/customer-pwa/src/features/order/hooks/use-customer-order-realtime.spec.tsx`
- `apps/customer-pwa/src/components/realtime/realtime-status-pill.tsx`
- `apps/customer-pwa/src/components/realtime/realtime-status-pill.spec.tsx`
- `apps/customer-pwa/src/components/layout/mobile-shell.tsx`

Do not modify BFF or Management App files unless Batch 1 left a documented
blocker that directly prevents Customer PWA tests from passing.

## Implementation Constraints

- Work on `main`; do not commit. The repository owner will review and commit.
- Use Context7/ctx7 for current Socket.IO Client or TanStack Query API behavior
  if package-specific docs are needed.
- Namespace remains `/orders`.
- FE must not send room names and must not emit `join.session`.
- WebSocket events are invalidation hints only; REST snapshots remain source of truth.
- Customer PWA must not listen to KDS station queue events.
- Do not add high-frequency polling to Customer PWA.
- Do not implement menu realtime.
- Do not add batching/gộp món/gom đơn under any name.
- Do not register domain event listeners inside the `connect` handler.
- Cleanup must use the same listener references passed to `on`.
- If the implementation plan contains commit snippets, ignore them for this
  handoff and report changed files instead.

## Verification

Run at minimum:

```bash
npx nx test customer-pwa --runTestsByPath src/features/order/hooks/use-customer-order-realtime.spec.tsx src/components/realtime/realtime-status-pill.spec.tsx
```

If time allows, also run:

```bash
npx nx lint customer-pwa
```

Record exact commands and results.

## Handoff Output For Batch 3

Before ending this batch, report:

- Customer PWA socket options,
- customer listener set and filtering behavior,
- customer query domains invalidated by each event,
- reconnect/auth/degraded status behavior,
- status UI placement,
- tests and lint commands run with results,
- any frontend auth/query issue Batch 3 must know before Management POS work.

## Stop Conditions

Stop and ask before continuing if:

- Customer PWA does not expose stable `tenantId` or `sessionId` for socket auth,
- current customer query keys cannot support the Step 2.7 invalidation matrix,
- adding listener cleanup conflicts with current socket test architecture.

---

## END HANDOFF
