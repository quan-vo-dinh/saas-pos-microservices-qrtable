# Prompt — Step 2.7 Batch 3: Management POS Realtime and Polling

You are implementing Step 2.7 Batch 3 for the QRTable Nx/NestJS monorepo.

## Mission

Move Management POS to the finalized realtime contract:

```txt
staff auth.token
-> no join.staff
-> POS event invalidation
-> 15s polling fallback
-> POS realtime status
```

## Required Inputs

Input from Batch 1 and Batch 2 is required. Read both final outputs before
editing.

Before editing, read:

- `AGENTS.md`
- `docs/specs/business-logic-step-2.7-spec.vi.md`
- `docs/superpowers/plans/2026-05-07-step-2.7-fe-be-realtime.md`
- `docs/superpowers/handoffs/step-2.7/2026-05-07-step-2.7-batch-1-handoff.md`
- `docs/superpowers/handoffs/step-2.7/2026-05-07-step-2.7-batch-2-handoff.md`
- `docs/superpowers/handoffs/step-2.7/2026-05-07-step-2.7-batch-3-handoff.md`
- `docs/specs/business-logic-step-2.6-spec.vi.md`
- `docs/architecture/permission-matrix.md`
- `apps/management-app/src/features/order/hooks/use-staff-order-realtime.ts`
- `apps/management-app/src/features/order/hooks/use-staff-order-realtime.spec.tsx`
- `apps/management-app/src/features/order/hooks/use-order-query.ts`
- `apps/management-app/src/features/order/hooks/__tests__/use-order-query.spec.ts`
- `apps/management-app/src/features/service-requests/hooks/use-service-request-query.ts`
- `apps/management-app/src/features/service-requests/hooks/__tests__/use-service-request-query.spec.ts`
- `apps/management-app/src/components/pos/pos-app-shell.tsx`

## Implementation Rules

- Work on `main`.
- Do not commit.
- Use Context7/ctx7 before changing package-specific Socket.IO Client or
  TanStack Query behavior if current docs are needed.
- Keep namespace `/orders`.
- Management POS must use `auth: { token: accessToken }`.
- Management POS must not emit `join.staff`.
- Treat WebSocket events as invalidation hints; REST snapshots remain source of truth.
- Do not implement menu realtime.
- Do not add batching under any name.
- Do not disable POS polling completely; reduce order/service list fallback to 15s.
- Do not modify KDS board, KDS realtime hook, or batching files.
- Ignore commit snippets in the implementation plan.

## Expected Work

1. Update POS realtime hook tests for no `join.staff`.
2. Add tests for bill-request and kitchen-ready invalidation.
3. Add reconnect/auth/degraded status and listener cleanup tests.
4. Update `useStaffOrderRealtime()` to use staff `auth.token`, return realtime
   status, and invalidate POS order/table/service domains from matching events.
5. Reduce Management POS order/service list polling fallback to 15s.
6. Update polling interval tests.
7. Add Management App realtime status pill and render it in `PosAppShell`.
8. Run focused Management POS tests.

## Required Verification

Run:

```bash
npx nx test management-app --runTestsByPath apps/management-app/src/features/order/hooks/use-staff-order-realtime.spec.tsx
npx nx test management-app --runTestsByPath apps/management-app/src/components/realtime/realtime-status-pill.spec.tsx
npx nx test management-app --runTestsByPath apps/management-app/src/features/order/hooks/__tests__/use-order-query.spec.ts apps/management-app/src/features/service-requests/hooks/__tests__/use-service-request-query.spec.ts
```

If time allows, also run:

```bash
npx nx lint management-app
```

## Required Batch Output

End with a concise handoff summary for Batch 4 containing:

- POS socket auth/listener behavior,
- POS query domains invalidated,
- polling interval changes,
- Management realtime status component API,
- status UI placement,
- files changed,
- tests/lint run and results,
- blockers or risks Batch 4 must know.

Stop and ask if staff token access is unavailable where the POS socket hook
needs it.
