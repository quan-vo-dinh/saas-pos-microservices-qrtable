# Prompt — Step 2.7 Batch 2: Customer PWA Realtime

You are implementing Step 2.7 Batch 2 for the QRTable Nx/NestJS monorepo.

## Mission

Move Customer PWA to the finalized realtime contract:

```txt
auth.tenantId/sessionId
-> no join.session
-> customer event invalidation
-> kitchen item ready refresh
-> non-blocking realtime status
```

## Required Inputs

Input from Batch 1 is required. Read the Batch 1 final output before editing.

Before editing, read:

- `AGENTS.md`
- `docs/specs/business-logic-step-2.7-spec.vi.md`
- `docs/superpowers/plans/2026-05-07-step-2.7-fe-be-realtime.md`
- `docs/superpowers/handoffs/step-2.7/2026-05-07-step-2.7-batch-1-handoff.md`
- `docs/superpowers/handoffs/step-2.7/2026-05-07-step-2.7-batch-2-handoff.md`
- `docs/specs/business-logic-step-2.6-spec.vi.md`
- `apps/customer-pwa/src/features/order/hooks/use-customer-order-realtime.ts`
- `apps/customer-pwa/src/features/order/hooks/use-customer-order-realtime.spec.tsx`
- `apps/customer-pwa/src/components/layout/mobile-shell.tsx`

## Implementation Rules

- Work on `main`.
- Do not commit.
- Use Context7/ctx7 before changing package-specific Socket.IO Client or
  TanStack Query behavior if current docs are needed.
- Keep namespace `/orders`.
- Customer PWA must use `auth: { tenantId, sessionId }`.
- Customer PWA must not emit `join.session`.
- Customer PWA must not listen to KDS station queue events.
- Treat WebSocket events as invalidation hints; REST snapshots remain source of truth.
- Do not add high-frequency polling to Customer PWA.
- Do not implement menu realtime.
- Do not add batching under any name.
- Do not modify BFF or Management App files unless Batch 1 output names a direct blocker.
- Ignore commit snippets in the implementation plan.

## Expected Work

1. Update Customer PWA hook tests for Socket.IO `auth: { tenantId, sessionId }`.
2. Update tests so `connect` does not emit `join.session`.
3. Add tests for `events.kitchenItemReady` invalidation.
4. Add reconnect/auth/degraded status and listener cleanup tests.
5. Update `useCustomerOrderRealtime()` to return realtime status and register
   stable socket/manager listeners with `off` cleanup.
6. Add Customer PWA realtime status pill.
7. Render the status pill from `MobileShell`.
8. Run focused Customer PWA tests.

## Required Verification

Run:

```bash
npx nx test customer-pwa --runTestsByPath src/features/order/hooks/use-customer-order-realtime.spec.tsx src/components/realtime/realtime-status-pill.spec.tsx
```

If time allows, also run:

```bash
npx nx lint customer-pwa
```

## Required Batch Output

End with a concise handoff summary for Batch 3 containing:

- Customer PWA socket auth/listener behavior,
- customer query domains invalidated,
- reconnect/auth/degraded status behavior,
- status UI placement,
- files changed,
- tests/lint run and results,
- blockers or risks Batch 3 must know.

Stop and ask if Customer PWA cannot provide stable `tenantId` and `sessionId`
for socket auth.
