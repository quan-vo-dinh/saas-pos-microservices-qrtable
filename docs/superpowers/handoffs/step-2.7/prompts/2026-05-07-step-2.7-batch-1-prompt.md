# Prompt — Step 2.7 Batch 1: BFF Realtime Auth Contract

You are implementing Step 2.7 Batch 1 for the QRTable Nx/NestJS monorepo.

## Mission

Lock the BFF realtime auth contract:

```txt
Customer socket auth payload
-> customer header fallback
-> rejected join.session/join.staff
-> server-derived rooms only
```

## Required Inputs

There is no previous Step 2.7 batch output for Batch 1.

Before editing, read:

- `AGENTS.md`
- `docs/specs/business-logic-step-2.7-spec.vi.md`
- `docs/superpowers/plans/2026-05-07-step-2.7-fe-be-realtime.md`
- `docs/superpowers/handoffs/step-2.7/2026-05-07-step-2.7-batch-1-handoff.md`
- `docs/specs/business-logic-step-2.6-spec.vi.md`
- `apps/bff/src/app/modules/realtime/services/realtime-auth.service.ts`
- `apps/bff/src/app/modules/realtime/gateways/order-events.gateway.ts`
- `apps/bff/src/app/modules/realtime/tests/realtime-auth.service.spec.ts`
- `apps/bff/src/app/modules/realtime/tests/order-events.gateway.spec.ts`

## Implementation Rules

- Work on `main`.
- Do not commit.
- Use Context7/ctx7 before changing package-specific Socket.IO Client or NestJS
  WebSocket behavior if current docs are needed.
- Keep namespace `/orders`.
- Do not trust client-origin `join.staff` or `join.session`.
- Do not accept client-provided room names.
- Keep Step 2.6 WebSocket event names unchanged.
- Do not implement menu realtime.
- Do not add batching under any name.
- Do not modify frontend files in this batch.
- Ignore commit snippets in the implementation plan.

## Expected Work

1. Add BFF tests for customer `handshake.auth.tenantId/sessionId`.
2. Keep and test legacy `x-tenant-id` and `x-session-id` header fallback.
3. Implement customer socket identity resolution from `handshake.auth`, with
   header fallback preserved.
4. Update legacy join error messages so they point clients to server-managed
   room assignment.
5. Add gateway test coverage that `join.staff` and `join.session` do not join
   client-supplied rooms.
6. Run focused BFF realtime tests.

## Required Verification

Run:

```bash
npx nx test bff --runTestsByPath apps/bff/src/app/modules/realtime/tests/realtime-auth.service.spec.ts apps/bff/src/app/modules/realtime/tests/order-events.gateway.spec.ts
```

## Required Batch Output

End with a concise handoff summary for Batch 2 containing:

- BFF auth payload behavior,
- header fallback behavior,
- legacy join behavior,
- files changed,
- tests run and results,
- blockers or risks Batch 2 must know.

Stop and ask if customer identity/session validation cannot be implemented
without changing the finalized Step 2.7 socket contract.
