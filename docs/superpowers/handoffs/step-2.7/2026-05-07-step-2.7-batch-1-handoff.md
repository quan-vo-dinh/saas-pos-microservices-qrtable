# Step 2.7 — Batch 1 Handoff (BFF Realtime Auth Contract)

**Date:** 2026-05-07  
**Branch:** `main`  
**Status:** Planned handoff, not executed yet  
**Scope:** Task 1 from
`docs/superpowers/plans/2026-05-07-step-2.7-fe-be-realtime.md`

---

## Execution Goal

Lock the BFF realtime auth boundary before frontend hooks are changed:

```txt
BFF accepts customer Socket.IO auth payload
-> header fallback remains available
-> legacy join.session/join.staff remains rejected
-> server-derived rooms stay authoritative
```

This batch touches only BFF realtime auth/gateway behavior. Customer PWA,
Management POS, and KDS are handled in Batches 2-4.

## Required Reading

- `AGENTS.md`
- `docs/specs/business-logic-step-2.7-spec.vi.md`
- `docs/superpowers/plans/2026-05-07-step-2.7-fe-be-realtime.md`
- `docs/superpowers/audits/step-2.7-realtime-audit-report.md`
- `docs/superpowers/audits/step-2.7-realtime-audit-report.vi.md`
- `docs/specs/business-logic-step-2.6-spec.vi.md`
- `apps/bff/src/app/modules/realtime/services/realtime-auth.service.ts`
- `apps/bff/src/app/modules/realtime/gateways/order-events.gateway.ts`
- `apps/bff/src/app/modules/realtime/tests/realtime-auth.service.spec.ts`
- `apps/bff/src/app/modules/realtime/tests/order-events.gateway.spec.ts`

## Batch Tasks

| Deliverable           | Required result                                                                               |
| --------------------- | --------------------------------------------------------------------------------------------- |
| Customer auth payload | Customer socket identity resolves from `handshake.auth.tenantId/sessionId`.                   |
| Header fallback       | Legacy `x-tenant-id` and `x-session-id` customer headers remain supported.                    |
| Legacy joins          | `join.session` and `join.staff` emit `events.authError` and never join client-supplied rooms. |
| Server authority      | FE-provided room names are still ignored. BFF remains the only room assignment authority.     |
| Tests                 | Focused BFF tests cover auth payload, header fallback, and rejected legacy joins.             |

## File Ownership

This batch may create/modify:

- `apps/bff/src/app/modules/realtime/services/realtime-auth.service.ts`
- `apps/bff/src/app/modules/realtime/gateways/order-events.gateway.ts`
- `apps/bff/src/app/modules/realtime/tests/realtime-auth.service.spec.ts`
- `apps/bff/src/app/modules/realtime/tests/order-events.gateway.spec.ts`

Do not modify Customer PWA or Management App files in this batch.

## Implementation Constraints

- Work on `main`; do not commit. The repository owner will review and commit.
- Use Context7/ctx7 for current Socket.IO Client/NestJS WebSocket API behavior
  if package-specific docs are needed.
- Namespace remains `/orders`; do not introduce `/kds`.
- Do not trust client-origin `join.staff` or `join.session`.
- Do not accept client-provided room names.
- Do not implement `events.menuUpdated`, `events.menu.updated`, or `menuUpdated`.
- Do not add batching/gộp món/gom đơn under any name.
- Keep Step 2.6 WebSocket event names unchanged.
- If the implementation plan contains commit snippets, ignore them for this
  handoff and report changed files instead.

## Verification

Run at minimum:

```bash
npx nx test bff --runTestsByPath apps/bff/src/app/modules/realtime/tests/realtime-auth.service.spec.ts apps/bff/src/app/modules/realtime/tests/order-events.gateway.spec.ts
```

Record exact commands and results.

## Handoff Output For Batch 2

Before ending this batch, report:

- customer `handshake.auth` fields accepted,
- header fallback behavior,
- legacy join behavior and emitted error event,
- tests run and results,
- any BFF contract issue Batch 2 must know before Customer PWA hook changes.

## Stop Conditions

Stop and ask before continuing if:

- customer session validation cannot safely use `auth.tenantId/sessionId`,
- header fallback conflicts with the finalized Step 2.7 socket contract,
- rejected legacy joins require changing event names already finalized in Step 2.6.

---

## END HANDOFF
