# Step 2.7 — Batch 3 Handoff (Management POS Realtime and Polling)

**Date:** 2026-05-07  
**Branch:** `main`  
**Status:** Planned handoff, not executed yet  
**Scope:** Task 3, Task 6, and the POS/shared Management status UI slice of
Task 7 from `docs/superpowers/plans/2026-05-07-step-2.7-fe-be-realtime.md`

---

## Execution Goal

Move Management POS to the finalized Step 2.7 realtime contract:

```txt
POS staff socket uses auth.token
-> no join.staff emit
-> order/service/table/bill hints invalidate active snapshots
-> POS polling becomes a 15s fallback
-> POS shows non-blocking realtime status
```

This batch owns POS and shared Management realtime status UI. KDS station
filtering and no-batching removal are handled in Batch 4.

## Required Reading

- Batch 1 final output
- Batch 2 final output
- `docs/superpowers/handoffs/step-2.7/2026-05-07-step-2.7-batch-1-handoff.md`
- `docs/superpowers/handoffs/step-2.7/2026-05-07-step-2.7-batch-2-handoff.md`
- `docs/specs/business-logic-step-2.7-spec.vi.md`
- `docs/superpowers/plans/2026-05-07-step-2.7-fe-be-realtime.md`
- `docs/specs/business-logic-step-2.6-spec.vi.md`
- `docs/architecture/permission-matrix.md`
- `apps/management-app/src/features/order/hooks/use-staff-order-realtime.ts`
- `apps/management-app/src/features/order/hooks/use-staff-order-realtime.spec.tsx`
- `apps/management-app/src/features/order/hooks/use-order-query.ts`
- `apps/management-app/src/features/order/hooks/__tests__/use-order-query.spec.ts`
- `apps/management-app/src/features/service-requests/hooks/use-service-request-query.ts`
- `apps/management-app/src/features/service-requests/hooks/__tests__/use-service-request-query.spec.ts`
- `apps/management-app/src/components/pos/pos-app-shell.tsx`

## Batch Tasks

| Deliverable        | Required result                                                                                  |
| ------------------ | ------------------------------------------------------------------------------------------------ |
| POS staff hook     | Management App connects to `/orders` with `auth: { token: accessToken }`.                        |
| No legacy join     | POS hook no longer emits `join.staff`.                                                           |
| POS event coverage | POS listens to bill-request and kitchen-ready hints in addition to order/service/table hints.    |
| Query invalidation | Matching tenant events invalidate POS order/table/service/bill domains from the Step 2.7 matrix. |
| Reconnect sync     | Connect/reconnect invalidates active POS domains.                                                |
| Cleanup            | Socket and manager listeners are removed safely; remount/reconnect must not duplicate listeners. |
| Polling fallback   | Order/service list polling is reduced to 15s and remains enabled as fallback.                    |
| Status UI          | Management shared realtime status pill exists and POS renders it.                                |
| Tests              | Focused tests cover POS hook, polling interval, and status UI.                                   |

## File Ownership

This batch may create/modify:

- `apps/management-app/src/features/order/hooks/use-staff-order-realtime.ts`
- `apps/management-app/src/features/order/hooks/use-staff-order-realtime.spec.tsx`
- `apps/management-app/src/features/order/hooks/use-order-query.ts`
- `apps/management-app/src/features/order/hooks/__tests__/use-order-query.spec.ts`
- `apps/management-app/src/features/service-requests/hooks/use-service-request-query.ts`
- `apps/management-app/src/features/service-requests/hooks/__tests__/use-service-request-query.spec.ts`
- `apps/management-app/src/components/realtime/realtime-status-pill.tsx`
- `apps/management-app/src/components/realtime/realtime-status-pill.spec.tsx`
- `apps/management-app/src/components/pos/pos-app-shell.tsx`

Do not modify KDS board, KDS realtime hook, or batching files in this batch.

## Implementation Constraints

- Work on `main`; do not commit. The repository owner will review and commit.
- Use Context7/ctx7 for current Socket.IO Client or TanStack Query API behavior
  if package-specific docs are needed.
- Namespace remains `/orders`.
- Management POS must use `auth.token` and must not emit `join.staff`.
- FE must not send room names. Server-side auth/RBAC remains the source of room assignment.
- WebSocket events are invalidation hints only; REST snapshots remain source of truth.
- Do not implement menu realtime.
- Do not add batching/gộp món/gom đơn under any name.
- Do not disable POS polling completely; reduce order/service list fallback to 15s.
- Do not register domain event listeners inside the `connect` handler.
- Cleanup must use the same listener references passed to `on`.
- If the implementation plan contains commit snippets, ignore them for this
  handoff and report changed files instead.

## Verification

Run at minimum:

```bash
npx nx test management-app --runTestsByPath apps/management-app/src/features/order/hooks/use-staff-order-realtime.spec.tsx
npx nx test management-app --runTestsByPath apps/management-app/src/components/realtime/realtime-status-pill.spec.tsx
npx nx test management-app --runTestsByPath apps/management-app/src/features/order/hooks/__tests__/use-order-query.spec.ts apps/management-app/src/features/service-requests/hooks/__tests__/use-service-request-query.spec.ts
```

If time allows, also run:

```bash
npx nx lint management-app
```

Record exact commands and results.

## Handoff Output For Batch 4

Before ending this batch, report:

- POS socket auth/listener behavior,
- POS query domains invalidated by each event,
- polling interval changes,
- Management realtime status component API,
- POS status UI placement,
- tests and lint commands run with results,
- any Management auth/query issue Batch 4 must know before KDS work.

## Stop Conditions

Stop and ask before continuing if:

- staff access token is not available where `useStaffOrderRealtime` needs it,
- reducing POS polling exposes missing realtime events that the backend does not emit,
- POS query keys cannot support the Step 2.7 invalidation matrix without broad refactors.

---

## END HANDOFF
