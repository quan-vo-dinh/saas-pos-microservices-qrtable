# Step 2.7 — Batch 4 Handoff (Management KDS Realtime and No Batching)

**Date:** 2026-05-07  
**Branch:** `main`  
**Status:** Planned handoff, not executed yet  
**Scope:** Task 4, Task 5, and the KDS status UI slice of Task 7 from
`docs/superpowers/plans/2026-05-07-step-2.7-fe-be-realtime.md`

---

## Execution Goal

Move KDS to the finalized Step 2.7 realtime contract and enforce the Step 2.6
no-batching decision:

```txt
KDS filters every realtime hint by tenant + station
-> KDS refetches REST snapshot after hints/reconnect
-> KDS shows non-blocking realtime status
-> batching panel/state/props are removed completely
-> frontend never groups or routes tickets by item name/category
```

This batch owns KDS behavior only. E2E and final verification are handled in
Batch 5.

## Required Reading

- Batch 1 final output
- Batch 2 final output
- Batch 3 final output
- `docs/superpowers/handoffs/step-2.7/2026-05-07-step-2.7-batch-3-handoff.md`
- `docs/specs/business-logic-step-2.7-spec.vi.md`
- `docs/superpowers/plans/2026-05-07-step-2.7-fe-be-realtime.md`
- `docs/specs/business-logic-step-2.6-spec.vi.md`
- `docs/architecture/permission-matrix.md`
- `apps/management-app/src/features/kds/hooks/use-kds-realtime.ts`
- `apps/management-app/src/features/kds/hooks/use-kds-realtime.spec.tsx`
- `apps/management-app/src/components/kds/kds-board.tsx`
- `apps/management-app/src/components/kds/kds-ticket-card.tsx`
- `apps/management-app/src/components/kds/kds-batching-panel.tsx`
- `apps/management-app/src/components/realtime/realtime-status-pill.tsx`
- `apps/management-app/src/components/realtime/realtime-status-pill.spec.tsx`
- `apps/management-app/src/mocks/store.ts`

## Batch Tasks

| Deliverable           | Required result                                                                                          |
| --------------------- | -------------------------------------------------------------------------------------------------------- |
| KDS hook              | `useKdsRealtime(station)` listens to KDS queue, kitchen-ready, SLA, auth, and connection events.         |
| Tenant/station filter | Every KDS-related event is filtered by `tenantId` and current `station`.                                 |
| Reconnect sync        | Connect/reconnect invalidates active `kdsKeys.queue(tenantId, station)` snapshot.                        |
| Cleanup               | Socket and manager listeners are removed safely; remount/reconnect must not duplicate listeners.         |
| Status UI             | KDS renders the Management realtime status pill, extending style only if needed.                         |
| No batching           | Remove `KdsBatchingPanel`, same-item highlight state, grouped UI affordances, imports, and props.        |
| No routing hardcode   | KDS renders backend `station`/ticket data and does not infer routing from item name/category.            |
| Tests                 | Focused tests cover KDS filtering, reconnect, cleanup, status UI changes, and static no-batching policy. |

## File Ownership

This batch may create/modify/delete:

- `apps/management-app/src/features/kds/hooks/use-kds-realtime.ts`
- `apps/management-app/src/features/kds/hooks/use-kds-realtime.spec.tsx`
- `apps/management-app/src/components/kds/kds-board.tsx`
- `apps/management-app/src/components/kds/kds-ticket-card.tsx`
- `apps/management-app/src/components/kds/kds-batching-panel.tsx`
- `apps/management-app/src/components/kds/__tests__/kds-no-batching.spec.ts`
- `apps/management-app/src/components/realtime/realtime-status-pill.tsx`
- `apps/management-app/src/components/realtime/realtime-status-pill.spec.tsx`
- `apps/management-app/src/mocks/store.ts`

Do not modify BFF, Customer PWA, or POS polling files in this batch unless a
previous batch left a documented blocker that directly prevents KDS tests from
passing.

## Implementation Constraints

- Work on `main`; do not commit. The repository owner will review and commit.
- Use Context7/ctx7 for current Socket.IO Client or TanStack Query API behavior
  if package-specific docs are needed.
- Namespace remains `/orders`; do not introduce `/kds`.
- KDS must use staff JWT socket auth from Management App.
- KDS event handling must filter by both `tenantId` and `station`.
- KDS mutations remain strict refetch-after-mutation; do not implement full optimistic board movement.
- Do not implement menu realtime.
- Do not add batching/gộp món/gom đơn under any name.
- Do not route KDS items by category/name heuristics.
- Do not register domain event listeners inside the `connect` handler.
- Cleanup must use the same listener references passed to `on`.
- If the implementation plan contains commit snippets, ignore them for this
  handoff and report changed files instead.

## Verification

Run at minimum:

```bash
npx nx test management-app --runTestsByPath apps/management-app/src/features/kds/hooks/use-kds-realtime.spec.tsx
npx nx test management-app --runTestsByPath apps/management-app/src/components/kds/__tests__/kds-no-batching.spec.ts
npx nx test management-app --runTestsByPath apps/management-app/src/components/realtime/realtime-status-pill.spec.tsx
rg -n "Batching|batching|KdsBatchingPanel|kdsHighlightedItemName|setKdsHighlightedItemName|same-item|grouped quantity" apps/management-app/src
```

Expected: tests pass and the `rg` scan returns no matches.

If time allows, also run:

```bash
npx nx lint management-app
```

Record exact commands and results.

## Handoff Output For Batch 5

Before ending this batch, report:

- KDS socket auth/listener behavior,
- KDS tenant/station filtering behavior,
- KDS query invalidation behavior,
- status UI placement,
- files removed/changed for no-batching,
- no-batching scan result,
- tests and lint commands run with results,
- any Management route/auth issue Batch 5 must know for E2E.

## Stop Conditions

Stop and ask before continuing if:

- staff access token is not available where `useKdsRealtime` needs it,
- KDS current route cannot determine station without item category/name routing,
- removing batching requires changing backend Step 2.6 contracts,
- KDS snapshot/query keys cannot support station-specific invalidation.

---

## END HANDOFF
