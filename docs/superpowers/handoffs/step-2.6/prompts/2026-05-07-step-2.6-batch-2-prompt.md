# Prompt — Step 2.6 Batch 2: Kitchen Core

You are implementing Step 2.6 Batch 2 for the QRTable Nx/NestJS monorepo.

## Mission

Create Kitchen Service core runtime: scaffold `apps/kitchen`, implement Redis KDS
keys/score/repository, consume Kafka `order.confirmed`, create tickets
idempotently, publish internal queue-change events, and expose Kitchen TCP
lifecycle handlers.

## Required Inputs

Input from Batch 1 is required. Read the Batch 1 final output before editing.

Before editing, read:

- `AGENTS.md`
- `docs/specs/business-logic-step-2.6-spec.vi.md`
- `docs/superpowers/plans/2026-05-07-step-2.6-kitchen-websocket.md`
- `docs/superpowers/handoffs/step-2.6/2026-05-07-step-2.6-batch-1-handoff.md`
- `docs/superpowers/handoffs/step-2.6/2026-05-07-step-2.6-batch-2-handoff.md`
- `apps/order/project.json`
- `apps/order/src/main.ts`
- `apps/order/src/configuration/index.ts`
- `apps/order/src/app/app.module.ts`
- `libs/providers/redis-client/src/lib/redis-client.service.ts`
- `libs/constants/src/lib/enum/tcp-request-message.ts`
- `libs/interfaces/src/lib/tcp/kitchen/`
- `libs/shared/types/src/lib/kds.types.ts`

## Implementation Rules

- Work on `main`.
- Do not commit.
- Use Context7/ctx7 for NestJS/KafkaJS specifics if API details are uncertain.
- Do not modify BFF or Order Service behavior.
- Do not implement SLA worker or recovery service yet.
- Do not add a Kitchen database.
- Do not implement batching under any name.
- Publish `kds.queue_changed` only after Redis mutation succeeds.

## Expected Work

1. Scaffold `apps/kitchen` manually from the current `apps/order` pattern.
2. Add Kitchen config/bootstrap/root module.
3. Implement KDS Redis key builders.
4. Implement priority-first FIFO queue score helper.
5. Implement `KdsRedisRepository`.
6. Implement `KitchenEventsPublisher`.
7. Implement `OrderConfirmedConsumer` with Kitchen Kafka client/group config.
8. Create idempotent tickets from `order.confirmed`.
9. Implement `KdsTicketService` lifecycle methods.
10. Add Kitchen TCP handlers for queue/start/mark-ready/recall/priority/void/
    patch/rebuild pattern stubs where needed.
11. Add tests for keys, score, Kafka idempotency, lifecycle, and no-batching
    writes.

## Required Verification

Run:

```bash
npx nx build kitchen
npx nx test kitchen --testFile=apps/kitchen/src/app/modules/kitchen/tests/kds-keys.spec.ts
npx nx test kitchen --testFile=apps/kitchen/src/app/modules/kitchen/tests/kds-score.spec.ts
npx nx test kitchen --testFile=apps/kitchen/src/app/modules/kitchen/tests/order-confirmed.consumer.spec.ts
npx nx test kitchen --testFile=apps/kitchen/src/app/modules/kitchen/tests/kds-ticket.service.spec.ts
rg -n "prepSignature|activeQuantity|batchId|batchTotal|batchQuantity|batch" apps/kitchen
```

The `rg` command should have no matches except tests that explicitly assert
batching is forbidden.

## Required Batch Output

End with a concise handoff summary for Batch 3 containing:

- Kitchen scaffold status,
- Redis key and ticket ID formats,
- dedupe strategy,
- lifecycle methods implemented,
- internal event channel/payload,
- tests/build run and results,
- known limitations for SLA/recovery work.

Stop and ask if Kitchen cannot be built without generator output, or if
idempotent Redis writes require changing the shared contract from Batch 1.
