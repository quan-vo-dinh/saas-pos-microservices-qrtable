# Prompt — Step 2.6 Batch 1: Contracts, Permissions, Shared Types

You are implementing Step 2.6 Batch 1 for the QRTable Nx/NestJS monorepo.

## Mission

Create the contract foundation for Step 2.6: dependencies, env values, Kafka/TCP
configuration, RBAC, KDS shared types, Kitchen TCP interfaces, Order KDS TCP
interfaces, and KDS gateway DTOs.

## Required Inputs

There is no previous Step 2.6 batch output for Batch 1.

Before editing, read:

- `AGENTS.md`
- `docs/specs/business-logic-step-2.6-spec.vi.md`
- `docs/superpowers/plans/2026-05-07-step-2.6-kitchen-websocket.md`
- `docs/superpowers/handoffs/step-2.6/2026-05-07-step-2.6-batch-1-handoff.md`
- `docs/architecture/permission-matrix.md`
- `docs/references/auth-system-reference.md`
- `libs/configuration/src/lib/kafka.config.ts`
- `libs/configuration/src/lib/tcp.config.ts`
- `libs/constants/src/lib/enum/tcp-request-message.ts`
- `libs/constants/src/lib/enum/role.enum.ts`
- `libs/shared/types/src/lib/realtime-events.types.ts`
- `libs/interfaces/src/lib/tcp/order/order-request.interface.ts`
- `libs/interfaces/src/lib/tcp/order/order-response.interface.ts`
- `apps/user-access/src/seeder/role.json`
- `apps/user-access/src/seeder/role.spec.ts`
- `apps/bff/src/app/guards/permission.guard.spec.ts`

## Implementation Rules

- Work on `main`.
- Do not commit.
- Use Context7/ctx7 before changing package-specific Socket.IO/NestJS API usage
  if current docs are needed.
- Do not create Kitchen Service runtime files yet.
- Do not implement BFF endpoints or WebSocket behavior yet.
- Do not add batching under any name.
- Canonical SLA topic is `kitchen.sla_warning`.
- Grant `KITCHEN_SET_PRIORITY` only to SUPER_ADMIN, OWNER, and MANAGER.

## Expected Work

1. Add `@socket.io/redis-adapter` and `redis`.
2. Add Kitchen/Kafka/KDS env keys to `.env` and `.env.example`.
3. Extend Kafka config with Kitchen and BFF bridge values.
4. Add `TCP_SERVICES.KITCHEN_SERVICE`.
5. Add `TCP_REQUEST_MESSAGE.KITCHEN` group.
6. Add Order KDS TCP message constants.
7. Add `PERMISSION.KITCHEN_SET_PRIORITY`.
8. Update `role.json`, `role.spec.ts`, `permission.guard.spec.ts`, and
   `permission-matrix.md`.
9. Create/export `libs/shared/types/src/lib/kds.types.ts`.
10. Create/export Kitchen TCP request/response interfaces.
11. Extend Order TCP request/response interfaces for KDS active snapshots and
    item readiness.
12. Create/export KDS gateway DTOs.

## Required Verification

Run:

```bash
npx nx test user-access --testFile=apps/user-access/src/seeder/role.spec.ts
npx nx test bff --testFile=apps/bff/src/app/guards/permission.guard.spec.ts
npx nx test shared-types
```

If available, also run:

```bash
npx nx test interfaces
```

## Required Batch Output

End with a concise handoff summary for Batch 2 containing:

- packages added,
- env keys added,
- config/constants/interfaces/types added,
- permission matrix changes,
- tests run and results,
- any type or package issue Batch 2 must know.

Stop and ask if shared KDS types conflict with existing Phase 2A exports or if
the permission matrix contradicts OWNER/MANAGER-only priority.
