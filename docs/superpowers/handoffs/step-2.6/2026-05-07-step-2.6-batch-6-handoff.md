# Step 2.6 — Batch 6 Handoff (Management KDS Integration and Final Verification)

**Date:** 2026-05-07  
**Branch:** `main`  
**Status:** Planned handoff, not executed yet  
**Scope:** Task 13 and Task 14 from
`docs/superpowers/plans/2026-05-07-step-2.6-kitchen-websocket.md`

---

## Execution Goal

Wire the Management App KDS screen to the real BFF KDS APIs and close Step 2.6
with full verification:

```txt
real KDS queue snapshot
→ realtime invalidation hints
→ reconnect refetch
→ no-batching/topic guardrails
→ backend builds/tests
→ smoke flow
```

This batch should keep UI scope narrow and preserve the existing Step 2.2 KDS
visual workflow.

## Required Reading

- Batch 5 final output
- `docs/superpowers/handoffs/step-2.6/2026-05-07-step-2.6-batch-5-handoff.md`
- `docs/specs/business-logic-step-2.6-spec.vi.md`
- `docs/superpowers/plans/2026-05-07-step-2.6-kitchen-websocket.md`
- `docs/superpowers/plans/2026-04-24-step-2.2-mock-ui.md`
- `docs/architecture/permission-matrix.md`
- `apps/management-app/src/app/(kds)/kds/kitchen/page.tsx`
- `apps/management-app/src/lib/api/authenticated-client.ts`
- `apps/management-app/src/lib/auth/role-routing.ts`
- `apps/management-app/src/features/order/hooks/use-staff-order-realtime.ts`
- `apps/customer-pwa/src/features/order/hooks/use-customer-order-realtime.ts`
- `libs/shared/types/src/lib/kds.types.ts`

## Batch Tasks

| Deliverable   | Required result                                                                       |
| ------------- | ------------------------------------------------------------------------------------- |
| KDS service   | Add typed Management App calls for queue/start/done/recall/priority.                  |
| Query hook    | Fetch real `KdsQueueSnapshot` by station and expose loading/error states.             |
| Realtime hook | Listen for `events.kdsQueueChanged` and `events.kitchenSlaWarning`; refetch snapshot. |
| Reconnect     | Refetch queue snapshot on socket reconnect; do not rely on replay.                    |
| UI wiring     | Replace primary KDS mock queue source with real API while preserving layout.          |
| Bar route     | Add `/kds/bar` route if missing and use the same real data path.                      |
| Tests         | Cover service methods, query/refetch behavior, and realtime invalidation.             |
| Final checks  | Run backend/frontend tests, builds, no-batching scan, topic scan, and smoke flow.     |

## File Ownership

This batch may create/modify:

- `apps/management-app/src/app/(kds)/kds/kitchen/page.tsx`
- `apps/management-app/src/app/(kds)/kds/bar/page.tsx`
- `apps/management-app/src/features/kds/services/kds.service.ts`
- `apps/management-app/src/features/kds/hooks/use-kds-queue.ts`
- `apps/management-app/src/features/kds/hooks/use-kds-realtime.ts`
- `apps/management-app/src/features/kds/hooks/use-kds-queue.spec.tsx`
- Existing KDS page/component tests if present
- Small backend fixes only if final verification exposes regressions from
  Batches 1-5

Do not redesign the KDS page in this batch.

## Implementation Constraints

- Use existing `authApiClient`; do not create a second authenticated HTTP client.
- Preserve existing management role routing. Fine-grained button permissions are
  not required beyond endpoint-level BFF enforcement unless existing app
  patterns already expose permissions.
- WebSocket events are invalidation hints only; always refetch snapshot.
- Do not add optimistic local prep state as source of truth.
- Do not add batching UI or backend contract.
- Keep error handling honest; do not pretend a ticket changed if API mutation
  failed.
- Priority action should be available only where the authenticated role can pass
  BFF permission; handle 403 gracefully.

## Verification

Run at minimum:

```bash
npx nx test management-app
npx nx test kitchen
npx nx test order
npx nx test bff
npx nx test user-access
npx nx test shared-types
npx nx build kitchen
npx nx build order
npx nx build bff
```

Run guard scans:

```bash
rg -n "kitchen\\.sla_warn" apps libs .env .env.example
rg -n "prepSignature|activeQuantity|batchId|batchTotal|batchQuantity|kds:\\{tid\\}:batch|station:\\{station\\}:batches" apps libs
```

Expected:

- no `kitchen.sla_warn` matches,
- no batching implementation matches.

If providers are available, run the smoke flow:

```txt
confirm order
→ Kitchen Redis ticket appears
→ KDS socket receives events.kdsQueueChanged
→ GET /api/v1/admin/kds/queue returns snapshot
→ start ticket
→ done ticket
→ customer receives events.kitchenItemReady only after Order readiness succeeds
→ lowered SLA threshold produces events.kitchenSlaWarning
```

Record exact commands, results, and any provider/smoke blockers.

## Final Handoff Output

Before ending this batch, produce a final Step 2.6 summary with:

- frontend files wired to real KDS API,
- queue query keys and realtime refetch behavior,
- KDS REST endpoints used,
- WebSocket events handled,
- all automated verification commands and results,
- smoke flow results or blockers,
- no-batching scan result,
- `kitchen.sla_warning` topic scan result,
- known residual risks and deferred hardening.

## Stop Conditions

Stop and ask before continuing if:

- Batch 5 did not expose stable KDS REST endpoints,
- KDS snapshot response lacks fields required by the existing KDS board,
- realtime auth requires a Management App auth shape not available to the socket
  client,
- final verification reveals a cross-service contract mismatch that cannot be
  fixed narrowly.

---

## END HANDOFF
