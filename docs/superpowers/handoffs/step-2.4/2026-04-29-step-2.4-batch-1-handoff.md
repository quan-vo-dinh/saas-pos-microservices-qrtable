# Step 2.4 — Batch 1 handoff (Foundations and shared contracts)

**Date:** 2026-04-29  
**Branch:** `main`  
**Status:** Planned handoff, not executed yet  
**Scope:** Tasks 1, 2, and 5 from `docs/superpowers/plans/step-2.4-implementation-plan.md`

---

## Execution Goal

Prepare Step 2.4 shared foundations before touching Catalog, Order Service, or
BFF runtime flows.

This batch owns dependency/env setup, Kafka config, the direct Redis command
client provider, shared realtime/session type alignment, and Order TCP
constants/interfaces.

## Required Reading

- `docs/business-logic-step-2.4-spec.vi.md`
- `docs/superpowers/specs/2026-04-28-step-2.4-architecture-decisions.md`
- `docs/superpowers/plans/step-2.4-implementation-plan.md`
- `docs/guides/kafka-qrtable.md`
- `libs/configuration/src/lib/redis.config.ts`
- `libs/configuration/src/lib/tcp.config.ts`
- `libs/constants/src/lib/enum/tcp-request-message.ts`
- `libs/interfaces/src/lib/tcp/catalog/`
- `libs/shared/types/src/lib/session.types.ts`
- `libs/shared/types/src/lib/realtime-events.types.ts`

## Batch Tasks

| Plan task | Deliverable                                  | Notes                                                        |
| --------- | -------------------------------------------- | ------------------------------------------------------------ |
| Task 1    | Runtime deps/env/Kafka/Redis client provider | Reuse existing Redis config; do not replace `RedisProvider`. |
| Task 2    | Shared cart/realtime types                   | Align with Step 2.4 canonical event payloads.                |
| Task 5    | Order TCP constants/interfaces               | Define TCP contract before Order/BFF wiring.                 |

## File Ownership

This batch may modify:

- `package.json`
- `pnpm-lock.yaml`
- `.env`
- `.env.example`
- `tsconfig.base.json`
- `libs/configuration/src/lib/kafka.config.ts`
- `libs/providers/redis-client/`
- `libs/shared/types/src/lib/session.types.ts`
- `libs/shared/types/src/lib/realtime-events.types.ts`
- `libs/shared/types/src/index.ts`
- `libs/constants/src/lib/enum/tcp-request-message.ts`
- `libs/interfaces/src/lib/tcp/order/`

Do not modify Catalog, Order app, or BFF modules in this batch except when a
compile error proves a shared export is missing.

## Implementation Constraints

- Add `ioredis` only for command-level Redis operations.
- The direct Redis provider must be `libs/providers/redis-client`, not
  `libs/providers/redis`.
- Keep existing cache-manager Redis config in
  `libs/configuration/src/lib/redis.config.ts`.
- Use existing path alias style. The plan currently expects
  `@common/providers/redis-client/*`.
- Event payload types must include `tenantId`, `sessionId`, `tableId`, and
  stable `eventType` fields where required by the business spec.

## Verification

Run at minimum:

```bash
npx prettier --check docs/superpowers/plans/step-2.4-implementation-plan.md docs/superpowers/specs/2026-04-28-step-2.4-architecture-decisions.md
npx nx build shared-types
npx nx build interfaces
npx nx build constants
npx nx build bff --configuration=development
```

If project names differ, use `npx nx show projects` and run the equivalent
build target. Record the exact command names in the session summary.

## Handoff Output For Batch 2

Before ending this batch, update the final response or a follow-up note with:

- dependency list actually installed,
- Redis provider import path,
- Order TCP constants added,
- Order TCP request/response interfaces added,
- shared type names and exported paths,
- any compile issues deferred to later batches.

## Stop Conditions

Stop and ask before continuing if:

- existing Redis env/config names do not match `RedisConfiguration`,
- Order TCP contracts conflict with the canonical business spec,
- `@einvoice/types` exports cannot be updated without breaking existing Step 2.2
  mock UI types,
- dependency installation fails because of package/version conflict.

---

## END HANDOFF
