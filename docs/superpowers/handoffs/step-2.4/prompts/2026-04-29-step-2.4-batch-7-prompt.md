# Prompt — Step 2.4 Batch 7: BFF Direct REST, WebSocket, E2E verification

You are implementing Step 2.4 Batch 7 for the QRTable Nx/NestJS monorepo.

## Mission

Expose Step 2.4 through BFF Direct REST endpoints and a minimal WebSocket
gateway, then verify the full Step 2.4 backend flow.

Batch scope:

- Task 13: BFF gateway DTOs, REST controllers, direct WebSocket gateway.
- Task 14: end-to-end verification.

## Required Inputs

Batch 6 output is required input. Before editing, read the Batch 6 final summary
from the previous session/conversation. Confirm it includes:

- Order TCP handlers,
- service method signatures,
- direct event payloads,
- state transition behavior,
- Order app build/test status.

Then read:

- `AGENTS.md`
- `docs/business-logic-step-2.4-spec.vi.md`
- `docs/superpowers/specs/2026-04-28-step-2.4-architecture-decisions.md`
- `docs/superpowers/plans/step-2.4-implementation-plan.md`
- `docs/architecture/permission-matrix.md`
- `docs/references/auth-system-reference.md`
- `docs/superpowers/handoffs/step-2.4/2026-04-29-step-2.4-batch-6-handoff.md`
- `docs/superpowers/handoffs/step-2.4/2026-04-29-step-2.4-batch-7-handoff.md`
- `apps/bff/src/app/app.module.ts`
- `apps/bff/src/app/modules/catalog/catalog.module.ts`
- `apps/bff/src/app/modules/catalog/controllers/`
- `apps/bff/src/configuration/index.ts`
- `libs/guards/`
- `libs/constants/src/lib/enum/tcp-request-message.ts`
- `libs/interfaces/src/lib/tcp/order/`
- `libs/interfaces/src/lib/gateway/order/`

## Implementation Rules

- Work on `main`.
- Do not commit.
- Do not implement BFF Kafka consumer bridge.
- Do not implement KDS Kafka rooms, Kitchen Service, or Socket.IO Redis Adapter.
- Staff endpoints must use `@Authorization({ secured: true })` and
  `@Permissions(...)`.
- Effective protected chain should remain
  `UserGuard -> TenantGuard -> PermissionGuard`.
- Customer endpoints use session/tenant context and rely on Order Service for
  session ownership enforcement.
- BFF emits WebSocket events directly only after successful TCP responses.
- Route permission mapping must follow `docs/architecture/permission-matrix.md`.

## Expected Work

1. Add gateway order DTOs.
2. Add `OrderModule` in BFF.
3. Add customer order controller.
4. Add staff order controller with permissions.
5. Add realtime module, gateway, and event service.
6. Wire BFF app module/config.
7. Run automated verification.
8. Run or document manual E2E flows:
   - submit then confirm,
   - cart version conflict,
   - no oversell,
   - bill request,
   - table transfer.

## Required Verification

```bash
npx nx test shared-types
npx nx test catalog
npx nx test order
npx nx test bff
npx nx build catalog --configuration=development
npx nx build order --configuration=development
npx nx build bff --configuration=development
```

Then, if local infra is available:

```bash
docker compose -f docker-compose.provider.yaml up -d postgres redis kafka
pnpm dev:bff-order
```

Record exact failures if infra/manual verification cannot run.

## Required Final Output

End with the final Step 2.4 implementation summary containing:

- completed endpoint list,
- permission mapping applied,
- WebSocket rooms/events implemented,
- automated verification commands and results,
- manual flow results or reason they could not run,
- known residual risks,
- deferred work confirmed out of scope,
- files changed grouped by service/lib.

Stop and ask if permission constants are missing, guard behavior differs from
the decision note, or Order TCP responses do not expose event payloads needed by
BFF Direct.
