# Prompt — Step 2.4 Batch 3: Order service scaffold, entities, repositories

You are implementing Step 2.4 Batch 3 for the QRTable Nx/NestJS monorepo.

## Mission

Create the Order Service app and persistence layer. Do not implement domain
business flows yet.

Batch scope:

- Task 6: scaffold Order Service app.
- Task 7: add Order domain entities.
- Task 8: add tenant-scoped repositories and module wiring.

## Required Inputs

Batch 2 output is required input. Before editing, read the Batch 2 final summary
from the previous session/conversation. Confirm it includes:

- station enum/entity/DTO changes,
- Catalog TCP pattern names,
- stock mutation strategy,
- Catalog verification results.

Then read:

- `AGENTS.md`
- `docs/business-logic-step-2.4-spec.vi.md`
- `docs/superpowers/specs/2026-04-28-step-2.4-architecture-decisions.md`
- `docs/superpowers/plans/step-2.4-implementation-plan.md`
- `docs/superpowers/handoffs/step-2.4/2026-04-29-step-2.4-batch-2-handoff.md`
- `docs/superpowers/handoffs/step-2.4/2026-04-29-step-2.4-batch-3-handoff.md`
- `apps/catalog/project.json`
- `apps/catalog/src/app/app.module.ts`
- `apps/catalog/src/main.ts`
- `apps/catalog/src/configuration/index.ts`
- `libs/entities/src/lib/base.entity.ts`
- `libs/configuration/src/lib/type-orm.config.ts`
- `libs/configuration/src/lib/tcp.config.ts`

## Implementation Rules

- Work on `main`.
- Do not commit.
- Mirror Catalog/Product Nx/NestJS conventions.
- Use one bounded-context module:
  `apps/order/src/app/modules/order/order.module.ts`.
- Use folders `controllers/`, `services/`, `repositories/`, `tests/`.
- Entity class names must be `Order`, `OrderItem`, `Session`, `Bill`,
  `ServiceRequest`, `OutboxEvent`.
- Do not use `OrderEntity`, `SessionEntity`, or similar suffixes.
- Every Order table must include `tenant_id`.
- Do not add foreign keys to Catalog-owned tables.
- Do not query Catalog tables from Order Service.
- Support `ORDER_TYPEORM_DATABASE` with fallback to current shared DB convention.

## Expected Work

1. Scaffold `apps/order`.
2. Add Order app config, HTTP/TCP bootstrap, Nx project config.
3. Add `ORDER_SERVICE` to TCP config.
4. Add Order domain entities.
5. Add tenant-scoped repositories.
6. Wire `OrderModule`, TypeORM feature entities, Catalog TCP client, and
   `RedisClientModule`.
7. Add `dev:bff-order` script if missing.

## Required Verification

```bash
npx nx build entities
npx nx build order --configuration=development
```

If app generation changes unrelated workspace files, keep only the minimum
required scaffold and report what was kept.

## Required Batch Output

End with a handoff summary for Batch 4 containing:

- Order app project name,
- serve/build commands,
- HTTP/TCP ports,
- entities/table names,
- repository file list,
- module imports/providers,
- `RedisClientModule` import path,
- verification results,
- generated Nx config caveats.

Stop and ask if Nx generation conflicts with Catalog/Product conventions or
TypeORM config cannot be aligned safely.
