# Prompt — Step 2.4 Batch 1: Foundations and shared contracts

You are implementing Step 2.4 Batch 1 for the QRTable Nx/NestJS monorepo.

## Mission

Implement the foundations and shared contracts required before Catalog, Order
Service, and BFF runtime work begins.

Batch scope:

- Task 1: runtime dependencies, env keys, Kafka config, direct Redis command
  client provider.
- Task 2: shared cart/session/realtime type alignment.
- Task 5: Order TCP constants and interfaces.

## Required Inputs

There is no previous batch output for Batch 1.

Before editing, read these files:

- `AGENTS.md`
- `docs/business-logic-step-2.4-spec.vi.md`
- `docs/phases/phase-2a-order-kafka.md`
- `docs/superpowers/specs/2026-04-28-step-2.4-architecture-decisions.md`
- `docs/superpowers/plans/step-2.4-implementation-plan.md`
- `docs/superpowers/handoffs/step-2.4/2026-04-29-step-2.4-batch-1-handoff.md`
- `docs/guides/kafka-qrtable.md`
- `libs/configuration/src/lib/redis.config.ts`
- `libs/configuration/src/lib/tcp.config.ts`
- `libs/constants/src/lib/enum/tcp-request-message.ts`
- `libs/shared/types/src/lib/session.types.ts`
- `libs/shared/types/src/lib/realtime-events.types.ts`

## Implementation Rules

- Work on `main`.
- Do not commit.
- Do not create an Order app in this batch.
- Do not modify Catalog/BFF runtime modules unless a shared export compile error
  proves it is required.
- Add `ioredis` only as a direct Redis command client dependency.
- Do not replace `RedisProvider` in `libs/configuration/src/lib/redis.config.ts`.
- Create the direct Redis provider under `libs/providers/redis-client`, not
  `libs/providers/redis`.
- Keep TCP and event payloads aligned with
  `docs/business-logic-step-2.4-spec.vi.md`.
- Use existing path alias and interface conventions.

## Expected Work

1. Install or verify required packages:
   `kafkajs`, `@nestjs/websockets`, `@nestjs/platform-socket.io`, `socket.io`,
   `ioredis`.
2. Add Order/Kafka env keys to `.env` and `.env.example`.
3. Add `libs/configuration/src/lib/kafka.config.ts`.
4. Add `libs/providers/redis-client` using existing Redis config values.
5. Add `@common/providers/redis-client/*` path alias.
6. Align shared cart/session/realtime types.
7. Add Order TCP constants and request/response interfaces.

## Required Verification

Run the strongest valid subset available in this repo:

```bash
npx nx build shared-types
npx nx build interfaces
npx nx build constants
npx nx build bff --configuration=development
```

If a project name differs, run `npx nx show projects` and use the equivalent
target. Record the exact command and result.

## Required Batch Output

End with a concise handoff summary for Batch 2 containing:

- packages installed or already present,
- env keys added,
- Redis provider import path,
- Order TCP constants added,
- Order TCP interfaces added,
- shared types changed/exported,
- verification commands and results,
- unresolved blockers or risks.

Stop and ask if a contract conflicts with the business spec or existing shared
types cannot be safely updated.
