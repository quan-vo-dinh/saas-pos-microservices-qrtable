# Prompt — Step 2.4 Batch 2: Catalog prerequisites

You are implementing Step 2.4 Batch 2 for the QRTable Nx/NestJS monorepo.

## Mission

Prepare Catalog Service for Order Service by adding preparation station metadata
and Catalog-owned orderability/stock TCP commands.

Batch scope:

- Task 3: `MenuItem.station` and admin DTO/response wiring.
- Task 4: Catalog orderability and stock TCP contracts.

## Required Inputs

Batch 1 output is required input. Before editing, read the Batch 1 final summary
from the previous session/conversation. Confirm it includes:

- dependency/env changes,
- `libs/providers/redis-client` status,
- Order TCP constants/interfaces,
- shared type exports,
- verification results.

Then read:

- `AGENTS.md`
- `docs/business-logic-step-2.4-spec.vi.md`
- `docs/superpowers/specs/2026-04-28-step-2.4-architecture-decisions.md`
- `docs/superpowers/plans/step-2.4-implementation-plan.md`
- `docs/superpowers/handoffs/step-2.4/2026-04-29-step-2.4-batch-1-handoff.md`
- `docs/superpowers/handoffs/step-2.4/2026-04-29-step-2.4-batch-2-handoff.md`
- `docs/superpowers/specs/2026-04-11-catalog-service-backend-design.md`
- `apps/catalog/src/app/modules/menu-item/menu-item.module.ts`
- `apps/catalog/src/app/modules/menu-item/controllers/menu-item.controller.ts`
- `apps/catalog/src/app/modules/menu-item/services/menu-item.service.ts`
- `apps/catalog/src/app/modules/menu-item/repositories/menu-item.repository.ts`
- `apps/catalog/src/app/modules/menu-item/tests/menu-item.service.spec.ts`

## Implementation Rules

- Work on `main`.
- Do not commit.
- Do not create or edit Order Service in this batch.
- Keep Catalog module structure unchanged:
  `controllers/`, `services/`, `repositories/`, `tests/`.
- Every Catalog query/mutation must include `tenantId`.
- Catalog owns stock. Order Service must never update `menu_items` directly.
- Use transactional/pessimistic stock mutation or an equivalent no-oversell
  approach.
- Do not introduce unrelated Catalog refactors.

## Expected Work

1. Add preparation station enum/constant.
2. Add `station` to `MenuItem` entity and Catalog DTO/response contracts.
3. Add TCP contracts for:
   - `MENU_ITEM.VALIDATE_ORDERABLE`
   - `MENU_ITEM.STOCK_DEDUCT_FOR_ORDER`
   - `MENU_ITEM.STOCK_RELEASE_FOR_ORDER`
4. Implement controller handlers in Catalog.
5. Implement service/repository stock logic with tenant scoping.
6. Extend `menu-item.service.spec.ts`.

## Required Verification

```bash
npx nx test catalog --testPathPattern=menu-item.service.spec.ts
npx nx build catalog --configuration=development
npx nx build interfaces
npx nx build entities
```

Use equivalent project names if needed and record exact results.

## Required Batch Output

End with a handoff summary for Batch 3 containing:

- station enum values,
- `MenuItem.station` behavior/default,
- Catalog TCP pattern names implemented,
- request/response contract names,
- stock lock/update strategy,
- tests added,
- verification results,
- any durable idempotency limitation.

Stop and ask if Batch 1 contracts are missing or stock semantics conflict with
the business spec.
